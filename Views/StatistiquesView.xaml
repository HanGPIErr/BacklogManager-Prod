<UserControl x:Class="BacklogManager.Views.StatistiquesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F5F5F5">
    <UserControl.Resources>
        <!-- Converter pour la visibilité -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Converter pour inverser la visibilité -->
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"
             xmlns:local="clr-namespace:BacklogManager.Shared"/>
        
        <!-- Style des cartes -->
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#D0D0D0" BlurRadius="16" ShadowDepth="0" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Page principale des statistiques -->
        <ScrollViewer x:Name="ScrollViewerStatsPrincipales" VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding AfficherPagePrincipale, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Margin="20">
                <!-- En-tête -->
                <Border CornerRadius="10" Padding="25" Margin="0,0,0,25">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#00915A" Offset="0"/>
                            <GradientStop Color="#00A566" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.2" Color="#00915A"/>
                    </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="📊 Tableau de bord" 
                                   FontSize="28" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Vue d'ensemble des performances et statistiques" 
                                   FontSize="14" Foreground="White" Opacity="0.9" Margin="0,5,0,0"/>
                        <!-- Filtres de période -->
                        <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                            <TextBlock Text="🗓 Période :" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0"/>
                            <ComboBox x:Name="CboPeriode" Width="180" VerticalAlignment="Center" Background="White"
                                      SelectedIndex="{Binding PeriodeSelectionneeIndex, Mode=TwoWay}"
                                      SelectionChanged="CboPeriode_SelectionChanged">
                                <ComboBoxItem Content="📅 Année en cours"/>
                                <ComboBoxItem Content="📆 6 derniers mois"/>
                                <ComboBoxItem Content="📌 Mois en cours"/>
                                <ComboBoxItem Content="🔖 3 derniers mois"/>
                                <ComboBoxItem Content="⏱ Période personnalisée"/>
                                <ComboBoxItem Content="🌐 Tout afficher"/>
                            </ComboBox>
                            <StackPanel x:Name="PanelDatesCustom" Orientation="Horizontal" Margin="15,0,0,0" Visibility="Collapsed">
                                <TextBlock Text="Du :" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <DatePicker x:Name="DateDebut" Width="130" VerticalAlignment="Center"
                                            SelectedDate="{Binding DateDebutFiltre, Mode=TwoWay}"/>
                                <TextBlock Text="Au :" VerticalAlignment="Center" Margin="15,0,5,0"/>
                                <DatePicker x:Name="DateFin" Width="130" VerticalAlignment="Center"
                                            SelectedDate="{Binding DateFinFiltre, Mode=TwoWay}"/>
                                <Button Content="✓" Padding="8,5" Margin="10,0,0,0" 
                                        Background="#00915A" Foreground="White" BorderThickness="0"
                                        Click="BtnAppliquerPeriode_Click" Cursor="Hand"
                                        ToolTip="Appliquer la période">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="🔄 Rafraîchir" 
                                Command="{Binding RafraichirCommand}"
                                Background="White" Foreground="#00915A" 
                                Padding="15,10" FontWeight="SemiBold" FontSize="13"
                                BorderThickness="2" BorderBrush="White" Cursor="Hand" VerticalAlignment="Center"
                                Margin="0,0,10,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="8" 
                                                        Padding="{TemplateBinding Padding}">
                                                    <Border.Effect>
                                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="White"/>
                                                    </Border.Effect>
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F0F0F0"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="🤖 Analyse IA" 
                                Click="BtnAnalyserIA_Click"
                                Background="White" Foreground="#667eea" 
                                Padding="15,10" FontWeight="SemiBold" FontSize="13"
                                BorderThickness="2" BorderBrush="White" Cursor="Hand" VerticalAlignment="Center"
                                Margin="0,0,10,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="8" 
                                                        Padding="{TemplateBinding Padding}">
                                                    <Border.Effect>
                                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="White"/>
                                                    </Border.Effect>
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F0F0F0"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="📄 Exporter PDF" 
                                Command="{Binding ExporterPDFCommand}"
                                Background="White" Foreground="#FF6F00" 
                                Padding="15,10" FontWeight="SemiBold" FontSize="13"
                                BorderThickness="2" BorderBrush="White" Cursor="Hand" VerticalAlignment="Center">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="8" 
                                                        Padding="{TemplateBinding Padding}">
                                                    <Border.Effect>
                                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="White"/>
                                                    </Border.Effect>
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F0F0F0"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
                </Border>

                <!-- Cartes de statistiques rapides -->
                <WrapPanel Margin="0,0,0,20">
                    <Border Style="{StaticResource StatCardStyle}" Width="250">
                        <StackPanel>
                            <TextBlock Text="📝 Total Tâches" FontSize="12" Foreground="#666666"/>
                            <TextBlock Text="{Binding TotalTaches}" FontSize="36" FontWeight="Bold" Foreground="#00915A" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}" Width="250">
                        <StackPanel>
                            <TextBlock Text="✅ Terminées" FontSize="12" Foreground="#666666"/>
                            <TextBlock Text="{Binding TachesTerminees}" FontSize="36" FontWeight="Bold" Foreground="#4CAF50" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding PourcentageTerminees}" FontSize="14" Foreground="#666666" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}" Width="250">
                        <StackPanel>
                            <TextBlock Text="⏳ En cours" FontSize="12" Foreground="#666666"/>
                            <TextBlock Text="{Binding TachesEnCours}" FontSize="36" FontWeight="Bold" Foreground="#2196F3" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}" Width="250">
                        <StackPanel>
                            <TextBlock Text="📊 Projets actifs" FontSize="12" Foreground="#666666"/>
                            <TextBlock Text="{Binding ProjetsActifs}" FontSize="36" FontWeight="Bold" Foreground="#FF9800" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>

                <!-- Statistiques CRA -->
                <WrapPanel Margin="0,0,0,20">
                    <Border Style="{StaticResource StatCardStyle}" Width="250">
                        <StackPanel>
                            <TextBlock Text="⏰ Total Heures Saisies" FontSize="12" Foreground="#666666"/>
                            <TextBlock Text="{Binding TotalHeuresCRA}" FontSize="36" FontWeight="Bold" Foreground="#00915A" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding TotalJoursCRA}" FontSize="14" Foreground="#666666" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}" Width="250">
                        <StackPanel>
                            <TextBlock Text="📅 Jours Saisis (ce mois)" FontSize="12" Foreground="#666666"/>
                            <TextBlock Text="{Binding JoursSaisisMoisCourant}" FontSize="36" FontWeight="Bold" Foreground="#2196F3" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding TauxCompletionMois}" FontSize="14" Foreground="#666666" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}" Width="250">
                        <StackPanel>
                            <TextBlock Text="👥 Devs Actifs" FontSize="12" Foreground="#666666"/>
                            <TextBlock Text="{Binding DevsActifs}" FontSize="36" FontWeight="Bold" Foreground="#9C27B0" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}" Width="250">
                        <StackPanel>
                            <TextBlock Text="🎯 Taux de Réalisation" FontSize="12" Foreground="#666666"/>
                            <TextBlock Text="{Binding TauxRealisation}" FontSize="36" FontWeight="Bold" Foreground="#FF6F00" Margin="0,5,0,0"/>
                            <TextBlock Text="Temps réel vs estimé" FontSize="14" Foreground="#666666" Margin="0,5,0,0"/>
                            <TextBlock Text="(>100% = dépassement)" FontSize="11" Foreground="#999999" FontStyle="Italic" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>

                <!-- Graphique: Tâches par statut -->
                <Border Style="{StaticResource StatCardStyle}" Margin="10,0,10,20">
                    <StackPanel>
                        <TextBlock Text="📊 Répartition des tâches par statut" 
                                   FontSize="16" FontWeight="Bold" Foreground="#1A1919" Margin="0,0,0,15"/>
                        <ItemsControl ItemsSource="{Binding TachesParStatut}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Statut}" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                        <Border Grid.Column="1" Height="30" Background="#E0E0E0" CornerRadius="4" Margin="10,0">
                                            <Border Background="{Binding Couleur}" CornerRadius="4" HorizontalAlignment="Left" 
                                                    Width="{Binding LargeurBarre}"/>
                                        </Border>
                                        <TextBlock Text="{Binding Pourcentage}" Grid.Column="2" VerticalAlignment="Center" 
                                                   Foreground="#666666" HorizontalAlignment="Right"/>
                                        <TextBlock Text="{Binding Nombre}" Grid.Column="3" VerticalAlignment="Center" 
                                                   FontWeight="Bold" HorizontalAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Deux colonnes: Charge par développeur + CRA par développeur -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Charge par développeur (tâches) - VERSION GRAPHIQUE -->
                    <Border Grid.Column="0" Style="{StaticResource StatCardStyle}" Margin="10,0,5,0">
                        <StackPanel>
                            <TextBlock Text="👥 Charge par développeur" 
                                       FontSize="16" FontWeight="Bold" Foreground="#1A1919" Margin="0,0,0,5"/>
                            <TextBlock Text="Distribution des tâches par statut" 
                                       FontSize="11" Foreground="#666666" FontStyle="Italic" Margin="0,0,0,15"/>
                            <ItemsControl ItemsSource="{Binding ChargeParDev}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#F9F9F9" CornerRadius="8" Padding="15" Margin="0,0,0,12"
                                                BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"
                                                MouseEnter="DevCard_MouseEnter" MouseLeave="DevCard_MouseLeave"
                                                Tag="{Binding}">
                                            <Border.InputBindings>
                                                <MouseBinding Gesture="LeftDoubleClick" 
                                                              Command="{Binding DataContext.AfficherDetailsDevCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              CommandParameter="{Binding}"/>
                                            </Border.InputBindings>
                                            <StackPanel>
                                                <!-- En-tête avec nom et total -->
                                                <Grid Margin="0,0,0,10">
                                                    <TextBlock Text="{Binding NomDev}" FontWeight="Bold" FontSize="14" 
                                                               Foreground="#1A1919" VerticalAlignment="Center"/>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                        <TextBlock Text="Total : " FontSize="12" Foreground="#666" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding Total}" FontWeight="Bold" FontSize="16" 
                                                                   Foreground="#00915A" VerticalAlignment="Center"/>
                                                        <TextBlock Text=" tâches" FontSize="12" Foreground="#666" VerticalAlignment="Center" Margin="3,0,0,0"/>
                                                    </StackPanel>
                                                </Grid>
                                                
                                                <!-- Barre de progression empilée -->
                                                <Grid Height="28" Margin="0,0,0,8">
                                                    <Border Background="#E0E0E0" CornerRadius="6"/>
                                                    <StackPanel Orientation="Horizontal">
                                                        <!-- À faire (gris) -->
                                                        <Border Background="#757575" CornerRadius="6,0,0,6" 
                                                                Width="{Binding LargeurAfaire}" Height="28" ToolTip="À faire"/>
                                                        <!-- En cours (bleu) -->
                                                        <Border Background="#2196F3" Height="28"
                                                                Width="{Binding LargeurEnCours}" ToolTip="En cours"/>
                                                        <!-- Terminées (vert) -->
                                                        <Border Background="#4CAF50" CornerRadius="0,6,6,0" Height="28"
                                                                Width="{Binding LargeurTerminees}" ToolTip="Terminées"/>
                                                    </StackPanel>
                                                </Grid>
                                                
                                                <!-- Légende -->
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                        <Border Width="12" Height="12" Background="#757575" CornerRadius="2" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding AFaire}" FontWeight="SemiBold" FontSize="13" Margin="5,0,2,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="À faire" FontSize="11" Foreground="#666" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                                        <Border Width="12" Height="12" Background="#2196F3" CornerRadius="2" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding EnCours}" FontWeight="SemiBold" FontSize="13" Margin="5,0,2,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="En cours" FontSize="11" Foreground="#666" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                                        <Border Width="12" Height="12" Background="#4CAF50" CornerRadius="2" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding Terminees}" FontWeight="SemiBold" FontSize="13" Margin="5,0,2,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="Terminées" FontSize="11" Foreground="#666" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Grid>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="💡 Double-cliquez sur une carte pour voir les détails des tâches" 
                                       FontSize="10" Foreground="#999" FontStyle="Italic" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- CRA par développeur (heures) - VERSION GRAPHIQUE -->
                    <Border Grid.Column="1" Style="{StaticResource StatCardStyle}" Margin="5,0,10,0">
                        <StackPanel>
                            <TextBlock Text="⏰ Temps CRA par développeur" 
                                       FontSize="16" FontWeight="Bold" Foreground="#1A1919" Margin="0,0,0,5"/>
                            <TextBlock Text="Heures saisies et validées" 
                                       FontSize="11" Foreground="#666666" FontStyle="Italic" Margin="0,0,0,15"/>
                            <ItemsControl ItemsSource="{Binding CRAParDev}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#F9F9F9" CornerRadius="8" Padding="15" Margin="0,0,0,12"
                                                BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand"
                                                MouseEnter="DevCard_MouseEnter" MouseLeave="DevCard_MouseLeave"
                                                Tag="{Binding}">
                                            <Border.InputBindings>
                                                <MouseBinding Gesture="LeftDoubleClick" 
                                                              Command="{Binding DataContext.AfficherDetailsCRADevCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              CommandParameter="{Binding}"/>
                                            </Border.InputBindings>
                                            <StackPanel>
                                                <!-- En-tête avec nom -->
                                                <Grid Margin="0,0,0,10">
                                                    <TextBlock Text="{Binding NomDev}" FontWeight="Bold" FontSize="14" 
                                                               Foreground="#1A1919" VerticalAlignment="Center"/>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                        <TextBlock Text="{Binding HeuresTotal}" FontWeight="Bold" FontSize="16" 
                                                                   Foreground="#00915A" VerticalAlignment="Center"/>
                                                        <TextBlock Text="h" FontSize="12" Foreground="#666" VerticalAlignment="Center" Margin="2,0,0,0"/>
                                                    </StackPanel>
                                                </Grid>
                                                
                                                <!-- Barre de progression (total) -->
                                                <Grid Height="28" Margin="0,0,0,8">
                                                    <Border Background="#E0E0E0" CornerRadius="6"/>
                                                    <Border Background="#00915A" CornerRadius="6" HorizontalAlignment="Left"
                                                            Width="{Binding LargeurBarreTotal}" Height="28"/>
                                                </Grid>
                                                
                                                <!-- Détails -->
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock FontSize="11" Foreground="#666">
                                                            <Run Text="📅 Ce mois :"/><Run Text=" "/><Run Text="{Binding HeuresMois}" FontWeight="SemiBold"/><Run Text="h"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                                        <TextBlock FontSize="11" Foreground="#666">
                                                            <Run Text="📊 Total :"/><Run Text=" "/><Run Text="{Binding JoursTotal}" FontWeight="SemiBold"/><Run Text=" jours"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="💡 Double-cliquez sur une carte pour voir l'historique CRA" 
                                       FontSize="10" Foreground="#999" FontStyle="Italic" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Complétion par projet - VERSION GRAPHIQUE -->
                <Border Style="{StaticResource StatCardStyle}" Margin="10,0,10,20">
                    <StackPanel>
                        <TextBlock Text="📁 Avancement des projets" 
                                   FontSize="16" FontWeight="Bold" Foreground="#1A1919" Margin="0,0,0,5"/>
                        <TextBlock FontSize="11" Foreground="#666666" Margin="0,0,0,15">
                            <Run Text="Tâches terminées (incluant archivées) et temps CRA saisi par projet" FontStyle="Italic"/>
                            <LineBreak/>
                            <Run Text="💡 Les tâches archivées sont comptées comme terminées" FontSize="10" Foreground="#00915A" FontWeight="SemiBold"/>
                        </TextBlock>
                        <ItemsControl ItemsSource="{Binding CompletionParProjet}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F9F9F9" CornerRadius="8" Padding="15" Margin="0,0,0,12"
                                            BorderBrush="#E0E0E0" BorderThickness="1">
                                        <StackPanel>
                                            <!-- En-tête projet -->
                                            <Grid Margin="0,0,0,10">
                                                <StackPanel Orientation="Horizontal">
                                                    <Border Width="8" Height="24" CornerRadius="2" VerticalAlignment="Center"
                                                            Background="{Binding CouleurProjet}" Margin="0,0,10,0"/>
                                                    <TextBlock Text="{Binding NomProjet}" FontWeight="Bold" FontSize="14" 
                                                               Foreground="#1A1919" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                    <TextBlock Text="{Binding TauxCompletion}" FontWeight="Bold" FontSize="18" 
                                                               Foreground="#00915A" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Grid>
                                            
                                            <!-- Barre de progression -->
                                            <Grid Height="32" Margin="0,0,0,10">
                                                <Border Background="#E0E0E0" CornerRadius="6"/>
                                                <Border Background="#4CAF50" CornerRadius="6" HorizontalAlignment="Left"
                                                        Width="{Binding LargeurBarreCompletion}" Height="32"/>
                                            </Grid>
                                            
                                            <!-- Détails -->
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock FontSize="11" Foreground="#666">
                                                        <Run Text="📋 Total :"/><Run Text=" "/><Run Text="{Binding TotalTaches}" FontWeight="SemiBold"/><Run Text=" tâches"/>
                                                    </TextBlock>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                                    <TextBlock FontSize="11" Foreground="#666">
                                                        <Run Text="✅ Terminées :"/><Run Text=" "/><Run Text="{Binding TachesTerminees}" FontWeight="SemiBold"/>
                                                    </TextBlock>
                                                    <TextBlock FontSize="9" Foreground="#00915A" FontStyle="Italic" Margin="0,2,0,0">
                                                        <Run Text="(+ archivées)"/>
                                                    </TextBlock>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                                    <TextBlock FontSize="11" Foreground="#666">
                                                        <Run Text="⏰ CRA :"/><Run Text=" "/><Run Text="{Binding HeuresCRA}" FontWeight="SemiBold"/><Run Text="h"/>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- NOUVELLES STATISTIQUES ÉQUIPES -->
                
                <!-- Répartition des ressources par équipe -->
                <Border Style="{StaticResource StatCardStyle}" Margin="10,0,10,20">
                    <StackPanel>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="🏢 Répartition des ressources par équipe" 
                                           FontSize="16" FontWeight="Bold" Foreground="#1A1919" Margin="0,0,0,5"/>
                                <TextBlock Text="Nombre de membres actifs (hors managers) par équipe" 
                                           FontSize="11" Foreground="#666666" FontStyle="Italic"/>
                            </StackPanel>
                            <Button Grid.Column="1" Click="BtnVoirToutesRessources_Click"
                                    Cursor="Hand" Padding="12,8" BorderThickness="0">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#00915A" Offset="0"/>
                                        <GradientStop Color="#00C078" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </Button.Resources>
                                <Button.Effect>
                                    <DropShadowEffect Color="#00915A" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                                </Button.Effect>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="👁️ " FontSize="14" Foreground="White"/>
                                    <TextBlock Text="Voir détail complet" FontSize="13" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                        <ItemsControl ItemsSource="{Binding RessourcesParEquipe}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="3" HorizontalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" CornerRadius="12" Padding="18" Margin="8"
                                            Tag="{Binding EquipeId}" MouseLeftButtonDown="BtnVoirEquipeDetail_Click"
                                            Cursor="Hand">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#D0D0D0" BlurRadius="12" ShadowDepth="2" Opacity="0.15"/>
                                        </Border.Effect>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="BorderBrush">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#00915A" Offset="0"/>
                                                                    <GradientStop Color="#00C078" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="#00915A" BlurRadius="16" ShadowDepth="3" Opacity="0.25"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <!-- En-tête équipe avec icône gradient -->
                                            <Grid Margin="0,0,0,12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="10" Margin="0,0,10,0">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#E7F5F1" Offset="0"/>
                                                            <GradientStop Color="#B2DFDB" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                    <TextBlock Text="🏢" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding NomEquipe}" FontWeight="Bold" FontSize="13" 
                                                               Foreground="#1A1919" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                                    <TextBlock FontSize="10" Margin="0,3,0,0">
                                                        <Run Text="👤 Manager:" Foreground="#666"/>
                                                        <Run Text=" " />
                                                        <Run Text="{Binding NomManager, Mode=OneWay}" Foreground="#00915A" FontWeight="SemiBold"/>
                                                    </TextBlock>
                                                    <TextBlock Text="{Binding CodeEquipe}" FontSize="9" 
                                                               Foreground="#999" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                            
                                            <!-- Séparateur subtil -->
                                            <Border Height="1" Background="#E0E0E0" Margin="0,0,0,12"/>
                                            
                                            <!-- Nombre de membres - mise en valeur -->
                                            <Grid Margin="0,0,0,12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Background="#F5F5F5" CornerRadius="8" Padding="12,8">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding NbMembres}" FontWeight="Bold" FontSize="32" 
                                                                   Foreground="#00915A" VerticalAlignment="Center"/>
                                                        <StackPanel Margin="8,0,0,0" VerticalAlignment="Center">
                                                            <TextBlock Text="membres" FontSize="11" Foreground="#666" FontWeight="SemiBold"/>
                                                            <TextBlock Text="actifs" FontSize="9" Foreground="#999"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Border>
                                            </Grid>
                                            
                                            <!-- Barre comparative stylée -->
                                            <StackPanel Margin="0,0,0,10">
                                                <TextBlock Text="Taille relative" FontSize="9" Foreground="#999" Margin="0,0,0,4"/>
                                                <Grid Height="8">
                                                    <Border Background="#F0F0F0" CornerRadius="4"/>
                                                    <Border CornerRadius="4" HorizontalAlignment="Left"
                                                            Width="{Binding LargeurBarre, Mode=OneWay}" Height="8">
                                                        <Border.Background>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                <GradientStop Color="#00915A" Offset="0"/>
                                                                <GradientStop Color="#00C078" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Border.Background>
                                                    </Border>
                                                </Grid>
                                            </StackPanel>
                                            
                                            <!-- Stats en grille -->
                                            <Grid Margin="0,8,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <Border Grid.Column="0" Background="#F8F9FA" CornerRadius="6" Padding="8,6" Margin="0,0,4,0">
                                                    <StackPanel>
                                                        <TextBlock Text="📊 Projets" FontSize="9" Foreground="#666" Margin="0,0,0,2"/>
                                                        <TextBlock Text="{Binding NbProjets}" FontWeight="Bold" FontSize="16" 
                                                                   Foreground="#1A1919" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                                
                                                <Border Grid.Column="1" Background="#F8F9FA" CornerRadius="6" Padding="8,6" Margin="4,0,0,0">
                                                    <StackPanel>
                                                        <TextBlock Text="⚖️ Charge" FontSize="9" Foreground="#666" Margin="0,0,0,2"/>
                                                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="#1A1919" HorizontalAlignment="Center">
                                                            <Run Text="{Binding ChargeParMembre, Mode=OneWay, StringFormat='{}{0:F1}'}"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Border>
                                            </Grid>
                                            
                                            <!-- Call to action -->
                                            <Border Margin="0,12,0,0" Padding="8,6" CornerRadius="8">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="#E3F2FD" Offset="0"/>
                                                        <GradientStop Color="#BBDEFB" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBlock Text="👁️" FontSize="12" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                    <TextBlock Text="Voir les membres" FontSize="11" 
                                                               Foreground="#0D47A1" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Distribution de charge entre équipes -->
                <Border Style="{StaticResource StatCardStyle}" Margin="10,0,10,20">
                    <StackPanel>
                        <TextBlock Text="⚖️ Équilibre de charge entre équipes" 
                                   FontSize="16" FontWeight="Bold" Foreground="#1A1919" Margin="0,0,0,5"/>
                        <TextBlock Text="Nombre de projets assignés par équipe (une équipe peut gérer plusieurs projets)" 
                                   FontSize="11" Foreground="#666666" FontStyle="Italic" Margin="0,0,0,15"/>
                        <ItemsControl ItemsSource="{Binding ChargeParEquipe}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2" HorizontalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F9F9F9" CornerRadius="8" Padding="15" Margin="5"
                                            BorderBrush="#E0E0E0" BorderThickness="1">
                                        <StackPanel>
                                            <!-- En-tête avec code équipe uniquement -->
                                            <Border Background="#E7F5F1" CornerRadius="16" Padding="8,4" Margin="0,0,0,10"
                                                    HorizontalAlignment="Left">
                                                <TextBlock FontWeight="Bold" FontSize="11" Foreground="#00915A">
                                                    <Run Text="{Binding CodeEquipe}"/>
                                                </TextBlock>
                                            </Border>
                                            
                                            <!-- Nombre de projets - grand et centré -->
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,10">
                                                <TextBlock Text="{Binding NbProjets}" FontWeight="Bold" FontSize="32" 
                                                           Foreground="{Binding CouleurBarre}" VerticalAlignment="Center"/>
                                                <TextBlock Text=" projets" FontSize="13" Foreground="#666" 
                                                           VerticalAlignment="Center" Margin="5,0,0,0"/>
                                            </StackPanel>
                                            
                                            <!-- Barre de progression horizontale -->
                                            <Grid Height="24" Margin="0,5">
                                                <Border Background="#E0E0E0" CornerRadius="6"/>
                                                <Border Background="{Binding CouleurBarre}" CornerRadius="6" HorizontalAlignment="Left"
                                                        Width="{Binding LargeurBarre}" Height="24">
                                                    <Border.Effect>
                                                        <DropShadowEffect Color="{Binding CouleurBarre}" BlurRadius="8" 
                                                                         ShadowDepth="0" Opacity="0.3"/>
                                                    </Border.Effect>
                                                </Border>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Page détail des ressources -->
        <Grid x:Name="GridPageRessources" 
              Visibility="{Binding AfficherPageRessources, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header avec bouton retour -->
            <Border Grid.Row="0" Padding="20,15" Margin="0,0,0,10">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#00915A" Offset="0"/>
                        <GradientStop Color="#00C078" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Click="BtnRetourStatistiques_Click" 
                            Padding="12,8" Margin="0,0,15,0" Cursor="Hand"
                            BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <Border.Effect>
                                                    <DropShadowEffect Color="#00915A" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F0F0F0"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="← " FontSize="16" Foreground="#00915A" FontWeight="Bold"/>
                            <TextBlock Text="Retour" FontSize="13" Foreground="#00915A" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtTitrePageRessources" Text="🏢 Détail des ressources par équipe" 
                                   FontSize="22" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="TxtSousTitrePageRessources" Text="Vue détaillée de la charge et des projets par ressource" 
                                   FontSize="13" Foreground="White" Opacity="0.9" Margin="0,3,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Contenu de la page ressources (include du contenu de RessourcesEquipeDetailWindow) -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20,0,20,20">
                <StackPanel>
                    <!-- Statistiques résumées -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="20" Margin="5">
                            <Border.Effect>
                                <DropShadowEffect Color="#D0D0D0" BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="👥" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtNbTotalRessources" Text="0" FontSize="32" FontWeight="Bold" 
                                           Foreground="#00915A" HorizontalAlignment="Center"/>
                                <TextBlock Text="Ressources" FontSize="12" Foreground="#666" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="20" Margin="5">
                            <Border.Effect>
                                <DropShadowEffect Color="#D0D0D0" BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="🏢" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtNbEquipesActives" Text="0" FontSize="32" FontWeight="Bold" 
                                           Foreground="#2196F3" HorizontalAlignment="Center"/>
                                <TextBlock Text="Équipes actives" FontSize="12" Foreground="#666" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="20" Margin="5">
                            <Border.Effect>
                                <DropShadowEffect Color="#D0D0D0" BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="📊" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtNbProjetsActifs" Text="0" FontSize="32" FontWeight="Bold" 
                                           Foreground="#FF9800" HorizontalAlignment="Center"/>
                                <TextBlock Text="Projets actifs" FontSize="12" Foreground="#666" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <Border Grid.Column="3" Background="White" CornerRadius="10" Padding="20" Margin="5">
                            <Border.Effect>
                                <DropShadowEffect Color="#D0D0D0" BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="⚖️" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtChargeMoyenne" Text="0.0" FontSize="32" FontWeight="Bold" 
                                           Foreground="#9C27B0" HorizontalAlignment="Center"/>
                                <TextBlock Text="Charge moyenne" FontSize="12" Foreground="#666" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                    
                    <!-- Filtres par équipe -->
                    <Border Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect Color="#D0D0D0" BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="🔍 Filtrer par équipe" FontSize="14" FontWeight="Bold" Foreground="#1A1919" Margin="0,0,0,10"/>
                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                <WrapPanel x:Name="WrapPanelFiltresEquipes" Orientation="Horizontal"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                    
                    <!-- Liste des ressources -->
                    <Border Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect Color="#D0D0D0" BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="📋 Ressources et charge de travail" FontSize="16" FontWeight="Bold" 
                                       Foreground="#1A1919" Margin="0,0,0,15"/>
                            <ItemsControl x:Name="ItemsControlRessources">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="3" HorizontalAlignment="Stretch"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#FAFAFA" CornerRadius="8" Padding="15" Margin="5"
                                                BorderBrush="#E0E0E0" BorderThickness="1">
                                            <StackPanel>
                                                <!-- Avatar et nom -->
                                                <Grid Margin="0,0,0,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border Grid.Column="0" Background="#E7F5F1" Width="40" Height="40" 
                                                            CornerRadius="20" Margin="0,0,10,0">
                                                        <TextBlock Text="👤" FontSize="20" HorizontalAlignment="Center" 
                                                                   VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Nom}" FontWeight="Bold" FontSize="13" 
                                                                   Foreground="#1A1919" TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock FontSize="11" Foreground="#666">
                                                            <Run Text="{Binding NomEquipe}"/>
                                                            <Run Text=" • "/>
                                                            <Run Text="{Binding CodeEquipe}"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                                
                                                <!-- Badge rôle -->
                                                <Border Background="#E3F2FD" CornerRadius="6" Padding="8,4" Margin="0,0,0,10"
                                                        HorizontalAlignment="Left">
                                                    <TextBlock Text="{Binding Role}" FontSize="10" Foreground="#1976D2" 
                                                               FontWeight="SemiBold"/>
                                                </Border>
                                                
                                                <!-- Statistiques -->
                                                <Grid Margin="0,0,0,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding NbProjets}" FontSize="18" FontWeight="Bold" 
                                                                   Foreground="#00915A" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="projets" FontSize="10" Foreground="#666" 
                                                                   HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1">
                                                        <TextBlock Text="{Binding NbTachesActives}" FontSize="18" FontWeight="Bold" 
                                                                   Foreground="#FF9800" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="tâches actives" FontSize="10" Foreground="#666" 
                                                                   HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Grid>
                                                
                                                <!-- Jauge de charge -->
                                                <StackPanel Margin="0,0,0,10">
                                                    <Grid Margin="0,0,0,5">
                                                        <TextBlock Text="Charge de travail" FontSize="10" Foreground="#666" 
                                                                   HorizontalAlignment="Left"/>
                                                        <TextBlock Text="{Binding NiveauCharge}" FontSize="10" 
                                                                   Foreground="{Binding CouleurChargeBrush}" 
                                                                   FontWeight="Bold" HorizontalAlignment="Right"/>
                                                    </Grid>
                                                    <Grid Height="12">
                                                        <Border Background="#E0E0E0" CornerRadius="6"/>
                                                        <Border Background="{Binding CouleurChargeBrush}" CornerRadius="6" 
                                                                HorizontalAlignment="Left" Height="12"
                                                                Width="{Binding LargeurBarreCharge}"/>
                                                    </Grid>
                                                </StackPanel>
                                                
                                                <!-- Liste des projets -->
                                                <Border Background="White" CornerRadius="6" Padding="10" 
                                                        BorderBrush="#E0E0E0" BorderThickness="1">
                                                    <StackPanel>
                                                        <TextBlock Text="📁 Projets assignés" FontSize="10" FontWeight="Bold" 
                                                                   Foreground="#1A1919" Margin="0,0,0,5"/>
                                                        <ItemsControl ItemsSource="{Binding ListeProjets}" MaxHeight="120">
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <Border Background="#F5F5F5" CornerRadius="4" Padding="6,3" Margin="0,2">
                                                                        <Grid>
                                                                            <TextBlock Text="{Binding Nom}" FontSize="10" 
                                                                                       Foreground="#333" TextTrimming="CharacterEllipsis"/>
                                                                            <Border Background="#00915A" CornerRadius="8" 
                                                                                    Padding="4,2" HorizontalAlignment="Right">
                                                                                <TextBlock Text="{Binding NbTaches}" FontSize="9" 
                                                                                           Foreground="White" FontWeight="Bold"/>
                                                                            </Border>
                                                                        </Grid>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                        <TextBlock Text="Aucun projet assigné" FontSize="10" 
                                                                   Foreground="#999" FontStyle="Italic" 
                                                                   Visibility="{Binding AucunProjet, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                   HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    
                    <!-- Légende -->
                    <Border Background="#FFF9E5" CornerRadius="8" Padding="15" BorderBrush="#FFE082" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="ℹ️ Légende des niveaux de charge" FontSize="12" FontWeight="Bold" 
                                       Foreground="#1A1919" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal" Margin="0,3">
                                <Border Background="#4CAF50" Width="16" Height="16" CornerRadius="3" Margin="0,0,8,0"/>
                                <TextBlock Text="Faible (0-3 projets)" FontSize="11" Foreground="#666" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3">
                                <Border Background="#FF9800" Width="16" Height="16" CornerRadius="3" Margin="0,0,8,0"/>
                                <TextBlock Text="Moyenne (4-6 projets)" FontSize="11" Foreground="#666" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3">
                                <Border Background="#F44336" Width="16" Height="16" CornerRadius="3" Margin="0,0,8,0"/>
                                <TextBlock Text="Élevée (7+ projets)" FontSize="11" Foreground="#666" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
