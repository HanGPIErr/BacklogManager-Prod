<UserControl x:Class="BacklogManager.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:shared="clr-namespace:BacklogManager.Shared"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F0F2F5">
    <UserControl.Resources>
        <shared:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <!-- Style pour les cartes principales avec effet glassmorphism -->
        <Style x:Key="MainCard" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="32,28"/>
            <Setter Property="Margin" Value="0,0,0,24"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E8E8E8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#D0D0D0" BlurRadius="20" ShadowDepth="0" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Style pour les KPI cards avec animation -->
        <Style x:Key="KPICard" TargetType="Border">
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="22,20"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E8E8E8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#D0D0D0" BlurRadius="16" ShadowDepth="0" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#00915A" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Style pour les boutons "Voir tout" modernisé -->
        <Style x:Key="ViewAllButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#00915A" Offset="0"/>
                        <GradientStop Color="#00A566" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#00915A" BlurRadius="12" ShadowDepth="0" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00915A" BlurRadius="16" ShadowDepth="0" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="35,25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header avec Bonjour + Date + BNP Branding - Moderne -->
            <Border Grid.Row="0" Style="{StaticResource MainCard}">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#00915A" Offset="0"/>
                        <GradientStop Color="#00A566" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock FontSize="32" FontWeight="Bold" Foreground="White" TextWrapping="Wrap">
                            <Run Text="👋 Bonjour, "/>
                            <Run Text="{Binding NomUtilisateur}"/>
                        </TextBlock>
                        <TextBlock FontSize="15" Foreground="#E0F2E9" Margin="0,8,0,0" Opacity="0.95">
                            <Run Text="📅 "/>
                            <Run Text="{Binding DateAujourdhui}"/>
                        </TextBlock>
                    </StackPanel>
                    
                    <!-- Boutons d'action -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                        <Button x:Name="BtnVoirGuide" Content="📖 Guide" 
                                Height="42" Padding="20,0" Margin="0,0,12,0"
                                Background="White" Foreground="#00915A" FontWeight="Bold"
                                BorderThickness="0" Cursor="Hand" FontSize="14"
                                Click="BtnVoirGuide_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                       CornerRadius="10" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F0F9F4"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        
                        <Border Background="White" CornerRadius="10" Padding="18,12" Opacity="0.95">
                            <StackPanel>
                                <TextBlock Text="{Binding MetriqueLabel}" FontSize="11" 
                                           Foreground="#00915A" FontWeight="SemiBold"/>
                                <TextBlock FontSize="24" FontWeight="Bold" Foreground="#00915A" 
                                           Margin="0,4,0,0" HorizontalAlignment="Center">
                                    <Run Text="{Binding MetriqueValeur, Mode=OneWay}"/>
                                    <Run Text="{Binding MetriqueSuffixe, Mode=OneWay}" FontSize="16"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- STATISTIQUES EN CARTES (KPI) - Responsive avec WrapPanel -->
            <ItemsControl Grid.Row="1" Margin="0,0,0,20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.Items>
                
                <!-- Tâches terminées -->
                <Border Style="{StaticResource KPICard}" Margin="0,0,12,12" Padding="22,20" 
                        MinWidth="220" Width="255">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#C8E6C9" Offset="0"/>
                            <GradientStop Color="#A5D6A7" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#FFFFFF" CornerRadius="10" Padding="10,6" Margin="0,0,10,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#4CAF50" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                </Border.Effect>
                                <TextBlock Text="✅" FontSize="18"/>
                            </Border>
                            <TextBlock Text="Terminées" FontSize="14" 
                                       Foreground="#2E7D32" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" FontSize="38" FontWeight="Bold" 
                                       Foreground="#00915A" VerticalAlignment="Center">
                                <Run Text="{Binding NbTachesTerminees, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock Grid.Column="1" FontSize="16" Foreground="#999999" 
                                       VerticalAlignment="Bottom" Margin="8,0,0,8">
                                <Run Text="/ "/>
                                <Run Text="{Binding NbTachesTotal, Mode=OneWay}"/>
                            </TextBlock>
                        </Grid>
                    </Grid>
                </Border>
                
                <!-- Tâches en cours -->
                <Border Style="{StaticResource KPICard}" Margin="0,0,12,12" Padding="22,20" 
                        MinWidth="220" Width="255">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#BBDEFB" Offset="0"/>
                            <GradientStop Color="#90CAF9" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#FFFFFF" CornerRadius="10" Padding="10,6" Margin="0,0,10,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#2196F3" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                </Border.Effect>
                                <TextBlock Text="🔄" FontSize="18"/>
                            </Border>
                            <TextBlock Text="En cours" FontSize="14" 
                                       Foreground="#1565C0" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Text="{Binding NbTachesEnCours, Mode=OneWay}" 
                                   FontSize="38" FontWeight="Bold" Foreground="#2196F3"/>
                    </Grid>
                </Border>
                
                <!-- Tâches urgentes -->
                <Border Style="{StaticResource KPICard}" Margin="0,0,12,12" Padding="22,20" 
                        MinWidth="220" Width="255">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FFCCBC" Offset="0"/>
                            <GradientStop Color="#FFAB91" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#FFFFFF" CornerRadius="10" Padding="10,6" Margin="0,0,10,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#FF5722" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                </Border.Effect>
                                <TextBlock Text="⚡" FontSize="18"/>
                            </Border>
                            <TextBlock Text="Urgentes" FontSize="14" 
                                       Foreground="#D84315" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Text="{Binding NbTachesUrgentes, Mode=OneWay}" 
                                   FontSize="38" FontWeight="Bold" Foreground="#FF5722"/>
                    </Grid>
                </Border>
                
                <!-- Projets actifs -->
                <Border Style="{StaticResource KPICard}" Margin="0,0,12,12" Padding="22,20" 
                        MinWidth="220" Width="255">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FFE0B2" Offset="0"/>
                            <GradientStop Color="#FFCC80" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#FFFFFF" CornerRadius="10" Padding="10,6" Margin="0,0,10,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#FF9800" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                </Border.Effect>
                                <TextBlock Text="📁" FontSize="18"/>
                            </Border>
                            <TextBlock Text="Projets" FontSize="14" 
                                       Foreground="#E65100" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Text="{Binding NbProjetsActifs, Mode=OneWay}" 
                                   FontSize="38" FontWeight="Bold" Foreground="#FF9800"/>
                    </Grid>
                </Border>
                
                <!-- Équipes actives -->
                <Border Style="{StaticResource KPICard}" Margin="0,0,12,12" Padding="22,20" 
                        MinWidth="220" Width="255">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#B3E5FC" Offset="0"/>
                            <GradientStop Color="#81D4FA" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#FFFFFF" CornerRadius="10" Padding="10,6" Margin="0,0,10,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#0288D1" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                </Border.Effect>
                                <TextBlock Text="🏢" FontSize="18"/>
                            </Border>
                            <TextBlock Text="Équipes" FontSize="14" 
                                       Foreground="#01579B" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Text="{Binding NbEquipesActives, Mode=OneWay}" 
                                   FontSize="38" FontWeight="Bold" Foreground="#0288D1"/>
                    </Grid>
                </Border>
                
                <!-- Total ressources -->
                <Border Style="{StaticResource KPICard}" Margin="0,0,12,12" Padding="22,20" 
                        MinWidth="220" Width="255">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#E1BEE7" Offset="0"/>
                            <GradientStop Color="#CE93D8" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#FFFFFF" CornerRadius="10" Padding="10,6" Margin="0,0,10,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#7B1FA2" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                </Border.Effect>
                                <TextBlock Text="👥" FontSize="18"/>
                            </Border>
                            <TextBlock Text="Ressources" FontSize="14" 
                                       Foreground="#4A148C" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Text="{Binding NbRessourcesTotal, Mode=OneWay}" 
                                   FontSize="38" FontWeight="Bold" Foreground="#7B1FA2"/>
                    </Grid>
                </Border>
                
                <!-- Charge moyenne par équipe -->
                <Border Style="{StaticResource KPICard}" Margin="0,0,12,12" Padding="22,20" 
                        MinWidth="220" Width="255">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FFF59D" Offset="0"/>
                            <GradientStop Color="#FFF176" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="#FFFFFF" CornerRadius="10" Padding="10,6" Margin="0,0,10,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#F9A825" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                </Border.Effect>
                                <TextBlock Text="⚖️" FontSize="18"/>
                            </Border>
                            <TextBlock Text="Charge / Équipe" FontSize="14" 
                                       Foreground="#F57F17" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" FontSize="38" FontWeight="Bold" 
                                       Foreground="#F57F17" VerticalAlignment="Center">
                                <Run Text="{Binding ChargeMoyenneParEquipe, Mode=OneWay, StringFormat='{}{0:F1}'}"/>
                            </TextBlock>
                            <TextBlock Grid.Column="1" FontSize="14" Foreground="#999999" 
                                       VerticalAlignment="Bottom" Margin="6,0,0,10"
                                       Text="proj/équipe"/>
                        </Grid>
                    </Grid>
                </Border>
                
                </ItemsControl.Items>
            </ItemsControl>
            
            <!-- SECTION ÉQUIPE -->
            <Border Grid.Row="2" Style="{StaticResource MainCard}">
                <StackPanel>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="#F0F9F4" CornerRadius="10" 
                                Padding="12,8" Margin="0,0,12,0">
                            <TextBlock Text="🏢" FontSize="20"/>
                        </Border>
                        <TextBlock Grid.Column="1" FontSize="22" FontWeight="Bold" 
                                   Foreground="#1A1919" VerticalAlignment="Center">
                            <Run x:Name="TxtTitreEquipe"/>
                        </TextBlock>
                    </Grid>
                    
                    <!-- Mon équipe (utilisateur standard) -->
                    <StackPanel x:Name="PanelMonEquipe" Visibility="Collapsed">
                        <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" 
                                Padding="20" Background="#FAFAFA" Cursor="Hand"
                                MouseDown="BtnVoirMonEquipe_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F0F9F4"/>
                                            <Setter Property="BorderBrush" Value="#00915A"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock FontSize="16" FontWeight="Bold" Foreground="#1A1919">
                                        <Run x:Name="TxtNomMonEquipe"/>
                                    </TextBlock>
                                    <TextBlock FontSize="13" Foreground="#666" Margin="0,8,0,0">
                                        <Run x:Name="TxtNbMembresMonEquipe"/>
                                        <Run Text=" membre(s)"/>
                                    </TextBlock>
                                    <TextBlock FontSize="13" Foreground="#666" Margin="0,4,0,0">
                                        <Run x:Name="TxtNbProjetsMonEquipe"/>
                                        <Run Text=" projet(s) actif(s)"/>
                                    </TextBlock>
                                </StackPanel>
                                
                                <Button Grid.Column="1" Content="Voir détails →" 
                                        Height="38" Padding="18,0" Background="#00915A" 
                                        Foreground="White" BorderThickness="0" FontSize="13" 
                                        FontWeight="SemiBold" Cursor="Hand"
                                        Click="BtnVoirMonEquipe_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                            VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#007A4D"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Border>
                    </StackPanel>
                    
                    <!-- Toutes les équipes (admin) -->
                    <ScrollViewer x:Name="ScrollEquipes" Visibility="Collapsed" MaxHeight="400" 
                                  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl x:Name="ListeToutesEquipes">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" 
                                            Padding="18,15" Margin="0,0,12,12" Background="#FAFAFA" 
                                            Cursor="Hand" Tag="{Binding Id}"
                                            MouseDown="BtnVoirEquipe_Click">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F0F9F4"/>
                                                        <Setter Property="BorderBrush" Value="#00915A"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            
                                            <TextBlock Grid.Row="0" Text="{Binding Nom}" FontSize="15" 
                                                       FontWeight="Bold" Foreground="#1A1919" TextWrapping="Wrap"/>
                                            
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                                <Border CornerRadius="8" Padding="10,5" Margin="0,0,8,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#C8E6C9" Offset="0"/>
                                            <GradientStop Color="#A5D6A7" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <TextBlock FontSize="11" FontWeight="Bold" Foreground="#1B5E20">
                                        <Run Text="{Binding NbMembres}"/>
                                        <Run Text=" 👥"/>
                                    </TextBlock>
                                </Border>
                                <Border CornerRadius="8" Padding="10,5">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#BBDEFB" Offset="0"/>
                                            <GradientStop Color="#90CAF9" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <TextBlock FontSize="11" FontWeight="Bold" Foreground="#0D47A1">
                                        <Run Text="{Binding NbProjets}"/>
                                        <Run Text=" 📁"/>
                                    </TextBlock>
                                </Border>
                            </StackPanel>                                            <TextBlock Grid.Row="2" Text="Cliquer pour voir les détails →" 
                                                       FontSize="11" Foreground="#00915A" Margin="0,8,0,0" 
                                                       FontStyle="Italic"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    
                    <!-- Graphique de répartition des ressources par équipe (barres verticales) -->
                    <Border x:Name="GraphiqueEquipes" Margin="0,25,0,0" Background="White" 
                            CornerRadius="12" Padding="25" BorderBrush="#E0E0E0" BorderThickness="1"
                            Visibility="Collapsed">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#D0D0D0"/>
                        </Border.Effect>
                        <StackPanel>
                            <!-- Titre du graphique -->
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="#E8F5E9" CornerRadius="8" 
                                        Padding="10,6" Margin="0,0,12,0">
                                    <TextBlock Text="📊" FontSize="18"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="RÉPARTITION DES RESSOURCES PAR ÉQUIPE" 
                                               FontSize="16" FontWeight="Bold" Foreground="#1A1919"/>
                                    <TextBlock Text="Nombre de membres actifs (hors managers)" 
                                               FontSize="11" Foreground="#666" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- Zone de graphique avec barres verticales -->
                            <Grid Height="300">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Barres verticales -->
                                <ItemsControl x:Name="GraphiqueBarresEquipes" Grid.Row="0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Rows="1"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="8,0" VerticalAlignment="Stretch">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                
                                                <!-- Conteneur de barre avec alignement bas -->
                                                <Grid Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Center">
                                                    <!-- Barre verticale -->
                                                    <Border Width="50" CornerRadius="8,8,0,0" VerticalAlignment="Bottom">
                                                        <Border.Height>
                                                            <MultiBinding Converter="{StaticResource BarWidthConverter}">
                                                                <Binding Path="NbMembres"/>
                                                                <Binding Path="MaxMembres"/>
                                                            </MultiBinding>
                                                        </Border.Height>
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background">
                                                                    <Setter.Value>
                                                                        <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                                                                            <GradientStop Color="#00915A" Offset="0"/>
                                                                            <GradientStop Color="#00C078" Offset="1"/>
                                                                        </LinearGradientBrush>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding NbMembres}" Value="0">
                                                                        <Setter Property="Background" Value="#E0E0E0"/>
                                                                        <Setter Property="Opacity" Value="0.5"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding EstTopEquipe}" Value="True">
                                                                        <Setter Property="Background">
                                                                            <Setter.Value>
                                                                                <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                                                                                    <GradientStop Color="#FF9800" Offset="0"/>
                                                                                    <GradientStop Color="#FFB74D" Offset="1"/>
                                                                                </LinearGradientBrush>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                        <Setter Property="Opacity" Value="1"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <Border.Effect>
                                                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" 
                                                                             Opacity="0.2" Color="#00915A"/>
                                                        </Border.Effect>
                                                        
                                                        <!-- Nombre au-dessus de la barre -->
                                                        <TextBlock Text="{Binding NbMembres}" 
                                                                   HorizontalAlignment="Center" 
                                                                   VerticalAlignment="Top"
                                                                   Margin="0,-25,0,0"
                                                                   FontSize="16" FontWeight="Bold">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="#00915A"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding NbMembres}" Value="0">
                                                                            <Setter Property="Foreground" Value="#999"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding EstTopEquipe}" Value="True">
                                                                            <Setter Property="Foreground" Value="#FF9800"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Border>
                                                </Grid>
                                                
                                                <!-- Nom de l'équipe en bas -->
                                                <TextBlock Grid.Row="1" 
                                                           Text="{Binding Nom}" 
                                                           FontSize="10" 
                                                           FontWeight="SemiBold"
                                                           Foreground="#333"
                                                           TextWrapping="Wrap"
                                                           TextAlignment="Center"
                                                           Margin="0,10,0,0"
                                                           MaxWidth="100"
                                                           MaxHeight="40"
                                                           TextTrimming="CharacterEllipsis">
                                                    <TextBlock.ToolTip>
                                                        <ToolTip Content="{Binding Nom}"/>
                                                    </TextBlock.ToolTip>
                                                </TextBlock>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                
                                <!-- Ligne de base -->
                                <Border Grid.Row="1" Height="2" Background="#E0E0E0" Margin="0,5,0,0"/>
                            </Grid>
                            
                            <!-- Légende -->
                            <Border Background="#F8F9FA" CornerRadius="8" Padding="15,12" Margin="0,15,0,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Border Background="#00915A" Width="16" Height="16" CornerRadius="4" 
                                            VerticalAlignment="Center"/>
                                    <TextBlock Text="Équipes standard" FontSize="11" Foreground="#666" 
                                               Margin="8,0,20,0" VerticalAlignment="Center"/>
                                    <Border Background="#FF9800" Width="16" Height="16" CornerRadius="4" 
                                            VerticalAlignment="Center"/>
                                    <TextBlock Text="Équipe avec le plus de ressources" FontSize="11" 
                                               Foreground="#666" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                    
                    <TextBlock x:Name="TxtAucuneEquipe" Text="Vous n'êtes assigné à aucune équipe" 
                               FontSize="14" Foreground="#999" Margin="0,10,0,0" 
                               HorizontalAlignment="Center" Visibility="Collapsed"/>
                </StackPanel>
            </Border>
            
            <!-- STATISTIQUES PROJETS -->
            <Border Grid.Row="3" Style="{StaticResource MainCard}">
                <StackPanel>
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="#F0F9F4" CornerRadius="10" 
                                Padding="12,8" Margin="0,0,12,0">
                            <TextBlock Text="📊" FontSize="20"/>
                        </Border>
                        <TextBlock Grid.Column="1" FontSize="22" FontWeight="Bold" 
                                   Foreground="#1A1919" VerticalAlignment="Center">
                            <Run Text="APERÇU DES PROJETS"/>
                        </TextBlock>
                    </Grid>
                    
                    <ItemsControl ItemsSource="{Binding StatistiquesProjetsList}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Background="Transparent" BorderThickness="0" 
                                        Padding="0" Margin="0,0,16,16" Cursor="Hand"
                                        Width="290" Height="190"
                                        Tag="{Binding}"
                                        Click="ProjetCard_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="border" CornerRadius="12" Padding="22,18" 
                                                                BorderThickness="1">
                                                            <Border.Background>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#FFFFFF" Offset="0"/>
                                                                    <GradientStop Color="#F9F9F9" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                            <Border.BorderBrush>
                                                                <SolidColorBrush Color="#E0E0E0"/>
                                                            </Border.BorderBrush>
                                                            <Border.Effect>
                                                                <DropShadowEffect Color="#D0D0D0" BlurRadius="12" ShadowDepth="0" Opacity="0.2"/>
                                                            </Border.Effect>
                                                            <ContentPresenter/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                                                <Setter TargetName="border" Property="BorderBrush">
                                                                    <Setter.Value>
                                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                            <GradientStop Color="#00915A" Offset="0"/>
                                                                            <GradientStop Color="#00A566" Offset="1"/>
                                                                        </LinearGradientBrush>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Setter TargetName="border" Property="Effect">
                                                                    <Setter.Value>
                                                                        <DropShadowEffect Color="#00915A" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                    
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        
                                        <!-- En-tête projet -->
                                        <Grid Grid.Row="0" Margin="0,0,0,12">
                                            <StackPanel Orientation="Horizontal">
                                                <Border Width="5" Height="24" Background="{Binding CouleurProjet}" 
                                                        CornerRadius="3" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding NomProjet}" FontSize="15" 
                                                           FontWeight="Bold" Foreground="#1A1919" 
                                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding NomProjet}"/>
                                            </StackPanel>
                                        </Grid>
                                        
                        <!-- Badges -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                            <Border CornerRadius="8" Padding="10,5" Margin="0,0,8,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#C8E6C9" Offset="0"/>
                                        <GradientStop Color="#A5D6A7" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <TextBlock FontSize="11" Foreground="#1B5E20" FontWeight="Bold">
                                    <Run Text="{Binding NbTachesTotal}"/>
                                    <Run Text=" tâches"/>
                                </TextBlock>
                            </Border>
                            <Border CornerRadius="8" Padding="10,5">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#BBDEFB" Offset="0"/>
                                        <GradientStop Color="#90CAF9" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <TextBlock FontSize="11" Foreground="#0D47A1" FontWeight="Bold">
                                    <Run Text="{Binding ProgressionPourcentage, StringFormat='{}{0:F0}%'}"/>
                                </TextBlock>
                            </Border>
                        </StackPanel>                                        <!-- Détails des tâches -->
                                        <Grid Grid.Row="2" Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            
                                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,5,8">
                                                <TextBlock Text="À faire" FontSize="9" Foreground="#999" 
                                                           Margin="0,0,0,2"/>
                                                <TextBlock FontSize="16" FontWeight="Bold" Foreground="#9E9E9E">
                                                    <Run Text="{Binding NbTachesAfaire}"/>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <StackPanel Grid.Column="1" Grid.Row="0" Margin="5,0,0,8">
                                                <TextBlock Text="En cours" FontSize="9" Foreground="#999" 
                                                           Margin="0,0,0,2"/>
                                                <TextBlock FontSize="16" FontWeight="Bold" Foreground="#2196F3">
                                                    <Run Text="{Binding NbTachesEnCours}"/>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,5,0">
                                                <TextBlock Text="Terminées" FontSize="9" Foreground="#999" 
                                                           Margin="0,0,0,2"/>
                                                <TextBlock FontSize="16" FontWeight="Bold" Foreground="#4CAF50">
                                                    <Run Text="{Binding NbTachesTerminees}"/>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <StackPanel Grid.Column="1" Grid.Row="1" Margin="5,0,0,0">
                                                <TextBlock Text="En retard" FontSize="9" Foreground="#999" 
                                                           Margin="0,0,0,2"/>
                                                <TextBlock FontSize="16" FontWeight="Bold" Foreground="#FF5722">
                                                    <Run Text="{Binding NbTachesEnRetard}"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </Grid>
                                        
                                        <!-- Barre de progression avec gradient -->
                                        <Grid Grid.Row="3">
                                            <Border Background="#F0F0F0" Height="8" CornerRadius="4"/>
                                            <Border Height="8" CornerRadius="4" 
                                                    HorizontalAlignment="Left"
                                                    Width="{Binding ProgressionLargeurCarte}">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="#00915A" Offset="0"/>
                                                        <GradientStop Color="#00C853" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                            </Border>
                                        </Grid>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <!-- Message si aucun projet -->
                    <Border Background="#F5F5F5" CornerRadius="6" Padding="30,20" 
                            Visibility="{Binding AucunProjet, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="📁" FontSize="40" HorizontalAlignment="Center" 
                                       Margin="0,0,0,10"/>
                            <TextBlock Text="Aucun projet actif" FontSize="16" 
                                       Foreground="#999" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
            
            <!-- MES TÂCHES URGENTES -->
            <Border Grid.Row="4" Style="{StaticResource MainCard}">
                <StackPanel>
                    <Grid Margin="0,0,0,18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" Background="#FFEBEE" CornerRadius="6" 
                                Padding="8,5" Margin="0,0,10,0">
                            <TextBlock FontSize="14" FontWeight="Bold" Foreground="#FF5722">
                                <Run Text="⚡ "/>
                                <Run Text="{Binding NbTachesUrgentes, Mode=OneWay}"/>
                            </TextBlock>
                        </Border>
                        
                        <TextBlock Grid.Column="1" FontSize="20" FontWeight="Bold" 
                                   Foreground="#1A1919" VerticalAlignment="Center">
                            <Run Text="MES TÂCHES URGENTES"/>
                        </TextBlock>
                        
                        <Button Grid.Column="2" Content="Voir tout →" 
                                Style="{StaticResource ViewAllButton}"
                                Click="VoirToutesUrgentes_Click"/>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding TachesUrgentes}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2" HorizontalAlignment="Stretch"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FAFAFA" CornerRadius="8" Padding="18,15" 
                                        Margin="0,0,15,12" BorderBrush="#FFE0E0" BorderThickness="2" 
                                        Cursor="Hand" MouseLeftButtonDown="TacheUrgente_Click">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#FFE0E0" BlurRadius="8" 
                                                          ShadowDepth="1" Opacity="0.4" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel>
                                        <Grid>
                                            <TextBlock FontSize="15" FontWeight="SemiBold" 
                                                       Foreground="#1A1919" TextWrapping="Wrap">
                                                <Run Text="{Binding Titre}"/>
                                            </TextBlock>
                                            <Border Background="#FF5722" CornerRadius="4" 
                                                    Padding="6,3" HorizontalAlignment="Right" 
                                                    VerticalAlignment="Top">
                                                <TextBlock Text="⚡" FontSize="12"/>
                                            </Border>
                                        </Grid>
                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <Border Background="#FFF3E0" CornerRadius="4" Padding="8,4" 
                                                    Margin="0,0,8,0">
                                                <TextBlock FontSize="12" Foreground="#FF9800" FontWeight="SemiBold">
                                                    <Run Text="📅 "/>
                                                    <Run Text="{Binding DateFinAttendue, StringFormat='dd/MM'}"/>
                                                </TextBlock>
                                            </Border>
                                            <Border Background="#E3F2FD" CornerRadius="4" Padding="8,4">
                                                <TextBlock FontSize="12" Foreground="#2196F3" FontWeight="SemiBold">
                                                    <Run Text="{Binding ProjetNom}"/>
                                                </TextBlock>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Message si aucune tâche urgente -->
                    <Border CornerRadius="12" Padding="24" 
                            Visibility="{Binding HasNoUrgentTasks, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#C8E6C9" Offset="0"/>
                                <GradientStop Color="#A5D6A7" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="✅" FontSize="32" TextAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="Aucune tâche urgente pour le moment" 
                                       FontSize="14" Foreground="#1B5E20" TextAlignment="Center" FontWeight="Bold"/>
                            <TextBlock Text="Excellent travail ! Toutes vos priorités sont à jour." 
                                       FontSize="12" Foreground="#2E7D32" TextAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- NOTIFICATIONS RÉCENTES -->
            <Border Grid.Row="5" Style="{StaticResource MainCard}">
                <StackPanel>
                    <Grid Margin="0,0,0,18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="6" 
                                Padding="8,5" Margin="0,0,10,0">
                            <TextBlock FontSize="14" FontWeight="Bold" Foreground="#2196F3">
                                <Run Text="🔔 "/>
                                <Run Text="{Binding NbNotifications, Mode=OneWay}"/>
                            </TextBlock>
                        </Border>
                        
                        <TextBlock Grid.Column="1" FontSize="20" FontWeight="Bold" 
                                   Foreground="#1A1919" VerticalAlignment="Center">
                            <Run Text="NOTIFICATIONS RÉCENTES"/>
                        </TextBlock>
                        
                        <Button Grid.Column="2" Content="Voir tout →" 
                                Style="{StaticResource ViewAllButton}"
                                Click="VoirToutesNotifications_Click"/>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding NotificationsRecentes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FAFAFA" CornerRadius="8" Padding="15,12" 
                                        Margin="0,0,0,10" BorderBrush="#E8E8E8" BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Grid.Column="0" Background="#F5F5F5" 
                                                Width="40" Height="40" CornerRadius="20" 
                                                Margin="0,0,12,0" VerticalAlignment="Top">
                                            <Image Source="{Binding ImageCaramelFlopy}" 
                                                   Width="38" Height="38" Stretch="Uniform"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock FontSize="14" FontWeight="SemiBold" 
                                                       Foreground="#1A1919" TextWrapping="Wrap">
                                                <Run Text="{Binding Titre}"/>
                                            </TextBlock>
                                            <TextBlock FontSize="12" Foreground="#666666" 
                                                       TextWrapping="Wrap" Margin="0,3,0,0">
                                                <Run Text="{Binding Message}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        
                                        <Border Grid.Column="2" Background="#00915A" CornerRadius="12" 
                                                Width="8" Height="8" VerticalAlignment="Top" Margin="10,5,0,0"
                                                Visibility="{Binding EstNonLue, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Message si aucune notification -->
                    <Border CornerRadius="12" Padding="24" 
                            Visibility="{Binding HasNoNotifications, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#BBDEFB" Offset="0"/>
                                <GradientStop Color="#90CAF9" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel HorizontalAlignment="Center">
                            <Image Source="/Images/agent-project-and-change.png" 
                                   Width="80" Height="80" 
                                   Stretch="Uniform"
                                   Margin="0,0,0,12"/>
                            <TextBlock Text="Aucune nouvelle notification" 
                                       FontSize="14" Foreground="#0D47A1" TextAlignment="Center" FontWeight="Bold"/>
                            <TextBlock Text="Vous êtes à jour avec toutes les actualités." 
                                       FontSize="12" Foreground="#1565C0" TextAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- ACTIVITÉ RÉCENTE & QUICK ACTIONS -->
            <Grid Grid.Row="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- ACTIVITÉ RÉCENTE -->
                <Border Grid.Column="0" Style="{StaticResource MainCard}" Margin="0,0,10,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,18">
                            <TextBlock FontSize="20" FontWeight="Bold" Foreground="#1A1919">
                                <Run Text="📊 ACTIVITÉ RÉCENTE"/>
                            </TextBlock>
                        </Grid>
                        
                        <ItemsControl ItemsSource="{Binding ActivitesRecentes}" MaxHeight="300">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Background="Transparent" BorderThickness="0" 
                                            Padding="0" Margin="0,0,0,8" Cursor="Hand"
                                            HorizontalContentAlignment="Stretch"
                                            Tag="{Binding}"
                                            Click="Activite_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border" Background="#FAFAFA" 
                                                                    CornerRadius="6" Padding="12,10">
                                                                <ContentPresenter/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#E8F5E9"/>
                                                                </Trigger>
                                                                <Trigger Property="IsPressed" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#C8E6C9"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <Border Grid.Column="0" Width="4" CornerRadius="2" 
                                                    Margin="0,0,12,0" Background="#00915A"/>
                                            
                                            <StackPanel Grid.Column="1">
                                                <TextBlock FontSize="13" FontWeight="SemiBold" 
                                                           Foreground="#1A1919" TextWrapping="Wrap">
                                                    <Run Text="{Binding Action}"/>
                                                </TextBlock>
                                                <TextBlock FontSize="11" Foreground="#666666" Margin="0,2,0,0">
                                                    <Run Text="{Binding Details}"/>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <TextBlock Grid.Column="2" FontSize="11" Foreground="#999999" 
                                                       VerticalAlignment="Top">
                                                <Run Text="{Binding Temps}"/>
                                            </TextBlock>
                                        </Grid>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                
                <!-- ACTIONS RAPIDES -->
                <Border Grid.Column="1" Style="{StaticResource MainCard}" Margin="10,0,0,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,18">
                            <TextBlock FontSize="20" FontWeight="Bold" Foreground="#1A1919">
                                <Run Text="⚡ ACTIONS"/>
                            </TextBlock>
                        </Grid>
                        
                        <StackPanel>
                            <!-- Nouvelle tâche -->
                            <Button BorderThickness="0" Padding="15,12" Margin="0,0,0,10" Cursor="Hand"
                                    Click="NouvelleTache_Click">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#00915A" Offset="0"/>
                                        <GradientStop Color="#00C078" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="10"/>
                                    </Style>
                                </Button.Resources>
                                <Button.Effect>
                                    <DropShadowEffect Color="#00915A" BlurRadius="12" 
                                                      ShadowDepth="2" Opacity="0.4" Direction="270"/>
                                </Button.Effect>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="➕" FontSize="16" Foreground="White" 
                                               Margin="0,0,8,0"/>
                                    <TextBlock Text="Nouvelle tâche" FontSize="14" 
                                               FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Button>
                            
                            <!-- Voir Kanban -->
                            <Button BorderThickness="2" Padding="15,12" Margin="0,0,0,10" 
                                    Cursor="Hand" Click="VoirKanban_Click">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#E0F2F1" Offset="0"/>
                                        <GradientStop Color="#B2DFDB" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <Button.BorderBrush>
                                    <SolidColorBrush Color="#00897B"/>
                                </Button.BorderBrush>
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="10"/>
                                    </Style>
                                </Button.Resources>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📋" FontSize="16" Foreground="#00695C" 
                                               Margin="0,0,8,0"/>
                                    <TextBlock Text="Voir Kanban" FontSize="14" 
                                               FontWeight="Bold" Foreground="#00695C"/>
                                </StackPanel>
                            </Button>
                            
                            <!-- Timeline -->
                            <Button BorderThickness="2" Padding="15,12" Cursor="Hand" 
                                    Click="VoirTimeline_Click">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#E1BEE7" Offset="0"/>
                                        <GradientStop Color="#CE93D8" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <Button.BorderBrush>
                                    <SolidColorBrush Color="#8E24AA"/>
                                </Button.BorderBrush>
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="10"/>
                                    </Style>
                                </Button.Resources>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📅" FontSize="16" Foreground="#6A1B9A" 
                                               Margin="0,0,8,0"/>
                                    <TextBlock Text="Timeline projet" FontSize="14" 
                                               FontWeight="Bold" Foreground="#6A1B9A"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
