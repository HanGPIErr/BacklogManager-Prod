<Window x:Class="BacklogManager.Views.ParametresWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="⚙️ Paramètres Système - BNP Paribas Backlog Manager" 
        Height="650" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="SectionTitle">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00915A"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="6" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="#00915A"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="⚙️ PARAMÈTRES SYSTÈME" FontSize="20" FontWeight="Bold" Foreground="#1A1919"/>
                <TextBlock Text="Configuration de l'application et gestion des données" 
                           FontSize="13" Foreground="#6D6D6D" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Contenu -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Section Base de données -->
                <Border Background="White" CornerRadius="6" Padding="20" Margin="0,0,0,15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.15"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="💾 Base de données" Style="{StaticResource SectionTitle}"/>
                        
                        <TextBlock Text="Chemin de la base de données:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TxtCheminDB" Grid.Column="0" Height="32" Padding="8" 
                                     VerticalContentAlignment="Center" IsReadOnly="True" 
                                     Background="#F9F9F9" Margin="0,0,10,0"/>
                            <Button x:Name="BtnChangerDB" Grid.Column="1" Content="📁 Parcourir" 
                                    Background="#3498DB" Foreground="White" Click="BtnChangerDB_Click"/>
                        </Grid>
                        
                        <TextBlock Text="ℹ️ Redémarrage requis après modification du chemin" 
                                   Foreground="#FF8C00" FontSize="11" Margin="0,5,0,0" 
                                   FontStyle="Italic"/>
                    </StackPanel>
                </Border>

                <!-- Section Export/Import -->
                <Border Background="White" CornerRadius="6" Padding="20" Margin="0,0,0,15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.15"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="📤 Export / Import des données" Style="{StaticResource SectionTitle}"/>
                        
                        <TextBlock Text="Export des données:" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="BtnExportJSON" Content="💾 Exporter en JSON" 
                                    Background="#00915A" Foreground="White" 
                                    Click="BtnExportJSON_Click"/>
                            <Button x:Name="BtnExportCSV" Content="📊 Exporter en CSV" 
                                    Background="#3498DB" Foreground="White" 
                                    Click="BtnExportCSV_Click"/>
                        </StackPanel>

                        <TextBlock Text="Import des données:" FontWeight="SemiBold" Margin="0,15,0,10"/>
                        <Button x:Name="BtnImportJSON" Content="📥 Importer depuis JSON" 
                                Background="#9B59B6" Foreground="White" 
                                Click="BtnImportJSON_Click" Width="200" HorizontalAlignment="Left"/>
                        
                        <Border Background="#FFF8E1" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                            <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="#856404">
                                <Run Text="⚠️ Attention: "/>
                                <Run Text="L'import remplacera toutes les données existantes. Faites une sauvegarde avant d'importer."/>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Section Sauvegarde/Restauration -->
                <Border Background="White" CornerRadius="6" Padding="20" Margin="0,0,0,15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.15"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="💼 Sauvegarde et restauration" Style="{StaticResource SectionTitle}"/>
                        
                        <TextBlock Text="Créer une sauvegarde complète:" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Button x:Name="BtnCreerSauvegarde" Content="💾 Créer une sauvegarde" 
                                Background="#00915A" Foreground="White" 
                                Click="BtnCreerSauvegarde_Click" Width="200" HorizontalAlignment="Left"/>
                        
                        <TextBlock Text="Restaurer depuis une sauvegarde:" FontWeight="SemiBold" Margin="0,15,0,10"/>
                        <Button x:Name="BtnRestaurerSauvegarde" Content="📥 Restaurer une sauvegarde" 
                                Background="#E74C3C" Foreground="White" 
                                Click="BtnRestaurerSauvegarde_Click" Width="200" HorizontalAlignment="Left"/>
                        
                        <TextBlock Text="Dernière sauvegarde:" FontWeight="SemiBold" Margin="0,15,0,5"/>
                        <TextBlock x:Name="TxtDerniereSauvegarde" Text="Aucune sauvegarde trouvée" 
                                   Foreground="#6D6D6D" FontSize="12"/>
                    </StackPanel>
                </Border>

                <!-- Section Affichage -->
                <Border Background="White" CornerRadius="6" Padding="20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.15"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="🎨 Paramètres d'affichage" Style="{StaticResource SectionTitle}"/>
                        
                        <TextBlock Text="Thème:" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <ComboBox x:Name="CboTheme" Height="32" Padding="8" 
                                  SelectionChanged="CboTheme_SelectionChanged">
                            <ComboBoxItem Content="🌞 Clair (BNP Paribas)" IsSelected="True"/>
                            <ComboBoxItem Content="🌙 Sombre" IsEnabled="False"/>
                            <ComboBoxItem Content="🎨 Haute contraste" IsEnabled="False"/>
                        </ComboBox>
                        
                        <TextBlock Text="Langue:" FontWeight="SemiBold" Margin="0,15,0,10"/>
                        <ComboBox x:Name="CboLangue" Height="32" Padding="8"
                                  SelectionChanged="CboLangue_SelectionChanged">
                            <ComboBoxItem Content="🇫🇷 Français" IsSelected="True"/>
                            <ComboBoxItem Content="🇬🇧 English" IsEnabled="False"/>
                            <ComboBoxItem Content="🇪🇸 Español" IsEnabled="False"/>
                        </ComboBox>
                        
                        <TextBlock Text="ℹ️ Fonctionnalités à venir dans une prochaine version" 
                                   Foreground="#95A5A6" FontSize="11" Margin="0,10,0,0" 
                                   FontStyle="Italic"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Boutons de bas de page -->
        <Border Grid.Row="2" Background="White" Padding="15" Margin="0,15,0,0" 
                BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnFermer" Content="✅ Fermer" 
                        Background="#95A5A6" Foreground="White"
                        Click="BtnFermer_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
