<Window x:Class="BacklogManager.Views.DetailsDemandeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Détails de la demande" Height="700" Width="650" 
        WindowStartupLocation="CenterOwner" Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- En-tête -->
        <Border Grid.Row="0" Background="#00A651" Padding="20">
            <TextBlock Text="DÉTAILS DE LA DEMANDE" FontSize="20" 
                      FontWeight="Bold" Foreground="White"/>
        </Border>
        
        <!-- Contenu -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30,20">
                
                <!-- Titre -->
                <TextBlock Text="TITRE" FontSize="11" Foreground="#999999" FontWeight="Bold"/>
                <TextBlock x:Name="TxtTitre" FontSize="16" FontWeight="Bold" 
                          TextWrapping="Wrap" Margin="0,5,0,15"/>
                
                <!-- Description -->
                <TextBlock Text="DESCRIPTION" FontSize="11" Foreground="#999999" FontWeight="Bold"/>
                <TextBlock x:Name="TxtDescription" FontSize="13" TextWrapping="Wrap" 
                          Margin="0,5,0,15"/>
                
                <!-- Infos de base -->
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                       CornerRadius="5" Padding="15" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="TYPE" FontSize="10" Foreground="#999999" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtType" FontSize="13" FontWeight="Bold"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="0" Grid.Column="1">
                            <TextBlock Text="CRITICITÉ" FontSize="10" Foreground="#999999" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtCriticite" FontSize="13" FontWeight="Bold"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="2" Grid.Column="0">
                            <TextBlock Text="STATUT" FontSize="10" Foreground="#999999" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtStatut" FontSize="13" FontWeight="Bold" Foreground="#00A651"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="2" Grid.Column="1">
                            <TextBlock Text="DATE CRÉATION" FontSize="10" Foreground="#999999" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtDateCreation" FontSize="13"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2">
                            <TextBlock Text="DATE PRÉVISIONNELLE" FontSize="10" Foreground="#999999" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtDatePrev" FontSize="13"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Contexte -->
                <TextBlock Text="CONTEXTE MÉTIER" FontSize="11" Foreground="#999999" FontWeight="Bold"/>
                <TextBlock x:Name="TxtContexte" FontSize="13" TextWrapping="Wrap" 
                          Margin="0,5,0,15"/>
                
                <!-- Bénéfices -->
                <TextBlock Text="BÉNÉFICES ATTENDUS" FontSize="11" Foreground="#999999" FontWeight="Bold"/>
                <TextBlock x:Name="TxtBenefices" FontSize="13" TextWrapping="Wrap" 
                          Margin="0,5,0,15"/>
                
                <!-- Intervenants -->
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                       CornerRadius="5" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="INTERVENANTS" FontSize="13" FontWeight="Bold" 
                                  Foreground="#00A651" Margin="0,0,0,10"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Demandeur:" FontWeight="Bold"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtDemandeur"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Business Analyst:" FontWeight="Bold"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtBA"/>
                            
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Chef de Projet:" FontWeight="Bold"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="TxtCP"/>
                            
                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Développeur:" FontWeight="Bold"/>
                            <TextBlock Grid.Row="6" Grid.Column="1" x:Name="TxtDev"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Chiffrage -->
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                       CornerRadius="5" Padding="15">
                    <StackPanel>
                        <TextBlock Text="CHIFFRAGE" FontSize="13" FontWeight="Bold" 
                                  Foreground="#00A651" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Estimation:" FontWeight="Bold" Margin="0,0,10,0"/>
                            <TextBlock x:Name="TxtChiffrage"/>
                            <TextBlock Text=" heures" Margin="3,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- Bouton -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" 
               Padding="20">
            <Button Content="Fermer" Width="100" Height="35" 
                   Background="#00A651" Foreground="White" BorderThickness="0" 
                   FontSize="13" FontWeight="Bold" Cursor="Hand"
                   HorizontalAlignment="Right" Click="OnFermer"/>
        </Border>
    </Grid>
</Window>
