<UserControl x:Class="BacklogManager.Views.ParametresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F0F2F5">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="SectionTitle">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1A1919"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="35,25">
            <!-- Header -->
            <Border CornerRadius="10" Padding="30,25" Margin="0,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#00915A" Offset="0"/>
                        <GradientStop Color="#00A566" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.2" Color="#00915A"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="TxtTitle" Text="⚙️ PARAMÈTRES" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="TxtSubtitle" Text="Configuration de l'application et gestion des données" 
                               FontSize="14" Foreground="White" Opacity="0.9" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Section Base de données -->
            <Border CornerRadius="10" Padding="25" Margin="0,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="TxtDatabaseSection" Text="💾 Base de données" Style="{StaticResource SectionTitle}"/>
                    
                    <TextBlock x:Name="TxtDatabasePath" Text="Chemin de la base de données:" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#666"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtCheminDB" Grid.Column="0" Height="40" Padding="12" 
                                 VerticalContentAlignment="Center" IsReadOnly="True" 
                                 Background="White" BorderBrush="#DDD" BorderThickness="1" Margin="0,0,10,0" 
                                 FontSize="13"/>
                        <Button x:Name="BtnChangerDB" Grid.Column="1" Content="📁 Parcourir" 
                                Background="#3498DB" Foreground="White" Click="BtnChangerDB_Click" 
                                Height="40" Padding="20,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                    <Border.Effect>
                                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="#3498DB"/>
                                                    </Border.Effect>
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2980B9"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                    
                    <TextBlock x:Name="TxtRestartRequired" Text="ℹ️ Redémarrage requis après modification du chemin" 
                               Foreground="#FF8C00" FontSize="12" Margin="0,8,0,0" 
                               FontStyle="Italic"/>
                </StackPanel>
            </Border>

            <!-- Section Export/Import -->
            <Border x:Name="BorderExportImport" CornerRadius="10" Padding="25" Margin="0,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="TxtExportImportSection" Text="📤 Export / Import des données" Style="{StaticResource SectionTitle}"/>
                    
                    <TextBlock x:Name="TxtExportFull" Text="Export des données (base complète):" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#666"/>
                    <WrapPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <Button x:Name="BtnExportSQLite" Content="💾 Exporter SQLite (.db)" 
                                Background="#00915A" Foreground="White" 
                                Click="BtnExportSQLite_Click"/>
                        <Button x:Name="BtnExportJSON" Content="📄 Exporter JSON complet" 
                                Background="#3498DB" Foreground="White" 
                                Click="BtnExportJSON_Click"/>
                        <Button x:Name="BtnExportComplet" Content="📦 Export complet (SQLite+JSON)" 
                                Background="#9B59B6" Foreground="White" 
                                Click="BtnExportComplet_Click"/>
                    </WrapPanel>
                    
                    <TextBlock x:Name="TxtExportPartial" Text="Export partiel:" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#666"/>
                    <Button x:Name="BtnExportCSV" Content="📊 Exporter backlog en CSV" 
                            Background="#95A5A6" Foreground="White" 
                            Click="BtnExportCSV_Click" Width="240" HorizontalAlignment="Left" Margin="5,0,0,20"/>

                    <TextBlock x:Name="TxtImportData" Text="Import des données:" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#666"/>
                    <WrapPanel Orientation="Horizontal">
                        <Button x:Name="BtnImportSQLite" Content="📥 Importer SQLite (.db)" 
                                Background="#E67E22" Foreground="White" 
                                Click="BtnImportSQLite_Click"/>
                        <Button x:Name="BtnImportJSON" Content="📥 Importer JSON" 
                                Background="#16A085" Foreground="White" 
                                Click="BtnImportJSON_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Section Apparence -->
            <Border CornerRadius="10" Padding="25" Margin="0,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="TxtAppearanceSection" Text="🎨 Apparence" Style="{StaticResource SectionTitle}"/>
                    
                    <TextBlock x:Name="TxtTheme" Text="Thème de l'interface:" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#666"/>
                    <ComboBox x:Name="CboTheme" Height="40" Padding="12" FontSize="13" Margin="0,0,0,15"
                              SelectedIndex="0" SelectionChanged="CboTheme_SelectionChanged">
                        <ComboBoxItem x:Name="CboThemeLight" Content="🌞 Clair (par défaut)"/>
                        <ComboBoxItem x:Name="CboThemeDark" Content="🌙 Sombre (prochainement)"/>
                    </ComboBox>
                    
                    <TextBlock x:Name="TxtLanguage" Text="Langue de l'interface:" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#666"/>
                    <ComboBox x:Name="CboLangue" Height="40" Padding="12" FontSize="13" Margin="0,0,0,10"
                              SelectionChanged="CboLangue_SelectionChanged">
                        <ComboBoxItem Content="🇫🇷 Français" Tag="fr"/>
                        <ComboBoxItem Content="🇬🇧 English" Tag="en"/>
                        <ComboBoxItem Content="🇪🇸 Español" Tag="es"/>
                    </ComboBox>
                    
                    <TextBlock x:Name="TxtLanguageChangeLater" Text="ℹ️ Le changement de langue sera appliqué au prochain démarrage" 
                               Foreground="#3498DB" FontSize="12" Margin="0,5,0,0" 
                               FontStyle="Italic"/>
                </StackPanel>
            </Border>

            <!-- Section Notifications -->
            <Border CornerRadius="10" Padding="25" Margin="0,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="TxtNotificationsSection" Text="🔔 Notifications" Style="{StaticResource SectionTitle}"/>
                    
                    <CheckBox x:Name="ChkNotifTachesAssignees" Content="Notifications pour les tâches assignées" 
                              FontSize="13" Margin="0,0,0,10" IsChecked="True"/>
                    <CheckBox x:Name="ChkNotifEcheances" Content="Alertes pour les échéances proches" 
                              FontSize="13" Margin="0,0,0,10" IsChecked="True"/>
                    <CheckBox x:Name="ChkNotifCommentaires" Content="Notifications pour les nouveaux commentaires" 
                              FontSize="13" Margin="0,0,0,10" IsChecked="True"/>
                    <CheckBox x:Name="ChkNotifMentions" Content="Alertes quand je suis mentionné (@utilisateur)" 
                              FontSize="13" Margin="0,0,0,10" IsChecked="True"/>
                    
                    <TextBlock x:Name="TxtNotifInfo" Text="💡 Les notifications sont actuellement gérées automatiquement. Paramétrage fin disponible prochainement" 
                               Foreground="#3498DB" FontSize="12" Margin="0,5,0,0" 
                               FontStyle="Italic"/>
                </StackPanel>
            </Border>

            <!-- Section Maintenance -->
            <Border x:Name="BorderMaintenance" CornerRadius="10" Padding="25" Margin="0,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="TxtMaintenanceSection" Text="🔧 Maintenance" Style="{StaticResource SectionTitle}"/>
                    
                    <TextBlock x:Name="TxtCleanup" Text="Nettoyage et optimisation:" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#666"/>
                    <WrapPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <Button x:Name="BtnViderCache" Content="🗑️ Vider le cache" 
                                Background="#E74C3C" Foreground="White" 
                                Click="BtnViderCache_Click"/>
                        <Button x:Name="BtnOptimiserDB" Content="⚡ Optimiser la base" 
                                Background="#F39C12" Foreground="White" 
                                Click="BtnOptimiser_Click"/>
                        <Button x:Name="BtnReinitialiser" Content="🔄 Réinitialiser l'app" 
                                Background="#C0392B" Foreground="White" 
                                Click="BtnReinitialiser_Click"/>
                    </WrapPanel>
                    
                    <TextBlock x:Name="TxtResetWarning" Text="⚠️ Attention: La réinitialisation supprimera TOUTES les données" 
                               Foreground="#E74C3C" FontSize="12" Margin="0,0,0,0" 
                               FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <!-- Section Informations -->
            <Border CornerRadius="10" Padding="25">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="TxtInfoSection" Text="ℹ️ Informations système" Style="{StaticResource SectionTitle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" x:Name="TxtLabelVersion" Text="Version:" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#666"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtVersion" Text="1.0.0" Margin="0,0,0,8" Foreground="#333"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="0" x:Name="TxtLabelDatabase" Text="Base de données:" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#666"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtTailleDB" Text="-" Margin="0,0,0,8" Foreground="#333"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="0" x:Name="TxtLabelTaskCount" Text="Nombre de tâches:" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#666"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtNbTaches" Text="-" Margin="0,0,0,8" Foreground="#333"/>
                        
                        <TextBlock Grid.Row="3" Grid.Column="0" x:Name="TxtLabelUserCount" Text="Nombre d'utilisateurs:" FontWeight="SemiBold" Foreground="#666"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" x:Name="TxtNbUtilisateurs" Text="-" Foreground="#333"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
