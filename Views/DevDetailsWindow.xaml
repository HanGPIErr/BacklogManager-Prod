<Window x:Class="BacklogManager.Views.DevDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Détails du Développeur" Height="750" Width="1200"
        WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header du dev -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.2" Color="#00915A"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Nom et avatar -->
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Border Background="#00915A" Width="60" Height="60" CornerRadius="30" Margin="0,0,15,0">
                        <TextBlock x:Name="TxtInitiales" FontSize="24" FontWeight="Bold" 
                                   Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="TxtNomDev" FontSize="28" FontWeight="Bold" Foreground="#1A1919"/>
                        <TextBlock x:Name="TxtRole" FontSize="14" Foreground="#00915A" FontWeight="SemiBold" Margin="0,3,0,0"/>
                        <!-- Filtres de période -->
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="🗓" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="10"/>
                            <ComboBox x:Name="CboPeriodeDev" Width="160" VerticalAlignment="Center"
                                      SelectionChanged="CboPeriodeDev_SelectionChanged" FontSize="11">
                                <ComboBoxItem Content="Année en cours"/>
                                <ComboBoxItem Content="6 derniers mois"/>
                                <ComboBoxItem Content="Mois en cours"/>
                                <ComboBoxItem Content="3 derniers mois"/>
                                <ComboBoxItem Content="Période perso"/>
                                <ComboBoxItem Content="Tout afficher" IsSelected="True"/>
                            </ComboBox>
                            <StackPanel x:Name="PanelDatesDevCustom" Orientation="Horizontal" Margin="10,0,0,0" Visibility="Collapsed">
                                <DatePicker x:Name="DateDebutDev" Width="100" FontSize="10"/>
                                <DatePicker x:Name="DateFinDev" Width="100" Margin="5,0,0,0" FontSize="10"/>
                                <Button Content="✓" Padding="5,3" Margin="5,0,0,0" 
                                        Background="#00915A" Foreground="White" BorderThickness="0"
                                        Click="BtnAppliquerPeriodeDev_Click" FontSize="10"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    <Button Content="🤖 Analyse IA" Click="AnalyserIA_Click"
                            Background="#667eea" Foreground="White" 
                            Padding="12,8" FontWeight="SemiBold" FontSize="12"
                            BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"
                            Margin="15,0,0,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#667eea"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderThickness="0" 
                                                    CornerRadius="6" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#5568d3"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="📄 Exporter PDF" Click="ExporterPDF_Click"
                            Background="#FF6F00" Foreground="White" 
                            Padding="12,8" FontWeight="SemiBold" FontSize="12"
                            BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"
                            Margin="15,0,0,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FF6F00"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderThickness="0" 
                                                    CornerRadius="6" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E66300"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Métriques clés -->
                <Grid Grid.Row="1" Margin="0,20,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total tâches -->
                    <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="6" Padding="12" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="📋 Total" FontSize="11" Foreground="#1565C0" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TxtTotalTaches" FontSize="26" FontWeight="Bold" 
                                       Foreground="#2196F3" Margin="0,4,0,0"/>
                            <TextBlock Text="tâches" FontSize="9" Foreground="#1565C0"/>
                        </StackPanel>
                    </Border>

                    <!-- En cours -->
                    <Border Grid.Column="1" Background="#FFF3E0" CornerRadius="6" Padding="12" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="🔄 En cours" FontSize="11" Foreground="#E65100" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TxtEnCours" FontSize="26" FontWeight="Bold" 
                                       Foreground="#FF9800" Margin="0,4,0,0"/>
                            <TextBlock Text="actives" FontSize="9" Foreground="#E65100"/>
                        </StackPanel>
                    </Border>

                    <!-- Terminées -->
                    <Border Grid.Column="2" Background="#E8F5E9" CornerRadius="6" Padding="12" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="✅ Terminé" FontSize="11" Foreground="#2E7D32" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TxtTerminees" FontSize="26" FontWeight="Bold" 
                                       Foreground="#00915A" Margin="0,4,0,0"/>
                            <TextBlock Text="finies" FontSize="9" Foreground="#2E7D32"/>
                        </StackPanel>
                    </Border>

                    <!-- Charge -->
                    <Border Grid.Column="3" Background="#F3E5F5" CornerRadius="6" Padding="12" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="⏱️ Charge" FontSize="11" Foreground="#6A1B9A" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock x:Name="TxtCharge" FontSize="26" FontWeight="Bold" Foreground="#9C27B0"/>
                                <TextBlock Text="j" FontSize="14" Foreground="#9C27B0" VerticalAlignment="Bottom" Margin="2,0,0,4"/>
                            </StackPanel>
                            <TextBlock Text="estimée" FontSize="9" Foreground="#6A1B9A"/>
                        </StackPanel>
                    </Border>

                    <!-- Temps réel -->
                    <Border Grid.Column="4" Background="#FCE4EC" CornerRadius="6" Padding="12" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="⚡ Réalisé" FontSize="11" Foreground="#C2185B" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock x:Name="TxtTempsReel" FontSize="26" FontWeight="Bold" Foreground="#E91E63"/>
                                <TextBlock Text="j" FontSize="14" Foreground="#E91E63" VerticalAlignment="Bottom" Margin="2,0,0,4"/>
                            </StackPanel>
                            <TextBlock Text="travail effectué" FontSize="9" Foreground="#C2185B"/>
                        </StackPanel>
                    </Border>

                    <!-- Taux de réalisation -->
                    <Border Grid.Column="5" Background="#FFF9C4" CornerRadius="6" Padding="12">
                        <StackPanel>
                            <TextBlock Text="🎯 Taux réal." FontSize="11" Foreground="#F57F17" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TxtTauxRealisation" FontSize="26" FontWeight="Bold" 
                                       Foreground="#FBC02D" Margin="0,4,0,0"/>
                            <TextBlock Text="prévu/réel" FontSize="9" Foreground="#F57F17"/>
                        </StackPanel>
                    </Border>

                    <!-- Deuxième ligne : Congés et Non travaillé -->
                    <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Background="#E3F2FD" CornerRadius="6" Padding="12" Margin="0,8,8,0">
                        <StackPanel>
                            <TextBlock Text="🏖️ Congés" FontSize="11" Foreground="#1565C0" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock x:Name="TxtConges" FontSize="26" FontWeight="Bold" Foreground="#2196F3"/>
                                <TextBlock Text="j" FontSize="14" Foreground="#2196F3" VerticalAlignment="Bottom" Margin="2,0,0,4"/>
                            </StackPanel>
                            <TextBlock Text="sur la période" FontSize="9" Foreground="#1565C0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Background="#EEEEEE" CornerRadius="6" Padding="12" Margin="0,8,8,0">
                        <StackPanel>
                            <TextBlock Text="🚫 Non travaillé" FontSize="11" Foreground="#424242" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock x:Name="TxtNonTravaille" FontSize="26" FontWeight="Bold" Foreground="#757575"/>
                                <TextBlock Text="j" FontSize="14" Foreground="#757575" VerticalAlignment="Bottom" Margin="2,0,0,4"/>
                            </StackPanel>
                            <TextBlock Text="sur la période" FontSize="9" Foreground="#424242"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="4" Grid.ColumnSpan="2" Background="#E8F5E9" CornerRadius="6" Padding="12" Margin="0,8,0,0">
                        <StackPanel>
                            <TextBlock Text="📋 Total CRA" FontSize="11" Foreground="#2E7D32" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock x:Name="TxtTotalCRA" FontSize="26" FontWeight="Bold" Foreground="#00915A"/>
                                <TextBlock Text="j" FontSize="14" Foreground="#00915A" VerticalAlignment="Bottom" Margin="2,0,0,4"/>
                            </StackPanel>
                            <TextBlock Text="travail + congés + non-travaillé" FontSize="9" Foreground="#2E7D32"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <!-- Graphiques et stats -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Répartition par statut -->
            <Border Grid.Column="0" Background="White" CornerRadius="6" Padding="15" Margin="0,0,8,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.1" Color="#00915A"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="📊 Répartition par Statut" FontSize="14" FontWeight="Bold" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- À faire -->
                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="À faire" FontSize="12" VerticalAlignment="Center"/>
                            <ProgressBar Grid.Column="1" x:Name="BarAfaire" Height="20" Foreground="#2196F3" Background="#E3F2FD" BorderThickness="0"/>
                            <TextBlock Grid.Column="2" x:Name="TxtCountAfaire" FontSize="12" FontWeight="Bold" 
                                       Foreground="#2196F3" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>

                        <!-- En cours -->
                        <Grid Grid.Row="1" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="En cours" FontSize="12" VerticalAlignment="Center"/>
                            <ProgressBar Grid.Column="1" x:Name="BarEnCours" Height="20" Foreground="#FF9800" Background="#FFF3E0" BorderThickness="0"/>
                            <TextBlock Grid.Column="2" x:Name="TxtCountEnCours" FontSize="12" FontWeight="Bold" 
                                       Foreground="#FF9800" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>

                        <!-- En test -->
                        <Grid Grid.Row="2" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="En test" FontSize="12" VerticalAlignment="Center"/>
                            <ProgressBar Grid.Column="1" x:Name="BarEnTest" Height="20" Foreground="#9C27B0" Background="#F3E5F5" BorderThickness="0"/>
                            <TextBlock Grid.Column="2" x:Name="TxtCountEnTest" FontSize="12" FontWeight="Bold" 
                                       Foreground="#9C27B0" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Terminé -->
                        <Grid Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Terminé" FontSize="12" VerticalAlignment="Center"/>
                            <ProgressBar Grid.Column="1" x:Name="BarTermine" Height="20" Foreground="#00915A" Background="#E8F5E9" BorderThickness="0"/>
                            <TextBlock Grid.Column="2" x:Name="TxtCountTermine" FontSize="12" FontWeight="Bold" 
                                       Foreground="#00915A" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Répartition par projet -->
            <Border Grid.Column="1" Background="White" CornerRadius="6" Padding="15" Margin="8,0,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.1" Color="#00915A"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="🎯 Tâches par Projet" FontSize="14" FontWeight="Bold" Margin="0,0,0,12"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ListeProjets">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="50"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding ProjetNom}" FontSize="12" 
                                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                        <Border Grid.Column="1" Background="#00915A" CornerRadius="10" Padding="6,3">
                                            <TextBlock Text="{Binding Count}" FontSize="11" FontWeight="Bold" 
                                                       Foreground="White" HorizontalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Liste des tâches -->
        <Border Grid.Row="2" Background="White" CornerRadius="6" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.1" Color="#00915A"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="📋 Toutes les Tâches" FontSize="16" FontWeight="Bold"/>
                    <ComboBox Grid.Column="1" x:Name="CmbFiltreStatut" Width="120" Padding="8,5" 
                              SelectionChanged="CmbFiltreStatut_SelectionChanged">
                        <ComboBoxItem Content="Tous" IsSelected="True"/>
                        <ComboBoxItem Content="À faire"/>
                        <ComboBoxItem Content="En cours"/>
                        <ComboBoxItem Content="En test"/>
                        <ComboBoxItem Content="Terminé"/>
                    </ComboBox>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ListeTaches">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1" 
                                        CornerRadius="6" Padding="12" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                <TextBlock Text="{Binding Titre}" FontSize="14" FontWeight="SemiBold" 
                                                           Foreground="#1A1919" VerticalAlignment="Center"/>
                                                <Border Background="{Binding StatutColor}" CornerRadius="3" 
                                                        Padding="6,2" Margin="10,0,0,0">
                                                    <TextBlock Text="{Binding Statut}" FontSize="10" 
                                                               Foreground="White" FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🎯" FontSize="11" Margin="0,0,4,0"/>
                                                <TextBlock Text="{Binding ProjetNom}" FontSize="12" Foreground="#00915A" 
                                                           FontWeight="SemiBold" Margin="0,0,15,0"/>
                                                <TextBlock Text="⏱️" FontSize="11" Margin="0,0,4,0"/>
                                                <TextBlock FontSize="12" Foreground="#555">
                                                    <Run Text="{Binding ChiffrageJours, StringFormat={}{0:F1}}"/>
                                                    <Run Text="j estimé"/>
                                                </TextBlock>
                                                <TextBlock Text=" • " FontSize="12" Foreground="#CCC" Margin="5,0"/>
                                                <TextBlock FontSize="12" Foreground="#9C27B0" FontWeight="SemiBold">
                                                    <Run Text="{Binding TempsReelJours, StringFormat={}{0:F1}}"/>
                                                    <Run Text="j réalisé"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Width="80">
                                            <TextBlock Text="{Binding ProgressionPct, StringFormat={}{0:F0}%}" 
                                                       FontSize="16" FontWeight="Bold" Foreground="#00915A" 
                                                       HorizontalAlignment="Right" Margin="0,0,0,3"/>
                                            <ProgressBar Value="{Binding ProgressionPct}" Height="6" 
                                                         Foreground="#00915A" Background="#E0E0E0" BorderThickness="0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>
