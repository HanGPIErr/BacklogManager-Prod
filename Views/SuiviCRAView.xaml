<UserControl x:Class="BacklogManager.Views.SuiviCRAView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BacklogManager.Shared"
             xmlns:converters="clr-namespace:BacklogManager.Converters"
             Background="#F5F5F5">

    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:IndexToPositionConverter x:Key="IndexToPositionConverter"/>
        <converters:PercentageToWidthConverter x:Key="PercentageToWidthConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Border Grid.Row="0" CornerRadius="10" Padding="20" Margin="0,0,0,15">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#00915A" Offset="0"/>
                    <GradientStop Color="#00A566" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.2" Color="#00915A"/>
            </Border.Effect>
            <WrapPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,10">
                    <TextBlock Text="📊 SUIVI DES CRA" FontSize="24" FontWeight="Bold" 
                               Foreground="White" Margin="0,0,0,5"/>
                    <TextBlock Text="Vue administrative - Tracking détaillé par équipe et membre" 
                               FontSize="13" Foreground="White" Opacity="0.9"/>
                </StackPanel>

                <!-- Sélection équipe et membre -->
                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Équipe -->
                    <TextBlock Text="🏢 Équipe:" FontSize="13" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="White" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding Equipes}" DisplayMemberPath="Nom"
                              SelectedItem="{Binding EquipeSelectionnee}"
                              Width="220" Padding="10,8" FontSize="13" Background="White" BorderBrush="White" BorderThickness="2" Margin="0,0,20,0"/>
                    
                    <!-- Membre -->
                    <TextBlock Text="👤 Membres:" FontSize="13" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="White" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding Devs}" DisplayMemberPath="Nom"
                              SelectedItem="{Binding DevSelectionne}"
                              Width="200" Padding="10,8" FontSize="13" Background="White" BorderBrush="White" BorderThickness="2"/>
                </WrapPanel>
            </WrapPanel>
        </Border>

        <!-- Navigation et mode d'affichage -->
        <Border Grid.Row="1" CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FFFFFF" Offset="0"/>
                    <GradientStop Color="#FAFAFA" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
            </Border.Effect>
            <WrapPanel Orientation="Horizontal">
                <!-- Mode d'affichage -->
                <WrapPanel Orientation="Horizontal" Margin="0,0,20,10">
                    <Button Content="📅 Mois" Command="{Binding ChangerModeCommand}" CommandParameter="mois"
                            Padding="15,8" Margin="0,0,5,0" FontSize="12" FontWeight="SemiBold" Cursor="Hand"
                            BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#E8E8E8"/>
                                <Setter Property="Foreground" Value="#666"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EstModeMois}" Value="True">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#00915A" Offset="0"/>
                                                    <GradientStop Color="#00A566" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Foreground" Value="White"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D0D0D0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="📋 Liste" Command="{Binding ChangerModeCommand}" CommandParameter="liste"
                            Padding="15,8" Margin="0,0,5,0" FontSize="12" FontWeight="SemiBold" Cursor="Hand"
                            BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#E8E8E8"/>
                                <Setter Property="Foreground" Value="#666"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EstModeListe}" Value="True">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#00915A" Offset="0"/>
                                                    <GradientStop Color="#00A566" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Foreground" Value="White"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D0D0D0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="📊 Timeline Programme" Command="{Binding ChangerModeCommand}" CommandParameter="timeline_programme"
                            Padding="15,8" FontSize="12" FontWeight="SemiBold" Cursor="Hand"
                            BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#E8E8E8"/>
                                <Setter Property="Foreground" Value="#666"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EstModeTimelineProgramme}" Value="True">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#00915A" Offset="0"/>
                                                    <GradientStop Color="#00A566" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Foreground" Value="White"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D0D0D0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="📋 Timeline Projet" Command="{Binding ChangerModeCommand}" CommandParameter="timeline"
                            Padding="15,8" FontSize="12" FontWeight="SemiBold" Cursor="Hand"
                            BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#E8E8E8"/>
                                <Setter Property="Foreground" Value="#666"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EstModeTimeline}" Value="True">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#00915A" Offset="0"/>
                                                    <GradientStop Color="#00A566" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Foreground" Value="White"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D0D0D0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </WrapPanel>

                <!-- Navigation période -->
                <WrapPanel Orientation="Horizontal" Margin="0,0,20,10">
                    <WrapPanel.Style>
                        <Style TargetType="WrapPanel">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EstModeTimeline}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EstModeTimelineProgramme}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </WrapPanel.Style>
                    <Button Content="◀" Command="{Binding PeriodePrecedenteCommand}"
                            Padding="12,8" Margin="0,0,15,0" BorderThickness="0" 
                            FontSize="14" FontWeight="Bold" Cursor="Hand" Foreground="White">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#00915A" Offset="0"/>
                                            <GradientStop Color="#00A566" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" 
                                                    Padding="{TemplateBinding Padding}">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="#00915A"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <TextBlock Text="{Binding PeriodeAffichage}" FontSize="16" FontWeight="Bold" 
                               Foreground="#00915A" VerticalAlignment="Center" MinWidth="250" TextAlignment="Center"/>

                    <Button Content="▶" Command="{Binding PeriodeSuivanteCommand}"
                            Padding="12,8" Margin="15,0,15,0" BorderThickness="0" 
                            FontSize="14" FontWeight="Bold" Cursor="Hand" Foreground="White">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#00915A" Offset="0"/>
                                            <GradientStop Color="#00A566" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" 
                                                    Padding="{TemplateBinding Padding}">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="#00915A"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="🎯 Aujourd'hui" Command="{Binding AujourdhuiCommand}"
                            Padding="12,8" Background="#E8F5E9" Foreground="#00915A" BorderThickness="1" BorderBrush="#00915A"
                            FontSize="12" FontWeight="SemiBold" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#C8E6C9"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </WrapPanel>

                <!-- Sélecteur de programme (mode Timeline Programme uniquement) -->
                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <WrapPanel.Style>
                        <Style TargetType="WrapPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EstModeTimelineProgramme}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </WrapPanel.Style>
                    <TextBlock Text="🎯 Programme:" FontSize="13" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#00915A" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding Programmes}" DisplayMemberPath="Nom"
                              SelectedItem="{Binding ProgrammeSelectionne}"
                              Width="280" Padding="10,8" FontSize="13"/>
                </WrapPanel>

                <!-- Sélecteur de projet (mode Timeline Projet uniquement) -->
                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <WrapPanel.Style>
                        <Style TargetType="WrapPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EstModeTimeline}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </WrapPanel.Style>
                    <TextBlock Text="📋 Projet:" FontSize="13" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#00915A" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding Projets}" DisplayMemberPath="Nom"
                              SelectedItem="{Binding ProjetSelectionne}"
                              Width="250" Padding="10,8" FontSize="13"/>
                </WrapPanel>
            </WrapPanel>
        </Border>

        <!-- Contenu principal -->
        <Grid Grid.Row="2">
            <!-- Mode Timeline Programme -->
            <Border CornerRadius="10" Padding="20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#F5F3FF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding EstModeTimelineProgramme}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                </Border.Effect>
                
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- En-tête avec info programme -->
                        <Border Background="#E8F5E9" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0">
                                    <Grid>
                                        <TextBlock FontSize="22" FontWeight="Bold" HorizontalAlignment="Left">
                                            <Run Text="🎯 " Foreground="#00915A"/>
                                            <Run Text="{Binding ProgrammeSelectionne.Nom, FallbackValue='Sélectionnez un programme'}" Foreground="#00915A"/>
                                        </TextBlock>
                                        
                                        <!-- Badge Statut Global -->
                                        <Border HorizontalAlignment="Right" VerticalAlignment="Center" 
                                                Background="{Binding CouleurStatutGlobal}" 
                                                CornerRadius="12" Padding="12,6">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding AfficherStatutGlobal}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding IconeStatutGlobal}" FontSize="14" Foreground="White" Margin="0,0,6,0"/>
                                                <TextBlock Text="{Binding StatutGlobalProgramme}" FontSize="13" FontWeight="Bold" Foreground="White"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                    <TextBlock Text="{Binding ProgrammeSelectionne.Description}" FontSize="13" 
                                               Foreground="#666" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                                
                                <!-- Dates du programme -->
                                <Grid Grid.Row="1" Margin="0,15,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" Background="White" CornerRadius="6" Padding="12,6" Margin="0,0,15,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🚀 Début: " FontSize="12" FontWeight="SemiBold" Foreground="#00915A"/>
                                            <TextBlock Text="{Binding DateDebutProgramme}" FontSize="12" Foreground="#666"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Grid.Column="2" Background="White" CornerRadius="6" Padding="12,6">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🎯 Objectif: " FontSize="12" FontWeight="SemiBold" Foreground="#FF9800"/>
                                            <TextBlock Text="{Binding DateFinCibleProgramme}" FontSize="12" Foreground="#666"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                                
                                <!-- Indicateurs RAG -->
                                <WrapPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0">
                                    <Border Background="#4CAF50" CornerRadius="20" Padding="12,6" Margin="0,0,10,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding AfficherGreen}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🟢 " FontSize="13"/>
                                            <TextBlock Text="{Binding NbProjetsGreen}" FontSize="13" FontWeight="Bold" Foreground="White" Margin="0,0,3,0"/>
                                            <TextBlock Text=" Green" FontSize="13" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Background="#FF9800" CornerRadius="20" Padding="12,6" Margin="0,0,10,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding AfficherAmber}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🟠 " FontSize="13"/>
                                            <TextBlock Text="{Binding NbProjetsAmber}" FontSize="13" FontWeight="Bold" Foreground="White" Margin="0,0,3,0"/>
                                            <TextBlock Text=" Amber" FontSize="13" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Background="#F44336" CornerRadius="20" Padding="12,6">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding AfficherRed}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🔴 " FontSize="13"/>
                                            <TextBlock Text="{Binding NbProjetsRed}" FontSize="13" FontWeight="Bold" Foreground="White" Margin="0,0,3,0"/>
                                            <TextBlock Text=" Red" FontSize="13" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                </WrapPanel>
                            </Grid>
                        </Border>

                        <!-- Statistiques du programme -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Projets -->
                            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="18" Margin="0,0,8,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.15" Color="#00915A"/>
                                </Border.Effect>
                                <StackPanel>
                                    <TextBlock Text="📁" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                    <TextBlock Text="Projets" FontSize="11" Foreground="#999" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding ProjetsTimelineProgramme.Count}" FontSize="26" FontWeight="Bold" 
                                               Foreground="#00915A" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <!-- Tâches totales -->
                            <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="18" Margin="0,0,8,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.15" Color="#FF9800"/>
                                </Border.Effect>
                                <StackPanel>
                                    <TextBlock Text="📋" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                    <TextBlock Text="Tâches totales" FontSize="11" Foreground="#999" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding NbTotalTachesProgramme}" FontSize="26" FontWeight="Bold" 
                                               Foreground="#FF9800" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <!-- Progression -->
                            <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="18" Margin="0,0,8,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.15" Color="#4CAF50"/>
                                </Border.Effect>
                                <StackPanel>
                                    <TextBlock Text="✓" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                    <TextBlock Text="Progression" FontSize="11" Foreground="#999" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding ProgressionGlobaleProgrammeTexte}" FontSize="26" FontWeight="Bold" 
                                               Foreground="#4CAF50" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <!-- Budget estimé -->
                            <Border Grid.Column="3" Background="White" CornerRadius="10" Padding="18" Margin="0,0,8,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.15" Color="#2196F3"/>
                                </Border.Effect>
                                <StackPanel>
                                    <TextBlock Text="📊" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                    <TextBlock Text="Budget estimé" FontSize="11" Foreground="#999" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding BudgetTempsEstimeJours}" FontSize="26" FontWeight="Bold" 
                                               Foreground="#2196F3" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <!-- Temps réel -->
                            <Border Grid.Column="4" Background="White" CornerRadius="10" Padding="18">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.15" Color="#9C27B0"/>
                                </Border.Effect>
                                <StackPanel>
                                    <TextBlock Text="⏱" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                    <TextBlock Text="Temps réel" FontSize="11" Foreground="#999" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding TempsReelJours}" FontSize="26" FontWeight="Bold" 
                                               Foreground="#9C27B0" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- Barre de progression globale -->
                        <Border Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.15" Color="#D0D0D0"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <TextBlock Text="📈 PROGRESSION GLOBALE DU PROGRAMME" FontSize="13" FontWeight="SemiBold" 
                                               Foreground="#00915A" HorizontalAlignment="Left"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <!-- Bouton Analyse IA Programme -->
                                        <Button Padding="12,6" Margin="0,0,15,0"
                                                Background="#667eea" Foreground="White" FontSize="12" FontWeight="SemiBold"
                                                BorderThickness="0" Cursor="Hand" Click="BtnAnalyserProgrammeIA_Click"
                                                ToolTip="Analyse IA : État global, risques, recommandations">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" 
                                                                        CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <TextBlock Text="🤖" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                                        <TextBlock Text="Analyser avec IA" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                                    </StackPanel>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#5568d3"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        
                                        <TextBlock Text="{Binding NbTachesTermineesProgramme}" FontSize="13" FontWeight="Bold" Foreground="#4CAF50"/>
                                        <TextBlock Text=" / " FontSize="13" Foreground="#999" Margin="3,0"/>
                                        <TextBlock Text="{Binding NbTotalTachesProgramme}" FontSize="13" Foreground="#999"/>
                                        <TextBlock Text=" tâches terminées" FontSize="13" Foreground="#999" Margin="3,0,0,0"/>
                                    </StackPanel>
                                </Grid>
                                <Border Background="#F0F0F0" CornerRadius="12" Height="36" MinWidth="1000">
                                    <Grid>
                                        <Border Background="#4CAF50" CornerRadius="12" HorizontalAlignment="Left" Height="36"
                                                Width="{Binding LargeurProgressionGlobale}"/>
                                        <TextBlock Text="{Binding ProgressionGlobaleProgrammeTexte}" FontSize="14" FontWeight="Bold" 
                                                   Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- Section Équipes impliquées -->
                        <Border Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20"
                                Visibility="{Binding AfficherEquipesProgramme, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.15" Color="#D0D0D0"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid Margin="0,0,0,15">
                                    <TextBlock Text="👥 ÉQUIPES IMPLIQUÉES DANS LE PROGRAMME" FontSize="13" FontWeight="SemiBold" 
                                               Foreground="#00915A" HorizontalAlignment="Left"/>
                                    <TextBlock HorizontalAlignment="Right">
                                        <Run Text="{Binding NbEquipesProgramme, Mode=OneWay}" FontSize="13" FontWeight="Bold" Foreground="#00915A"/>
                                        <Run Text=" équipe(s)" FontSize="13" Foreground="#999"/>
                                    </TextBlock>
                                </Grid>
                                
                                <ItemsControl ItemsSource="{Binding EquipesProgramme}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#F8F9FA" CornerRadius="8" Padding="15,12" Margin="0,0,10,10"
                                                    BorderBrush="#00915A" BorderThickness="1" Cursor="Hand"
                                                    MinWidth="200" Tag="{Binding}" MouseDown="Equipe_Click">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                                <Setter Property="BorderBrush" Value="#00A566"/>
                                                                <Setter Property="BorderThickness" Value="2"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Nom}" FontSize="13" FontWeight="SemiBold" 
                                                               Foreground="#333" TextWrapping="Wrap"/>
                                                    <TextBlock Text="{Binding Code}" FontSize="11" Foreground="#666" 
                                                               Margin="0,3,0,0" Opacity="0.8"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                        <TextBlock Text="📊" FontSize="11" Margin="0,0,4,0"/>
                                                        <TextBlock Text="Voir dans Dashboard" FontSize="11" 
                                                                   Foreground="#00915A" FontWeight="Medium"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <!-- Timeline des projets style Gantt -->
                        <Border Background="White" CornerRadius="10" Padding="0" BorderBrush="#E0E0E0" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- En-tête des mois -->
                                <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="0,0,0,2" Padding="200,15,20,15">
                                    <ItemsControl ItemsSource="{Binding MoisTimeline}" AlternationCount="999">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <Canvas Height="40"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="100" Height="40" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
                                                    <Border.RenderTransform>
                                                        <TranslateTransform>
                                                            <TranslateTransform.X>
                                                                <Binding Path="(ItemsControl.AlternationIndex)" 
                                                                         RelativeSource="{RelativeSource TemplatedParent}"
                                                                         Converter="{StaticResource IndexToPositionConverter}"/>
                                                            </TranslateTransform.X>
                                                        </TranslateTransform>
                                                    </Border.RenderTransform>
                                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding NomMois}" FontSize="13" FontWeight="Bold" 
                                                                   Foreground="#00915A" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="{Binding Annee}" FontSize="11" 
                                                                   Foreground="#999" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>

                                <!-- Lignes des projets -->
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding ProjetsTimelineProgramme}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="15,20" 
                                                        Background="Transparent" Cursor="Hand">
                                                    <Border.InputBindings>
                                                        <MouseBinding MouseAction="LeftClick" 
                                                                      Command="{Binding DataContext.OuvrirTimelineProjetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                      CommandParameter="{Binding Projet}"/>
                                                    </Border.InputBindings>
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#F5F5F5"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="180"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>

                                                        <!-- Nom du projet -->
                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="{Binding Nom}" FontSize="13" FontWeight="SemiBold" 
                                                                       Foreground="#333" TextTrimming="CharacterEllipsis"/>
                                                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                                                <TextBlock Text="{Binding NbTachesTerminees}" FontSize="11" FontWeight="Bold" Foreground="#667EEA"/>
                                                                <TextBlock Text="/" FontSize="11" Foreground="#999" Margin="2,0"/>
                                                                <TextBlock Text="{Binding NbTaches}" FontSize="11" Foreground="#999"/>
                                                                <TextBlock Text=" tâches" FontSize="11" Foreground="#999" Margin="3,0,0,0"/>
                                                            </StackPanel>
                                                        </StackPanel>

                                                        <!-- Barre de progression du projet -->
                                                        <Canvas Grid.Column="1" Height="40">
                                                            <!-- Barre du projet -->
                                                            <Border Canvas.Left="{Binding PositionX}" 
                                                                    Width="{Binding LargeurBarre}" 
                                                                    Height="30" 
                                                                    CornerRadius="6" 
                                                                    Background="{Binding CouleurBarre}" 
                                                                    Opacity="0.9">
                                                                <Grid>
                                                                    <!-- Progression interne -->
                                                                    <Border Background="White" Opacity="0.3" 
                                                                            HorizontalAlignment="Left"
                                                                            Width="{Binding LargeurProgression}"
                                                                            CornerRadius="6"/>
                                                                    <TextBlock Text="{Binding ProgressionPourcentage, StringFormat='{}{0:F0}%'}" 
                                                                               FontSize="11" FontWeight="Bold" Foreground="White"
                                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Grid>
                                                            </Border>
                                                        </Canvas>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Mode Timeline Projet -->
            <Border CornerRadius="10" Padding="20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding EstModeTimeline}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                </Border.Effect>
                
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- En-tête avec info projet -->
                        <Border Background="#F5F5F5" CornerRadius="6" Padding="15" Margin="0,0,0,20">
                            <WrapPanel Orientation="Horizontal">
                                <StackPanel Margin="0,0,20,10">
                                    <TextBlock FontSize="18" FontWeight="Bold" Foreground="#00915A">
                                        <Run Text="📊 "/>
                                        <Run Text="{Binding ProjetSelectionne.Nom, FallbackValue='Sélectionnez un projet'}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding ProjetSelectionne.Description}" FontSize="12" 
                                               Foreground="#666" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                                
                                <StackPanel Margin="0,0,15,10">
                                    <TextBlock Text="{Binding TachesProjetTimeline.Count, StringFormat='{}{0} tâches'}" 
                                               FontSize="14" FontWeight="SemiBold" Foreground="#00915A"/>
                                    <TextBlock Text="Dates du projet à venir" FontSize="11" Foreground="#666" 
                                               Margin="0,3,0,0" FontStyle="Italic"/>
                                </StackPanel>
                                
                                <!-- Bouton Analyse IA -->
                                <Button Padding="15,10" Margin="0,0,0,10"
                                        Background="#667eea" Foreground="White" FontSize="13" FontWeight="SemiBold"
                                        BorderThickness="0" Cursor="Hand" Click="BtnAnalyserProjetIA_Click"
                                        ToolTip="Analyse IA : Deadlines, retards, recommandations"
                                        VerticalAlignment="Center">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="🤖" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                                <TextBlock Text="Analyser avec IA" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#5568d3"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Background" Value="#CCCCCC"/>
                                                    <Setter Property="Cursor" Value="Arrow"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </WrapPanel>
                        </Border>
                        
                        <!-- Message d'extension projet détectée -->
                        <Border Background="#FFF3CD" BorderBrush="#FFC107" BorderThickness="2" 
                                CornerRadius="6" Padding="15" Margin="0,0,0,20">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ExtensionProjetDetectee}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="🔔 Extension de projet détectée" FontSize="14" FontWeight="Bold" 
                                               Foreground="#856404" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding MessageExtension}" FontSize="12" Foreground="#856404" 
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                                
                                <Button Grid.Column="1" Content="✓ Valider l'extension" 
                                        Command="{Binding ValiderExtensionProjetCommand}"
                                        Padding="12,8" Background="#28A745" Foreground="White" 
                                        FontSize="12" FontWeight="SemiBold" BorderThickness="0" 
                                        Cursor="Hand" VerticalAlignment="Center" Margin="15,0,0,0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#218838"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- Timeline des tâches -->
                        <Grid Margin="0,20,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Axe des mois -->
                            <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                                          x:Name="TimelineHeaderScroll">
                                <Grid>
                                    <!-- Mois -->
                                    <ItemsControl ItemsSource="{Binding MoisTimeline}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="120" BorderBrush="#E0E0E0" BorderThickness="1,0,0,1" 
                                                        Background="#F9F9F9" Padding="8">
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding NomMois}" FontSize="12" FontWeight="SemiBold" 
                                                                   Foreground="#00915A" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="{Binding Annee}" FontSize="10" Foreground="#999" 
                                                                   HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    
                                    <!-- Badge DÉBUT PROJET -->
                                    <Border Background="#4CAF50" CornerRadius="4" Padding="6,3" Height="22" 
                                            VerticalAlignment="Bottom" HorizontalAlignment="Left">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding AfficherMarqueursProjet}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.RenderTransform>
                                            <TranslateTransform X="{Binding PositionDebutProjet}"/>
                                        </Border.RenderTransform>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🚀" FontSize="8" Margin="0,0,2,0"/>
                                            <TextBlock Text="DÉBUT" FontSize="8" FontWeight="Bold" 
                                                       Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- Badge OBJECTIF LIVRAISON -->
                                    <Border Background="#FF9800" CornerRadius="4" Padding="6,3" Height="22" 
                                            VerticalAlignment="Bottom" HorizontalAlignment="Left">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding AfficherMarqueursProjet}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.RenderTransform>
                                            <TranslateTransform X="{Binding PositionFinProjet}"/>
                                        </Border.RenderTransform>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🎯" FontSize="8" Margin="0,0,2,0"/>
                                            <TextBlock Text="OBJECTIF" FontSize="8" FontWeight="Bold" 
                                                       Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </ScrollViewer>
                            
                            <!-- Zone des tâches avec timeline -->
                            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                                          ScrollChanged="TimelineScroll_ScrollChanged">
                                <Grid>
                                    <!-- Grille de fond avec séparateurs de mois -->
                                    <ItemsControl ItemsSource="{Binding MoisTimeline}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="120" BorderBrush="#E0E0E0" BorderThickness="1,0,0,0" 
                                                        Height="{Binding DataContext.HauteurTimeline, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    
                                    <!-- Tâches positionnées sur la timeline -->
                                    <ItemsControl ItemsSource="{Binding TachesProjetTimeline}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <Canvas/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="White" BorderBrush="#00915A" BorderThickness="2" 
                                                        CornerRadius="6" Padding="10" Margin="5"
                                                        Width="{Binding LargeurBarre}"
                                                        Height="80"
                                                        Cursor="Hand"
                                                        Tag="{Binding}"
                                                        MouseDown="TacheTimeline_MouseDown">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="White"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Statut}" Value="Termine">
                                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Statut}" Value="EnCours">
                                                                    <Setter Property="Background" Value="#E3F2FD"/>
                                                                    <Setter Property="BorderBrush" Value="#2196F3"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding EstEnRetard}" Value="True">
                                                                    <Setter Property="Background" Value="#FFEBEE"/>
                                                                    <Setter Property="BorderBrush" Value="#D32F2F"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="*"/>
                                                        </Grid.RowDefinitions>
                                                        
                                                        <TextBlock Grid.Row="0" Text="{Binding Titre}" FontSize="11" 
                                                                   FontWeight="SemiBold" Foreground="#333" 
                                                                   TextTrimming="CharacterEllipsis" ToolTip="{Binding Titre}"/>
                                                        
                                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,3,0,0">
                                                            <Border CornerRadius="3" Padding="4,2" Margin="0,0,5,0">
                                                                <Border.Style>
                                                                    <Style TargetType="Border">
                                                                        <Setter Property="Background" Value="#9E9E9E"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Statut}" Value="EnCours">
                                                                                <Setter Property="Background" Value="#2196F3"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Statut}" Value="Termine">
                                                                                <Setter Property="Background" Value="#4CAF50"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>
                                                                <TextBlock Text="{Binding Statut}" FontSize="8" 
                                                                           Foreground="White" FontWeight="SemiBold"/>
                                                            </Border>
                                                            
                                                            <TextBlock Text="{Binding ProgressionPourcentage, StringFormat='{}{0:F0}%'}" 
                                                                       FontSize="10" FontWeight="Bold" Foreground="#00915A"/>
                                                        </StackPanel>
                                                        
                                                        <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
                                                            <Border CornerRadius="3" Padding="4,2" Background="#F5F5F5">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="👤 " FontSize="8"/>
                                                                    <TextBlock Text="{Binding DevAssigneNom, FallbackValue='?'}" 
                                                                               FontSize="8" FontWeight="SemiBold"/>
                                                                    <TextBlock Text=" • " FontSize="8" Margin="3,0"/>
                                                                    <TextBlock Text="{Binding TempsReelJours, StringFormat='{}{0:F1}j'}" 
                                                                               FontSize="8" FontWeight="SemiBold"/>
                                                                    <TextBlock Text="/" FontSize="8" Margin="2,0"/>
                                                                    <TextBlock Text="{Binding ChiffrageJours, StringFormat='{}{0:F1}j', TargetNullValue='?'}" 
                                                                               FontSize="8"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemContainerStyle>
                                            <Style>
                                                <Setter Property="Canvas.Left" Value="{Binding PositionX}"/>
                                                <Setter Property="Canvas.Top" Value="{Binding PositionY}"/>
                                            </Style>
                                        </ItemsControl.ItemContainerStyle>
                                    </ItemsControl>

                                    <!-- Marqueur de début du projet -->
                                    <Line X1="{Binding PositionDebutProjet}" Y1="0" 
                                          X2="{Binding PositionDebutProjet}" Y2="{Binding HauteurTimeline}"
                                          Stroke="#4CAF50" StrokeThickness="2" StrokeDashArray="6,3">
                                        <Line.Style>
                                            <Style TargetType="Line">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding AfficherMarqueursProjet}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Line.Style>
                                    </Line>

                                    <!-- Marqueur de fin prévue du projet -->
                                    <Line X1="{Binding PositionFinProjet}" Y1="0" 
                                          X2="{Binding PositionFinProjet}" Y2="{Binding HauteurTimeline}"
                                          Stroke="#FF9800" StrokeThickness="2" StrokeDashArray="6,3">
                                        <Line.Style>
                                            <Style TargetType="Line">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding AfficherMarqueursProjet}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Line.Style>
                                    </Line>
                                </Grid>
                            </ScrollViewer>
                        </Grid>
                        
                        <!-- Message si aucune tâche -->
                        <Border Background="#F5F5F5" CornerRadius="6" Padding="30" Margin="0,20,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TachesProjetTimeline.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center" Opacity="0.3"/>
                                <TextBlock Text="Aucune tâche dans ce projet" FontSize="14" Foreground="#999" 
                                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                <TextBlock Text="Sélectionnez un projet avec des tâches pour voir la timeline" 
                                           FontSize="12" Foreground="#999" HorizontalAlignment="Center" 
                                           Margin="0,5,0,0" FontStyle="Italic"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Modes Mois et Liste (existants) -->
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding EstModeTimeline}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding EstModeTimelineProgramme}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Calendrier -->
            <Border Grid.Column="0" CornerRadius="10" Padding="15" Margin="0,0,10,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FAFAFA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- En-têtes jours semaine (seulement en mode calendrier) -->
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EstModeListe}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="LUN" HorizontalAlignment="Center" FontWeight="Bold" 
                                   Foreground="#00915A" FontSize="12"/>
                        <TextBlock Grid.Column="1" Text="MAR" HorizontalAlignment="Center" FontWeight="Bold" 
                                   Foreground="#00915A" FontSize="12"/>
                        <TextBlock Grid.Column="2" Text="MER" HorizontalAlignment="Center" FontWeight="Bold" 
                                   Foreground="#00915A" FontSize="12"/>
                        <TextBlock Grid.Column="3" Text="JEU" HorizontalAlignment="Center" FontWeight="Bold" 
                                   Foreground="#00915A" FontSize="12"/>
                        <TextBlock Grid.Column="4" Text="VEN" HorizontalAlignment="Center" FontWeight="Bold" 
                                   Foreground="#00915A" FontSize="12"/>
                        <TextBlock Grid.Column="5" Text="SAM" HorizontalAlignment="Center" FontWeight="Bold" 
                                   Foreground="#757575" FontSize="12"/>
                        <TextBlock Grid.Column="6" Text="DIM" HorizontalAlignment="Center" FontWeight="Bold" 
                                   Foreground="#757575" FontSize="12"/>
                    </Grid>

                    <!-- Grille calendrier (mode mois) -->
                    <ItemsControl Grid.Row="1" ItemsSource="{Binding JoursCalendrier}">
                        <ItemsControl.Style>
                            <Style TargetType="ItemsControl">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EstModeListe}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ItemsControl.Style>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="7"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.JourSelectionnCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Margin="2" BorderThickness="0" Cursor="Hand" Padding="8">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="6" 
                                                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                                                Padding="{TemplateBinding Padding}">
                                                            <Grid>
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                </Grid.RowDefinitions>

                                                                <TextBlock Grid.Row="0" Text="{Binding Jour}" FontSize="16" FontWeight="Bold" 
                                                                           HorizontalAlignment="Center" Foreground="#333"/>

                                                                <!-- Icône jour férié personnalisée -->
                                                                <Image Grid.Row="1" Source="{Binding IconeJourFerie}" Width="24" Height="24" 
                                                                       Margin="0,2,0,0" HorizontalAlignment="Center" Stretch="Uniform"
                                                                       ToolTip="{Binding NomJourFerie}"
                                                                       Visibility="{Binding EstJourFerie, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                                                <Border Grid.Row="3" CornerRadius="3" Padding="6,3" Margin="0,4,0,0">
                                                                    <Border.Style>
                                                                        <Style TargetType="Border">
                                                                            <Setter Property="Background" Value="#00915A"/>
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding EstConges}" Value="True">
                                                                                    <Setter Property="Background" Value="#2196F3"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding EstNonTravaille}" Value="True">
                                                                                    <Setter Property="Background" Value="#757575"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding TotalJoursAffiche}" Value="">
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Border.Style>
                                                                    <TextBlock Text="{Binding TotalJoursAffiche}" FontSize="11" FontWeight="Bold" 
                                                                               Foreground="White" HorizontalAlignment="Center"/>
                                                                </Border>
                                                            </Grid>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="BorderBrush" Value="#00915A"/>
                                                                <Setter Property="BorderThickness" Value="2"/>
                                                            </Trigger>
                                                            <DataTrigger Binding="{Binding EstAujourdhui}" Value="True">
                                                                <Setter Property="Background" Value="#C8E6C9"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding EstWeekend}" Value="True">
                                                                <Setter Property="Background" Value="#F5F5F5"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding EstJourFerie}" Value="True">
                                                                <Setter Property="Background" Value="#FFF3E0"/>
                                                            </DataTrigger>
                                                            <!-- Distinction futur vs passé pour CRA prévisionnels -->
                                                            <DataTrigger Binding="{Binding EstDansFutur}" Value="True">
                                                                <Setter Property="Background" Value="#FFE082"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding EstDansPasse}" Value="True">
                                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                            </DataTrigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Vue liste (mode liste) -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ScrollViewer.Style>
                            <Style TargetType="ScrollViewer">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EstModeListe}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ScrollViewer.Style>
                        <ItemsControl ItemsSource="{Binding MoisAnnee}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.MoisSelectionnCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                                        CornerRadius="8" Width="140" Height="180" Margin="8" Padding="12">
                                                    <Border.Effect>
                                                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.1" Color="#00915A"/>
                                                    </Border.Effect>
                                                    <StackPanel>
                                                        <!-- Nom du mois -->
                                                        <TextBlock Text="{Binding NomMois}" FontSize="16" FontWeight="Bold" 
                                                                   HorizontalAlignment="Center" Foreground="#00915A" Margin="0,0,0,15"/>
                                                        
                                                        <!-- Total jours travaillés -->
                                                        <Border Background="#E8F5E9" CornerRadius="6" Padding="12,8" Margin="0,0,0,10">
                                                            <StackPanel>
                                                                <TextBlock Text="TOTAL" FontSize="10" Foreground="#666" 
                                                                           HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                                                <TextBlock Text="{Binding TotalAffiche}" FontSize="24" FontWeight="Bold" 
                                                                           Foreground="#00915A" HorizontalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>
                                                        
                                                        <!-- Nombre de jours saisis -->
                                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                            <TextBlock Text="📅 " FontSize="12"/>
                                                            <TextBlock FontSize="12" Foreground="#666">
                                                                <Run Text="{Binding NombreJoursSaisis, Mode=OneWay}"/>
                                                                <Run Text=" jour(s)"/>
                                                            </TextBlock>
                                                        </StackPanel>
                                                        
                                                        <!-- Heures totales -->
                                                        <TextBlock FontSize="11" Foreground="#999" HorizontalAlignment="Center" Margin="0,6,0,0">
                                                            <Run Text="⏱️ "/>
                                                            <Run Text="{Binding TotalHeures, StringFormat='{}{0:F1}h', Mode=OneWay}"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="BorderBrush" Value="#00915A"/>
                                                                    <Setter Property="BorderThickness" Value="2"/>
                                                                    <Setter Property="Effect">
                                                                        <Setter.Value>
                                                                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.2" Color="#00915A"/>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Panneau latéral -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Statistiques par dev -->
                    <Border CornerRadius="10" Padding="15" Margin="0,0,0,10">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFFFF" Offset="0"/>
                                <GradientStop Color="#FAFAFA" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.1" Color="#00915A"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="👥 STATISTIQUES" FontSize="14" FontWeight="Bold" 
                                       Foreground="#00915A" Margin="0,0,0,12"/>

                            <ItemsControl ItemsSource="{Binding StatsDev}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" 
                                                CornerRadius="6" Padding="12" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <TextBlock Grid.Row="0" Text="{Binding Nom}" FontSize="13" FontWeight="SemiBold" 
                                                           Foreground="#333" Margin="0,0,0,6"/>

                                                <Grid Grid.Row="1">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock FontSize="11" Foreground="#666">
                                                            <Run Text="📊"/>
                                                            <Run Text="{Binding TotalJoursPeriode, StringFormat='{}{0:F1}j', Mode=OneWay}"/>
                                                            <Run Text="(" Foreground="#999"/>
                                                            <Run Text="{Binding TotalHeuresPeriode, StringFormat='{}{0:F1}h', Mode=OneWay}" Foreground="#999"/>
                                                            <Run Text=")" Foreground="#999"/>
                                                        </TextBlock>
                                                        <TextBlock FontSize="11" Foreground="#666" Margin="0,2,0,0">
                                                            <Run Text="📅"/>
                                                            <Run Text="{Binding NombreJoursSaisis, Mode=OneWay}"/>
                                                            <Run Text="jour(s) saisi(s)"/>
                                                        </TextBlock>
                                                        <TextBlock FontSize="11" Margin="0,4,0,0">
                                                            <Run Text="📋" Foreground="#00915A"/>
                                                            <Run Text="Travail:" Foreground="#00915A" FontWeight="SemiBold"/>
                                                            <Run Text="{Binding JoursTravail, StringFormat='{}{0:F1}j', Mode=OneWay}" Foreground="#00915A"/>
                                                        </TextBlock>
                                                        <TextBlock FontSize="11" Margin="0,2,0,0">
                                                            <Run Text="🏖️" Foreground="#2196F3"/>
                                                            <Run Text="Congés:" Foreground="#2196F3" FontWeight="SemiBold"/>
                                                            <Run Text="{Binding JoursConges, StringFormat='{}{0:F1}j', Mode=OneWay}" Foreground="#2196F3"/>
                                                        </TextBlock>
                                                        <TextBlock FontSize="11" Margin="0,2,0,0">
                                                            <Run Text="🚫" Foreground="#757575"/>
                                                            <Run Text="Non travaillé:" Foreground="#757575" FontWeight="SemiBold"/>
                                                            <Run Text="{Binding JoursNonTravaille, StringFormat='{}{0:F1}j', Mode=OneWay}" Foreground="#757575"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Détail jour sélectionné -->
                    <Border CornerRadius="10" Padding="15">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFFFF" Offset="0"/>
                                <GradientStop Color="#FAFAFA" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="📅 DÉTAIL DU JOUR" FontSize="14" FontWeight="Bold" 
                                       Foreground="#00915A" Margin="0,0,0,8"/>

                            <TextBlock Text="{Binding JourSelectionne.Date, StringFormat='dddd d MMMM yyyy'}" 
                                       FontSize="12" FontWeight="SemiBold" Margin="0,0,0,10"/>

                            <ItemsControl ItemsSource="{Binding CRAsJourSelectionne}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#E0E0E0" BorderThickness="1" 
                                                CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#F5F5F5"/>
                                                    <Style.Triggers>
                                                        <!-- Congés : fond bleu clair -->
                                                        <DataTrigger Binding="{Binding EstConges}" Value="True">
                                                            <Setter Property="Background" Value="#E3F2FD"/>
                                                            <Setter Property="BorderBrush" Value="#2196F3"/>
                                                        </DataTrigger>
                                                        <!-- Non travaillé : fond gris -->
                                                        <DataTrigger Binding="{Binding EstNonTravaille}" Value="True">
                                                            <Setter Property="Background" Value="#EEEEEE"/>
                                                            <Setter Property="BorderBrush" Value="#9E9E9E"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <!-- Nom de la tâche avec icône -->
                                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                                    <!-- Icône selon le type -->
                                                    <TextBlock FontSize="14" Margin="0,0,5,0">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="📋"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding EstConges}" Value="True">
                                                                        <Setter Property="Text" Value="🏖️"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding EstNonTravaille}" Value="True">
                                                                        <Setter Property="Text" Value="🚫"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    
                                                    <TextBlock Text="{Binding TacheNom}" FontWeight="SemiBold" 
                                                               FontSize="12" TextWrapping="Wrap"/>
                                                </StackPanel>
                                                
                                                <TextBlock Grid.Row="1" Text="{Binding Commentaire}" FontSize="10" 
                                                           Foreground="#666" Margin="0,0,0,5" TextWrapping="Wrap"
                                                           Visibility="{Binding Commentaire, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                
                                                <!-- Badge avec couleur selon le type -->
                                                <Border Grid.Row="2" CornerRadius="3" Padding="4,2" HorizontalAlignment="Left">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="#00915A"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding EstConges}" Value="True">
                                                                    <Setter Property="Background" Value="#2196F3"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding EstNonTravaille}" Value="True">
                                                                    <Setter Property="Background" Value="#757575"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Jours, StringFormat='{}{0:F1}j'}" 
                                                               FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock Text="Aucun CRA saisi ce jour" FontSize="12" Foreground="#999" 
                                       FontStyle="Italic" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
