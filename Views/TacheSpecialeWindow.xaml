<Window x:Class="BacklogManager.Views.TacheSpecialeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BacklogManager.Domain"
        Title="Créer une tâche spéciale" Height="500" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="⭐ Créer une tâche spéciale" FontSize="22" FontWeight="Bold" Foreground="#1A1919"/>
            <TextBlock Text="Ces tâches ne sont pas visibles dans le Kanban" FontSize="12" Foreground="#666" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Type de tâche -->
                <TextBlock Text="Type de tâche *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox x:Name="TypeComboBox" Height="35" FontSize="13" Margin="0,0,0,15"
                          SelectionChanged="TypeComboBox_SelectionChanged">
                    <ComboBoxItem Content="Congés" Tag="Conges"/>
                    <ComboBoxItem Content="Non travaillé" Tag="NonTravaille"/>
                    <ComboBoxItem Content="Support développeur" Tag="Support"/>
                    <ComboBoxItem Content="Run - Support production" Tag="Run"/>
                </ComboBox>

                <!-- Titre -->
                <TextBlock Text="Titre *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="TitreTextBox" Height="35" FontSize="13" Margin="0,0,0,15"
                         VerticalContentAlignment="Center" Padding="8"/>

                <!-- Description -->
                <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="DescriptionTextBox" Height="80" FontSize="13" Margin="0,0,0,15"
                         TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         Padding="8"/>

                <!-- Date début -->
                <TextBlock Text="Date début" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <DatePicker x:Name="DateDebutPicker" Height="35" FontSize="13" Margin="0,0,0,15"/>

                <!-- Date fin -->
                <TextBlock Text="Date fin" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <DatePicker x:Name="DateFinPicker" Height="35" FontSize="13" Margin="0,0,0,15"/>

                <!-- Chiffrage (pour le suivi CRA) -->
                <TextBlock Text="Estimation (jours)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="ChiffrageTextBox" Height="35" FontSize="13" Margin="0,0,0,5"
                         VerticalContentAlignment="Center" Padding="8" Text="1"/>
                <TextBlock Text="💡 Estimation pour le suivi du temps passé (1 jour = 8h)" 
                           FontSize="11" FontStyle="Italic" Foreground="#666" Margin="0,0,0,15"/>

                <!-- Info Support (visible seulement si Support) -->
                <Border x:Name="SupportInfoPanel" Visibility="Collapsed" Background="#FFF3E0" 
                        BorderBrush="#FFB800" BorderThickness="1" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="⚠️ Tâche de support" FontWeight="Bold" Foreground="#FF8F00" Margin="0,0,0,10"/>
                        
                        <TextBlock Text="Développeur aidé" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="DevSupporteComboBox" Height="35" FontSize="13" Margin="0,0,0,10"
                                  DisplayMemberPath="Nom" SelectedValuePath="Id"/>
                        
                        <TextBlock Text="Tâche supportée" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="TacheSupporteeComboBox" Height="35" FontSize="13"
                                  DisplayMemberPath="Titre" SelectedValuePath="Id"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Annuler" Width="100" Height="35" Margin="0,0,10,0"
                    Background="#E0E0E0" Foreground="#333" BorderThickness="0"
                    FontSize="13" FontWeight="SemiBold" Cursor="Hand"
                    Click="Annuler_Click"/>
            <Button Content="Créer" Width="100" Height="35"
                    Background="#00915A" Foreground="White" BorderThickness="0"
                    FontSize="13" FontWeight="SemiBold" Cursor="Hand"
                    Click="Creer_Click"/>
        </StackPanel>
    </Grid>
</Window>
