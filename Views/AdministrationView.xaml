<UserControl x:Class="BacklogManager.Views.AdministrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F0F2F5">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="35,25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="10" Padding="30,25" Margin="0,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#00915A" Offset="0"/>
                        <GradientStop Color="#00A566" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#00915A"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock FontSize="28" FontWeight="Bold" Foreground="White">
                        <Run Text="⚙️ "/>
                        <Run Text="ADMINISTRATION"/>
                    </TextBlock>
                    <TextBlock Text="Gestion des utilisateurs, rôles, projets et paramètres système" 
                               FontSize="14" Foreground="White" Opacity="0.9" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Tabs Navigation -->
            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Grid>
                                        <Border Name="Border" Background="White" BorderBrush="#E0E0E0" 
                                                BorderThickness="0,0,0,3" CornerRadius="0" 
                                                Padding="30,18" Margin="0,0,5,0">
                                            <ContentPresenter x:Name="ContentSite"
                                                              VerticalAlignment="Center"
                                                              HorizontalAlignment="Center"
                                                              ContentSource="Header"/>
                                        </Border>
                                        <Border Name="ActiveIndicator" Background="#00915A" Height="3" 
                                                VerticalAlignment="Bottom" Visibility="Collapsed"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#00915A"/>
                                            <Setter TargetName="ActiveIndicator" Property="Visibility" Value="Visible"/>
                                            <Setter Property="Foreground" Value="#00915A"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#E0E0E0"/>
                                            <Setter Property="Foreground" Value="#6D6D6D"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <!-- Tab Utilisateurs & Rôles -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👥" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Utilisateurs &amp; Rôles" FontSize="14"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border CornerRadius="10" Padding="30" Margin="0,10,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFFFF" Offset="0"/>
                                <GradientStop Color="#FAFAFA" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Sous-navigation avec style pill -->
                            <Border Grid.Row="0" Background="#F5F5F5" CornerRadius="25" Padding="5" Margin="0,0,0,25">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="BtnSousOngletUtilisateurs" 
                                            Background="#00915A" Foreground="White" 
                                            FontSize="13" FontWeight="SemiBold" Padding="22,10"
                                            Margin="0" BorderThickness="0" Cursor="Hand"
                                            Click="BtnSousOngletUtilisateurs_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="20" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="👥" Margin="0,0,6,0"/>
                                            <TextBlock Text="Utilisateurs"/>
                                        </StackPanel>
                                    </Button>
                                    <Button x:Name="BtnSousOngletRoles" 
                                            Background="Transparent" Foreground="#6D6D6D" 
                                            FontSize="13" FontWeight="Normal" Padding="22,10"
                                            Margin="5,0,0,0" BorderThickness="0" Cursor="Hand"
                                            Click="BtnSousOngletRoles_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="20" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🎭" Margin="0,0,6,0"/>
                                            <TextBlock Text="Rôles"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Border>
                            
                            <!-- Contenu dynamique -->
                            <Frame x:Name="FrameUtilisateursRoles" Grid.Row="1" NavigationUIVisibility="Hidden"/>
                        </Grid>
                    </Border>
                </TabItem>

                <!-- Tab Projets & Équipe -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Projets &amp; Équipe" FontSize="14"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border CornerRadius="10" Padding="30" Margin="0,10,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFFFF" Offset="0"/>
                                <GradientStop Color="#FAFAFA" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Sous-navigation avec style pill -->
                            <Border Grid.Row="0" Background="#F5F5F5" CornerRadius="25" Padding="5" Margin="0,0,0,25">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="BtnSousOngletProjets" 
                                            Background="#00915A" Foreground="White" 
                                            FontSize="13" FontWeight="SemiBold" Padding="22,10"
                                            Margin="0" BorderThickness="0" Cursor="Hand"
                                            Click="BtnSousOngletProjets_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="20" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📁" Margin="0,0,6,0"/>
                                            <TextBlock Text="Projets"/>
                                        </StackPanel>
                                    </Button>
                                    <Button x:Name="BtnSousOngletDevs" 
                                            Background="Transparent" Foreground="#6D6D6D" 
                                            FontSize="13" FontWeight="Normal" Padding="22,10"
                                            Margin="5,0,0,0" BorderThickness="0" Cursor="Hand"
                                            Click="BtnSousOngletDevs_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="20" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="👨‍💻" Margin="0,0,6,0"/>
                                            <TextBlock Text="Développeurs"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Border>
                            
                            <!-- Contenu dynamique -->
                            <Frame x:Name="FrameProjetsEquipe" Grid.Row="1" NavigationUIVisibility="Hidden"/>
                        </Grid>
                    </Border>
                </TabItem>

                <!-- Tab Historique Chat -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💬" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Historique Chat IA" FontSize="14"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border CornerRadius="10" Padding="30" Margin="0,10,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFFFF" Offset="0"/>
                                <GradientStop Color="#FAFAFA" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                        </Border.Effect>
                        <Grid>
                            <Button x:Name="BtnOuvrirHistoriqueChat" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Background="#00915A" Foreground="White" 
                                    FontSize="14" FontWeight="SemiBold" Padding="25,15"
                                    BorderThickness="0" Cursor="Hand"
                                    Click="BtnOuvrirHistoriqueChat_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#00A568"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💬" FontSize="18" Margin="0,0,10,0"/>
                                    <TextBlock Text="Ouvrir l'historique des conversations" FontSize="14"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>
                </TabItem>

                <!-- Tab Audit -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📋" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Journal d'audit" FontSize="14"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border CornerRadius="10" Padding="30" Margin="0,10,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFFFF" Offset="0"/>
                                <GradientStop Color="#FAFAFA" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.2" Color="#D0D0D0"/>
                        </Border.Effect>
                        <Frame x:Name="FrameAudit" NavigationUIVisibility="Hidden"/>
                    </Border>
                </TabItem>
            </TabControl>
        </Grid>
    </ScrollViewer>
</UserControl>
