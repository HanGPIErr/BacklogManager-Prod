<UserControl x:Class="BacklogManager.Views.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BacklogManager.Domain"
             xmlns:converters="clr-namespace:BacklogManager.Converters"
             Background="#F5F5F5">
    <UserControl.Resources>
        <converters:TimelineBarMarginConverter x:Key="TimelineBarMarginConverter"/>
    </UserControl.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="6" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.15" Color="#00915A"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📅 TIMELINE" FontSize="22" FontWeight="Bold" Foreground="#1A1919"/>
                <TextBlock Text="BNP Paribas" FontSize="16" Foreground="#00915A" 
                           VerticalAlignment="Center" Margin="15,0,0,0" FontWeight="Medium"/>
            </StackPanel>
        </Border>

        <!-- Filters Bar -->
        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.15" Color="#00915A"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Ligne 1: Navigation temporelle -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="📅 Navigation:" FontWeight="Bold" FontSize="14" 
                               VerticalAlignment="Center" Margin="0,0,15,0"/>
                    
                    <TextBlock Text="Année:" FontSize="13" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox ItemsSource="{Binding Annees}"
                              SelectedItem="{Binding SelectedAnnee}"
                              Width="80" Padding="8,5" Margin="0,0,15,0" FontWeight="Bold"/>
                    
                    <TextBlock Text="Vue:" FontSize="13" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <ComboBox ItemsSource="{Binding Periodes}"
                              SelectedItem="{Binding SelectedPeriode}"
                              Width="150" Padding="8,5" Margin="0,0,15,0"/>

                    <TextBlock Text="{Binding ProjetsInfo}" FontSize="12" FontWeight="SemiBold"
                               Foreground="#00915A" VerticalAlignment="Center" Margin="15,0,0,0"/>
                </StackPanel>

                <!-- Ligne 2: Filtres de données -->
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBlock Text="🔍 Filtres:" FontWeight="Bold" FontSize="14" 
                               VerticalAlignment="Center" Margin="0,0,15,0"/>
                    
                    <TextBlock Text="Recherche:" FontSize="13" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Width="180" Padding="8,5" Margin="0,0,15,0"
                             VerticalContentAlignment="Center">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="#CCC"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="BorderBrush" Value="#00915A"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    
                    <TextBlock Text="Dev:" FontSize="13" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <ComboBox ItemsSource="{Binding Devs}" DisplayMemberPath="Nom"
                              SelectedValue="{Binding SelectedDevId}" SelectedValuePath="Id" 
                              Width="150" Padding="8,5" Margin="0,0,15,0"/>
                    
                    <TextBlock Text="Projet:" FontSize="13" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <ComboBox ItemsSource="{Binding Projets}" DisplayMemberPath="Nom"
                              SelectedValue="{Binding SelectedProjetId}" SelectedValuePath="Id" 
                              Width="150" Padding="8,5" Margin="0,0,15,0"/>
                
                <Button Content="🔄 Actualiser" Command="{Binding RefreshCommand}"
                        Padding="10,5" Margin="0,0,10,0" Background="#00915A" Foreground="White" 
                        BorderThickness="0" FontSize="12" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#007A4D"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="🗑️ Effacer filtres" Command="{Binding ClearFiltersCommand}"
                        Padding="10,5" Background="#E0E0E0" Foreground="#333" 
                        BorderThickness="0" FontSize="12" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#D0D0D0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
            </Grid>
        </Border>

        <!-- Timeline Items -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Légende des couleurs - Branding BNP Paribas -->
                <Border Background="White" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.12" Color="#00915A"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Titre de la légende -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="🎨" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="LÉGENDE DES COULEURS" FontSize="14" FontWeight="Bold" 
                                     Foreground="#1A1919" VerticalAlignment="Center"/>
                            <Rectangle Width="3" Height="18" Fill="#00915A" Margin="10,0" RadiusX="2" RadiusY="2"/>
                            <TextBlock Text="BNP Paribas" FontSize="12" Foreground="#00915A" 
                                     FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Ligne 1: Statuts -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="📊 Statuts:" FontWeight="SemiBold" FontSize="12" 
                                     Foreground="#555" VerticalAlignment="Center" Width="90"/>
                            
                            <!-- À faire -->
                            <Border Background="#E3F2FD" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Fill="#2196F3" Margin="0,0,6,0"/>
                                    <TextBlock Text="À faire" FontSize="11" Foreground="#1565C0" FontWeight="Medium"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- En cours -->
                            <Border Background="#FFF3E0" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Fill="#FF9800" Margin="0,0,6,0"/>
                                    <TextBlock Text="En cours" FontSize="11" Foreground="#E65100" FontWeight="Medium"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- En test -->
                            <Border Background="#F3E5F5" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Fill="#9C27B0" Margin="0,0,6,0"/>
                                    <TextBlock Text="En test" FontSize="11" Foreground="#6A1B9A" FontWeight="Medium"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Terminé -->
                            <Border Background="#E8F5E9" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Fill="#00915A" Margin="0,0,6,0"/>
                                    <TextBlock Text="Terminé" FontSize="11" Foreground="#00915A" FontWeight="Medium"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        
                        <!-- Ligne 2: Priorités et Alertes -->
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Priorités -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                                <TextBlock Text="⚡ Priorités:" FontWeight="SemiBold" FontSize="12" 
                                         Foreground="#555" VerticalAlignment="Center" Width="90"/>
                                
                                <Border Background="#FFEBEE" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <Polygon Points="0,8 5,0 10,8" Fill="#D32F2F" Margin="0,0,6,0"/>
                                        <TextBlock Text="Urgente" FontSize="11" Foreground="#B71C1C" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Background="#FFF8E1" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <Polygon Points="0,8 5,0 10,8" Fill="#FFA000" Margin="0,0,6,0"/>
                                        <TextBlock Text="Haute" FontSize="11" Foreground="#F57C00" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Background="#E0F2F1" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="10" Height="10" Fill="#00897B" Margin="0,0,6,0"/>
                                        <TextBlock Text="Normale" FontSize="11" Foreground="#00695C" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            
                            <!-- Alertes -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <TextBlock Text="⚠️ Alertes:" FontWeight="SemiBold" FontSize="12" 
                                         Foreground="#555" VerticalAlignment="Center" Width="80"/>
                                
                                <Border Background="#FFCDD2" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🔴" FontSize="10" Margin="0,0,6,0"/>
                                        <TextBlock Text="Retard critique" FontSize="11" Foreground="#C62828" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Background="#FFE0B2" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🟠" FontSize="10" Margin="0,0,6,0"/>
                                        <TextBlock Text="Échéance proche" FontSize="11" Foreground="#E65100" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Background="#C8E6C9" CornerRadius="4" Padding="8,4">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🟢" FontSize="10" Margin="0,0,6,0"/>
                                        <TextBlock Text="Dans les temps" FontSize="11" Foreground="#00915A" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
                
                <!-- Timeline des projets par mois -->
                <Border Background="White" CornerRadius="6" Padding="20" Margin="0,0,0,15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.15" Color="#00915A"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="📊 Vue Mensuelle des Projets" FontSize="18" FontWeight="Bold" 
                                   Foreground="#1A1919" Margin="0,0,0,15"/>

                        <!-- En-tête des mois -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Projet" FontWeight="Bold" FontSize="12" 
                                       Foreground="#6D6D6D" VerticalAlignment="Center"/>

                            <ItemsControl Grid.Column="1" ItemsSource="{Binding Months}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Rows="1"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Label}" FontSize="10" FontWeight="Bold"
                                                   Foreground="#6D6D6D" TextAlignment="Center" 
                                                   TextWrapping="NoWrap"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>

                        <!-- Lignes des projets avec ScrollViewer pour 50+ projets -->
                        <ScrollViewer MaxHeight="500" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding ProjetsTimeline}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,3,0,3">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Nom du projet + infos compactes -->
                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Projet.Nom}" FontWeight="SemiBold" 
                                                           FontSize="11" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"
                                                           Foreground="#1A1919" ToolTip="{Binding Projet.Nom}"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,1,0,0">
                                                    <TextBlock Text="{Binding ProgressionPct, StringFormat={}{0:F0}%}" 
                                                               FontSize="9" Foreground="#6D6D6D" Margin="0,0,5,0"/>
                                                    <TextBlock Text="•" FontSize="9" Foreground="#CCC" Margin="0,0,5,0"/>
                                                    <TextBlock Text="{Binding ChargeInfo}" FontSize="9" 
                                                               Foreground="#00915A" FontWeight="SemiBold"/>
                                                </StackPanel>
                                            </StackPanel>

                                        <!-- Barre de progression temporelle compacte -->
                                        <Grid Grid.Column="1" Height="28" Margin="5,0,0,0">
                                            <Border Background="#F0F0F0" CornerRadius="3"/>
                                            <Grid>
                                                <Border Height="24" VerticalAlignment="Center"
                                                        Background="{Binding BarreColor}"
                                                        CornerRadius="3" HorizontalAlignment="Stretch"
                                                        MouseDown="ProjetBarre_MouseDown"
                                                        Tag="{Binding}"
                                                        Cursor="Hand">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Margin">
                                                                <Setter.Value>
                                                                    <MultiBinding Converter="{StaticResource TimelineBarMarginConverter}">
                                                                        <Binding Path="BarreLeft"/>
                                                                        <Binding Path="BarreWidth"/>
                                                                        <Binding RelativeSource="{RelativeSource AncestorType=Grid}" Path="ActualWidth"/>
                                                                    </MultiBinding>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Opacity" Value="0.85"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Border.ToolTip>
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding PeriodeInfo}" FontWeight="Bold"/>
                                                            <TextBlock Text="{Binding ProgressionInfo}" Margin="0,2,0,0"/>
                                                            <TextBlock Text="{Binding ChargeInfo}"/>
                                                        </StackPanel>
                                                    </Border.ToolTip>
                                                    <TextBlock Text="{Binding ProgressionPct, StringFormat={}{0:F0}%}" 
                                                               Foreground="White" FontSize="9" FontWeight="Bold"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </Grid>
                                        </Grid>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        </ScrollViewer>

                        <TextBlock Text="💡 La largeur des barres représente la durée des projets. La couleur indique la progression."
                                   FontSize="10" Foreground="#6D6D6D" FontStyle="Italic" Margin="0,15,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Liste détaillée des tâches -->
                <TextBlock Text="📋 Détail des Tâches" FontSize="18" FontWeight="Bold" 
                           Foreground="#1A1919" Margin="0,10,0,15"/>

                <ItemsControl ItemsSource="{Binding TimelineItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.15" Color="#00915A"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Header with title and status -->
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Item.Titre}" FontSize="16" FontWeight="Bold" 
                                                   Foreground="#1A1919" TextWrapping="Wrap"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                            <TextBlock Text="👤 " FontSize="12"/>
                                            <TextBlock Text="{Binding DevName}" FontSize="12" Foreground="#00915A" FontWeight="SemiBold"/>
                                            <TextBlock Text=" • 📁 " FontSize="12" Margin="10,0,0,0"/>
                                            <TextBlock Text="{Binding ProjetName}" FontSize="12" Foreground="#6D6D6D"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <Border Grid.Column="1" Padding="10,5" CornerRadius="4" VerticalAlignment="Top">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#00915A"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Item.Statut}" Value="Afaire">
                                                        <Setter Property="Background" Value="#9E9E9E"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Item.Statut}" Value="EnCours">
                                                        <Setter Property="Background" Value="#2196F3"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Item.Statut}" Value="Test">
                                                        <Setter Property="Background" Value="#FF9800"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Item.Statut}" Value="Termine">
                                                        <Setter Property="Background" Value="#4CAF50"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Item.Statut}" Foreground="White" 
                                                   FontSize="11" FontWeight="Bold"/>
                                    </Border>
                                </Grid>

                                <!-- Dates -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
                                    <Border Background="#E8F5E9" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🎬 Début: " FontSize="11" Foreground="#2E7D32" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Item.DateCreation, StringFormat='{}{0:dd/MM/yyyy}'}" 
                                                       FontSize="11" Foreground="#2E7D32"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Background="#FFEBEE" CornerRadius="4" Padding="8,4">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🎯 Deadline: " FontSize="11" Foreground="#C62828" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Item.DateFinAttendue, StringFormat='{}{0:dd/MM/yyyy}'}" 
                                                       FontSize="11" Foreground="#C62828"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Padding="8,4" Margin="10,0,0,0" CornerRadius="4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DaysRemaining}" Value="0">
                                                        <Setter Property="Background" Value="#FFF3E0"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="11" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                                    <Setter Property="Text" Value="{Binding ProgressStatus}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DaysRemaining}" Value="0">
                                                            <Setter Property="Foreground" Value="#EF6C00"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </StackPanel>

                                <!-- Progress Bar -->
                                <Grid Grid.Row="2" Margin="0,0,0,10">
                                    <Border Height="30" Background="#E0E0E0" CornerRadius="4"/>
                                    <Border Height="30" CornerRadius="4" HorizontalAlignment="Left">
                                        <Border.Width>
                                            <MultiBinding Converter="{StaticResource ProgressWidthConverter}">
                                                <Binding Path="ProgressPercentage"/>
                                                <Binding RelativeSource="{RelativeSource AncestorType=Grid}" Path="ActualWidth"/>
                                            </MultiBinding>
                                        </Border.Width>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#00915A"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DaysRemaining}" Value="0">
                                                        <Setter Property="Background" Value="#F57C00"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding ProgressPercentage, StringFormat='{}{0:F0}%'}" 
                                                   Foreground="White" FontWeight="Bold" FontSize="12"
                                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>
                                </Grid>

                                <!-- Stats -->
                                <StackPanel Grid.Row="3" Orientation="Horizontal">
                                    <Border Background="#E3F2FD" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📊 Durée totale: " FontSize="11" Foreground="#1976D2" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding DaysTotal}" FontSize="11" Foreground="#1976D2"/>
                                            <TextBlock Text=" jours" FontSize="11" Foreground="#1976D2"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Background="#FFF3E0" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⏱️ Jours écoulés: " FontSize="11" Foreground="#F57C00" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding DaysElapsed}" FontSize="11" Foreground="#F57C00"/>
                                            <TextBlock Text=" jours" FontSize="11" Foreground="#F57C00"/>
                                        </StackPanel>
                                    </Border>

                                    <Border CornerRadius="4" Padding="8,4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DaysRemaining}" Value="0">
                                                        <Setter Property="Background" Value="#FFEBEE"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⏳ Jours restants: " FontSize="11" FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding DaysRemaining}" Value="0">
                                                                <Setter Property="Foreground" Value="#C62828"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock Text="{Binding DaysRemaining}" FontSize="11">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding DaysRemaining}" Value="0">
                                                                <Setter Property="Foreground" Value="#C62828"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock Text=" jours" FontSize="11">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding DaysRemaining}" Value="0">
                                                                <Setter Property="Foreground" Value="#C62828"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
