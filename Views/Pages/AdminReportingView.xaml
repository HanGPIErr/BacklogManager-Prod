<UserControl x:Class="BacklogManager.Views.Pages.AdminReportingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BacklogManager.Views.Pages"
             xmlns:services="clr-namespace:BacklogManager.Services"
             Background="#F5F7FA"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <UserControl.Resources>
        <!--<local:ReportingPercentageToWidthConverter x:Key="PercentageToWidthConverter"/>-->
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="40,25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header avec titre -->
            <Border Grid.Row="0" CornerRadius="12" Padding="35,25" Margin="0,0,0,25">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#00915A" Offset="0"/>
                        <GradientStop Color="#00A566" Offset="0.5"/>
                        <GradientStop Color="#00B96E" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="3" Opacity="0.25" Color="#00915A"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock FontSize="32" FontWeight="Bold" Foreground="White">
                        <Run Text="📊 "/>
                        <Run x:Name="TxtReportingTitle" Text="REPORTING &amp; PILOTAGE"/>
                    </TextBlock>
                    <TextBlock x:Name="TxtReportingDescription" Text="Analyse consolidée des programmes et projets pour le pilotage stratégique" 
                               FontSize="14" Foreground="White" Opacity="0.9" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- Sélecteur de périmètre (Programme / Projet) -->
            <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="30,25" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Toggle Programme / Projet -->
                    <Border Grid.Row="0" Background="#F5F5F5" CornerRadius="25" Padding="5" HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="BtnModeProgramme" 
                                    Background="#00915A" Foreground="White" 
                                    FontSize="14" FontWeight="SemiBold" Padding="28,12"
                                    Margin="0" BorderThickness="0" Cursor="Hand"
                                    Click="BtnModeProgramme_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            CornerRadius="20" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📊" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtProgramView" Text="Vue Programme"/>
                                </StackPanel>
                            </Button>
                            
                            <Button x:Name="BtnModeProjet" 
                                    Background="Transparent" Foreground="#6D6D6D" 
                                    FontSize="14" FontWeight="Normal" Padding="28,12"
                                    Margin="5,0,0,0" BorderThickness="0" Cursor="Hand"
                                    Click="BtnModeProjet_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            CornerRadius="20" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📁" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtProjectView" Text="Vue Projet"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                    
                    <!-- Sélecteurs dynamiques -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Sélecteur Programme -->
                        <StackPanel x:Name="PanelProgramme" Grid.Column="0" Visibility="Visible">
                            <TextBlock x:Name="LblProgramme" Text="Programme *" FontSize="13" FontWeight="Bold" Foreground="#333" Margin="0,0,0,8"/>
                            <ComboBox x:Name="ComboProgramme" Height="40" FontSize="13" 
                                      DisplayMemberPath="Nom" SelectionChanged="ComboProgramme_SelectionChanged"/>
                        </StackPanel>
                        
                        <!-- Sélecteur Équipe (visible en mode Projet) -->
                        <StackPanel x:Name="PanelEquipe" Grid.Column="0" Visibility="Collapsed">
                            <TextBlock x:Name="LblEquipe" Text="Équipe *" FontSize="13" FontWeight="Bold" Foreground="#333" Margin="0,0,0,8"/>
                            <ComboBox x:Name="ComboEquipe" Height="40" FontSize="13" 
                                      DisplayMemberPath="Nom" SelectionChanged="ComboEquipe_SelectionChanged"/>
                        </StackPanel>
                        
                        <!-- Sélecteur Projet -->
                        <StackPanel x:Name="PanelProjet" Grid.Column="2" Visibility="Collapsed">
                            <TextBlock x:Name="LblProjet" Text="Projet *" FontSize="13" FontWeight="Bold" Foreground="#333" Margin="0,0,0,8"/>
                            <ComboBox x:Name="ComboProjet" Height="40" FontSize="13" 
                                      DisplayMemberPath="Nom" SelectionChanged="ComboProjet_SelectionChanged"/>
                        </StackPanel>
                        
                        <!-- Sélecteur de période -->
                        <StackPanel Grid.Column="4" MinWidth="250">
                            <TextBlock x:Name="LblPeriode" Text="Période d'analyse *" FontSize="13" FontWeight="Bold" Foreground="#333" Margin="0,0,0,8"/>
                            <ComboBox x:Name="ComboPeriode" Height="40" FontSize="13" 
                                      SelectedIndex="3" SelectionChanged="ComboPeriode_SelectionChanged">
                                <ComboBoxItem Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Stats_PeriodCurrentMonth], Mode=OneWay}"/>
                                <ComboBoxItem Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Stats_Period2Months], Mode=OneWay}"/>
                                <ComboBoxItem Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Stats_PeriodCustom], Mode=OneWay}"/>
                                <ComboBoxItem Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Stats_PeriodAll], Mode=OneWay}"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <!-- Sélecteur de période personnalisée (caché par défaut) -->
            <Border x:Name="PanelPeriodePersonnalisee" Grid.Row="2" Background="White" CornerRadius="12" 
                    Padding="30,20" Margin="0,0,0,20" Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Mois de début" FontSize="13" FontWeight="Bold" Foreground="#333" Margin="0,0,0,8"/>
                        <DatePicker x:Name="DateDebutPeriode" Height="40" FontSize="13" Width="160"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Mois de fin" FontSize="13" FontWeight="Bold" Foreground="#333" Margin="0,0,0,8"/>
                        <DatePicker x:Name="DateFinPeriode" Height="40" FontSize="13" Width="160"/>
                    </StackPanel>
                    
                    <Button x:Name="BtnAppliquerPeriode" Grid.Column="4" VerticalAlignment="Bottom"
                            Height="40" Padding="25,0" Background="#00915A" Foreground="White"
                            BorderThickness="0" Cursor="Hand" Click="BtnAppliquerPeriode_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#00A568"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock x:Name="TxtApplyButton" Text="Appliquer" FontSize="13" FontWeight="SemiBold"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Message si rien n'est sélectionné -->
            <Border x:Name="PanelAucuneSelection" Grid.Row="3" Background="White" CornerRadius="12" 
                    Padding="50" Margin="0,20,0,0" Visibility="Visible">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                </Border.Effect>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="📊" FontSize="48" TextAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock x:Name="TxtSelectMessage" Text="Sélectionnez un programme ou un projet" FontSize="16" 
                               Foreground="#999" TextAlignment="Center"/>
                    <TextBlock x:Name="TxtSelectSubMessage" Text="pour afficher les indicateurs de pilotage" FontSize="14" 
                               Foreground="#BBB" TextAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- KPIs en cards - FULL WIDTH GRID (caché par défaut) -->
            <Grid x:Name="PanelKPIs" Grid.Row="3" Margin="0,0,0,20" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="15"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Ligne 1: KPIs principaux -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                
                <!-- Avancement -->
                <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="25,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock x:Name="LblProgress" Text="AVANCEMENT" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                        <TextBlock FontSize="36" FontWeight="Bold" Foreground="#2196F3" Margin="0,10,0,8">
                            <Run x:Name="TxtAvancement" Text="0"/>
                            <Run Text="%" FontSize="22"/>
                        </TextBlock>
                        <ProgressBar x:Name="BarAvancement" Height="8" Minimum="0" Maximum="100" 
                                     Value="0" Background="#E0E0E0" Foreground="#2196F3" BorderThickness="0"/>
                    </StackPanel>
                </Border>
                
                <!-- Tâches -->
                <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="25,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock x:Name="LblTasks" Text="TÂCHES" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                        <TextBlock FontSize="36" FontWeight="Bold" Foreground="#9C27B0" Margin="0,10,0,0">
                            <Run x:Name="TxtTachesCompletes" Text="0"/>
                            <Run Text="/" FontSize="24" Foreground="#999"/>
                            <Run x:Name="TxtTachesTotal" Text="0" FontSize="24" Foreground="#999"/>
                        </TextBlock>
                        <TextBlock x:Name="TxtTachesRestantes" FontSize="11" Foreground="#666" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Équipes -->
                <Border Grid.Column="4" Background="White" CornerRadius="12" Padding="25,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock x:Name="TxtLabelEquipes" Text="ÉQUIPES" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                        <TextBlock FontSize="36" FontWeight="Bold" Foreground="#FF9800" Margin="0,10,0,0">
                            <Run x:Name="TxtNbEquipes" Text="0"/>
                        </TextBlock>
                        <TextBlock x:Name="TxtDescriptionEquipes" Text="équipe(s) impliquée(s)" FontSize="11" Foreground="#666" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Livraison -->
                <Border Grid.Column="6" Background="White" CornerRadius="12" Padding="25,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock x:Name="LblDelivery" Text="LIVRAISON" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtTargetDelivery" FontSize="20" FontWeight="Bold" 
                                   Foreground="#F44336" Margin="0,10,0,0" Text="--"/>
                        <TextBlock x:Name="TxtDeadlineInfo" FontSize="11" Foreground="#666" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>                </Grid>
                
                <!-- Ligne 2: 6 KPIs supplémentaires -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Vélocité -->
                    <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="25,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock x:Name="LblVelocity" Text="VÉLOCITÉ" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                            <TextBlock FontSize="36" FontWeight="Bold" Foreground="#00915A" Margin="0,10,0,0">
                                <Run x:Name="TxtVelocite" Text="0"/>
                            </TextBlock>
                            <TextBlock x:Name="LblTasksPerMonth" Text="tâche(s) / mois" FontSize="11" Foreground="#999" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Écart Charge -->
                    <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="25,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="ÉCART ESTIMATION" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtEcartCharge" FontSize="36" FontWeight="Bold" Foreground="#FF9800" Margin="0,10,0,8" Text="-"/>
                            <TextBlock x:Name="LblActualVsEstimated" Text="Réel vs Estimé" FontSize="11" Foreground="#999" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Priorités -->
                    <Border Grid.Column="4" Background="White" CornerRadius="12" Padding="20,18">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock x:Name="LblPriorities" Text="PRIORITÉS" FontSize="10" Foreground="#999" FontWeight="Bold" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <Ellipse Width="16" Height="16" Fill="#F44336" Margin="0,0,0,4"/>
                                    <TextBlock x:Name="TxtPrioriteHaute" Text="0" FontSize="24" FontWeight="Bold" 
                                               Foreground="#F44336" TextAlignment="Center"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <Ellipse Width="16" Height="16" Fill="#FF9800" Margin="0,0,0,4"/>
                                    <TextBlock x:Name="TxtPrioriteMoyenne" Text="0" FontSize="24" FontWeight="Bold" 
                                               Foreground="#FF9800" TextAlignment="Center"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                    <Ellipse Width="16" Height="16" Fill="#4CAF50" Margin="0,0,0,4"/>
                                    <TextBlock x:Name="TxtPrioriteBasse" Text="0" FontSize="24" FontWeight="Bold" 
                                               Foreground="#4CAF50" TextAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Statut RAG -->
                    <Border x:Name="BorderStatutRAG" Grid.Column="6" Background="White" CornerRadius="12" Padding="20,18">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock x:Name="LblHealthStatus" Text="STATUT SANTÉ" FontSize="10" Foreground="#999" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10"/>
                            
                            <!-- Badge RAG comme dans DetailProjetSteerCoView -->
                            <Border x:Name="BadgeRAG" CornerRadius="15" Padding="15,8" Background="#4CAF50" 
                                    HorizontalAlignment="Center">
                                <TextBlock x:Name="TxtStatutRAG" FontSize="13" FontWeight="Bold" 
                                           Foreground="White" Text="GREEN" TextAlignment="Center"/>
                            </Border>
                            
                            <TextBlock x:Name="LblOnTrack" Text="Dans les temps" FontSize="10" Foreground="#4CAF50" 
                                       FontWeight="SemiBold" TextAlignment="Center" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Tâches en retard -->
                    <Border Grid.Column="8" Background="White" CornerRadius="12" Padding="20,18">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock x:Name="LblDelayed" Text="EN RETARD" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                            <TextBlock FontSize="32" FontWeight="Bold" Foreground="#F44336" Margin="0,8,0,0">
                                <Run x:Name="TxtTachesRetard" Text="0"/>
                            </TextBlock>
                            <TextBlock x:Name="LblRetardTasks" Text="tâche(s)" FontSize="10" Foreground="#666" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Complexité -->
                    <Border Grid.Column="10" Background="White" CornerRadius="12" Padding="20,18">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock x:Name="LblComplexity" Text="COMPLEXITÉ" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                            <TextBlock FontSize="32" FontWeight="Bold" Foreground="#673AB7" Margin="0,8,0,0">
                                <Run x:Name="TxtComplexiteMoyenne" Text="-"/>
                            </TextBlock>
                            <TextBlock x:Name="LblAverageComplexity" Text="Moyenne" FontSize="10" Foreground="#666" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>            </Grid>

            <!-- Section Comparative (avant / période / reste) -->
            <Border x:Name="PanelComparatif" Grid.Row="4" Background="White" CornerRadius="12" 
                    Padding="30,25" Margin="0,0,0,20" Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="LblComparativeView" Text="📈 VUE COMPARATIVE" FontSize="16" FontWeight="Bold" 
                               Foreground="#333" Margin="0,0,0,20"/>
                    
                    <Grid x:Name="GridComparatif3Col">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Avant la période -->
                        <Border Grid.Column="0" Background="#F5F5F5" CornerRadius="10" Padding="20">
                            <StackPanel>
                                <TextBlock x:Name="LblBeforePeriod1" Text="⏮️ AVANT LA PÉRIODE" FontSize="13" FontWeight="Bold" 
                                           Foreground="#666" Margin="0,0,0,15" TextAlignment="Center"/>
                                <TextBlock x:Name="LblCompletedTasksLabel1" Text="Tâches terminées" FontSize="11" Foreground="#999" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtAvantTaches" FontSize="24" FontWeight="Bold" 
                                           Foreground="#4CAF50" Text="0"/>
                                <TextBlock x:Name="LblWorkloadDays1" Text="Charge (jours)" FontSize="11" Foreground="#999" Margin="0,10,0,5"/>
                                <TextBlock x:Name="TxtAvantCharge" FontSize="18" FontWeight="Bold" 
                                           Foreground="#666" Text="0j"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Pendant la période -->
                        <Border Grid.Column="2" Background="#E3F2FD" BorderBrush="#2196F3" 
                                BorderThickness="2" CornerRadius="10" Padding="20">
                            <StackPanel>
                                <TextBlock x:Name="LblSelectedPeriod1" Text="🔍 PÉRIODE SÉLECTIONNÉE" FontSize="13" FontWeight="Bold" 
                                           Foreground="#1976D2" Margin="0,0,0,15" TextAlignment="Center"/>
                                <TextBlock x:Name="LblTasksLabel1" Text="Tâches" FontSize="11" Foreground="#1565C0" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtPeriodeTaches" FontSize="24" FontWeight="Bold" 
                                           Foreground="#2196F3" Text="0"/>
                                <TextBlock x:Name="LblWorkloadDays2" Text="Charge (jours)" FontSize="11" Foreground="#1565C0" Margin="0,10,0,5"/>
                                <TextBlock x:Name="TxtPeriodeCharge" FontSize="18" FontWeight="Bold" 
                                           Foreground="#1976D2" Text="0j"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Reste à faire -->
                        <Border Grid.Column="4" Background="#FFF3E0" CornerRadius="10" Padding="20">
                            <StackPanel>
                                <TextBlock x:Name="LblToDoRemaining" Text="⏭️ RESTE À FAIRE" FontSize="13" FontWeight="Bold" 
                                           Foreground="#E65100" Margin="0,0,0,15" TextAlignment="Center"/>
                                <TextBlock x:Name="LblRemainingTasksComp" Text="Tâches restantes" FontSize="11" Foreground="#999" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtResteTaches" FontSize="24" FontWeight="Bold" 
                                           Foreground="#FF9800" Text="0"/>
                                <TextBlock x:Name="LblEstimatedWorkloadDays" Text="Charge estimée (jours)" FontSize="11" Foreground="#999" Margin="0,10,0,5"/>
                                <TextBlock x:Name="TxtResteCharge" FontSize="18" FontWeight="Bold" 
                                           Foreground="#F57C00" Text="0j"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                    
                    <!-- Version 2 colonnes (si tout est fait) -->
                    <Grid x:Name="GridComparatif2Col" Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Avant la période -->
                        <Border Grid.Column="0" Background="#F5F5F5" CornerRadius="10" Padding="20">
                            <StackPanel>
                                <TextBlock x:Name="LblBeforePeriod2" Text="⏮️ AVANT LA PÉRIODE" FontSize="13" FontWeight="Bold" 
                                           Foreground="#666" Margin="0,0,0,15" TextAlignment="Center"/>
                                <TextBlock x:Name="LblCompletedTasksLabel2" Text="Tâches terminées" FontSize="11" Foreground="#999" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtAvantTaches2Col" FontSize="24" FontWeight="Bold" 
                                           Foreground="#4CAF50" Text="0"/>
                                <TextBlock x:Name="LblWorkloadDays3" Text="Charge (jours)" FontSize="11" Foreground="#999" Margin="0,10,0,5"/>
                                <TextBlock x:Name="TxtAvantCharge2Col" FontSize="18" FontWeight="Bold" 
                                           Foreground="#666" Text="0j"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Pendant la période -->
                        <Border Grid.Column="2" Background="#E3F2FD" BorderBrush="#2196F3" 
                                BorderThickness="2" CornerRadius="10" Padding="20">
                            <StackPanel>
                                <TextBlock x:Name="LblSelectedPeriod2" Text="🔍 PÉRIODE SÉLECTIONNÉE" FontSize="13" FontWeight="Bold" 
                                           Foreground="#1976D2" Margin="0,0,0,15" TextAlignment="Center"/>
                                <TextBlock x:Name="LblTasksLabel2" Text="Tâches" FontSize="11" Foreground="#1565C0" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtPeriodeTaches2Col" FontSize="24" FontWeight="Bold" 
                                           Foreground="#2196F3" Text="0"/>
                                <TextBlock x:Name="LblWorkloadDays4" Text="Charge (jours)" FontSize="11" Foreground="#1565C0" Margin="0,10,0,5"/>
                                <TextBlock x:Name="TxtPeriodeCharge2Col" FontSize="18" FontWeight="Bold" 
                                           Foreground="#1976D2" Text="0j"/>
                                <Border Background="#4CAF50" CornerRadius="5" Padding="10,5" Margin="0,15,0,0">
                                    <TextBlock x:Name="LblAllCompleted" Text="✅ TOUT EST TERMINÉ" FontSize="11" FontWeight="Bold" 
                                               Foreground="White" TextAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Timeline & Gains (caché par défaut) -->
            <Grid x:Name="PanelTimelineGains" Grid.Row="5" Margin="0,0,0,20" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Timeline visuelle -->
                <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="25,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="📅 TIMELINE &amp; ÉCHÉANCES" FontSize="14" FontWeight="Bold" 
                                   Foreground="#333" Margin="0,0,0,15"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="DATE DÉBUT" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                                <TextBlock x:Name="TxtDateDebut" FontSize="16" FontWeight="Bold" 
                                           Foreground="#666" Margin="0,5,0,0"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="DATE FIN" FontSize="10" Foreground="#999" FontWeight="Bold"/>
                                <TextBlock x:Name="TxtDateFin" FontSize="16" FontWeight="Bold" 
                                           Foreground="#666" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Grid>
                        
                        <Grid Margin="0,15,0,10">
                            <Border Height="10" Background="#E8EAF0" CornerRadius="5"/>
                            <Border x:Name="ProgressTemporel" Height="10" Background="#2196F3" 
                                    CornerRadius="5" HorizontalAlignment="Left" Width="0"/>
                        </Grid>
                        
                        <TextBlock x:Name="TxtProgressionTemps" FontSize="12" Foreground="#666" 
                                   TextAlignment="Center" Margin="0,5,0,0" Text="--"/>
                    </StackPanel>
                </Border>

                <!-- Gains Temps -->
                <Border Grid.Column="2" Background="#E3F2FD" BorderBrush="#2196F3" BorderThickness="2" 
                        CornerRadius="12" Padding="25,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#2196F3"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="⏱️ GAINS TEMPS" FontSize="14" Foreground="#1976D2" FontWeight="Bold" Margin="0,0,0,15"/>
                        <TextBlock x:Name="TxtGainsTemps" FontSize="26" FontWeight="Bold" 
                                   Foreground="#1565C0" TextWrapping="Wrap" TextAlignment="Center" 
                                   VerticalAlignment="Center" Margin="0,20,0,20" Text="--"/>
                    </StackPanel>
                </Border>
                
                <!-- Gains Financiers -->
                <Border Grid.Column="4" Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="2" 
                        CornerRadius="12" Padding="25,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#4CAF50"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="💰 GAINS FINANCIERS" FontSize="14" Foreground="#2E7D32" FontWeight="Bold" Margin="0,0,0,15"/>
                        <TextBlock x:Name="TxtGainsFinanciers" FontSize="26" FontWeight="Bold" 
                                   Foreground="#1B5E20" TextWrapping="Wrap" TextAlignment="Center"
                                   VerticalAlignment="Center" Margin="0,20,0,20" Text="--"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Section Contributions des Ressources (caché par défaut) -->
            <Border x:Name="PanelContributions" Grid.Row="6" Background="White" CornerRadius="12" 
                    Padding="30,25" Margin="0,0,0,20" Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                </Border.Effect>
                <StackPanel>
                    <Grid Margin="0,0,0,20">
                        <TextBlock x:Name="LblContributionsTitle" Text="👥 CONTRIBUTIONS DES RESSOURCES" FontSize="16" FontWeight="Bold" 
                                   Foreground="#333" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <Button x:Name="BtnExportExcel" Content="📥 Export Excel" HorizontalAlignment="Right"
                                Background="#00915A" Foreground="White" Padding="15,8" BorderThickness="0"
                                Cursor="Hand" Click="BtnExportExcel_Click">
                            <Button.Style>
                                 <Style TargetType="Button">
                                      <Setter Property="Template">
                                          <Setter.Value>
                                              <ControlTemplate TargetType="Button">
                                                  <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                       <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                  </Border>
                                              </ControlTemplate>
                                          </Setter.Value>
                                      </Setter>
                                      <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                 <Setter Property="Background" Value="#00A568"/>
                                            </Trigger>
                                      </Style.Triggers>
                                 </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                    
                    <DataGrid x:Name="ListeContributions" AutoGenerateColumns="False" 
                              IsReadOnly="True" HeadersVisibility="Column"
                              GridLinesVisibility="None" Background="Transparent"
                              BorderThickness="0" RowHeight="50"
                              RowDetailsVisibilityMode="Collapsed">
                        <DataGrid.RowDetailsTemplate>
                            <DataTemplate>
                                <Border Background="#FAFAFA" Padding="20,15" BorderBrush="#E0E0E0" BorderThickness="0,1,0,1" Margin="0,0,0,10">
                                     <StackPanel>
                                         <TextBlock Text="📅 Détail temps par jour" FontWeight="Bold" Foreground="#666" Margin="0,0,0,10" FontSize="12"/>
                                         
                                         <!-- Ajout d'un ScrollViewer pour limiter la hauteur si la liste est longue -->
                                         <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
                                             <ItemsControl ItemsSource="{Binding DetailTempsParJour}">
                                                 <ItemsControl.ItemTemplate>
                                                     <DataTemplate>
                                                         <Grid Margin="0,0,10,8">
                                                             <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="120"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="100"/>
                                                             </Grid.ColumnDefinitions>
                                                             <TextBlock Text="{Binding DateFormattee}" Foreground="#888" FontSize="12"/>
                                                             <TextBlock Grid.Column="1" Text="{Binding Scope}" Foreground="#333" FontSize="12"/>
                                                             <TextBlock Grid.Column="2" Text="{Binding Heures, StringFormat={}{0:0.##} h}" FontWeight="Bold" Foreground="#00915A" HorizontalAlignment="Right" FontSize="12"/>
                                                         </Grid>
                                                     </DataTemplate>
                                                 </ItemsControl.ItemTemplate>
                                                 <ItemsControl.ItemsPanel>
                                                     <ItemsPanelTemplate>
                                                         <StackPanel/>
                                                     </ItemsPanelTemplate>
                                                 </ItemsControl.ItemsPanel>
                                             </ItemsControl>
                                         </ScrollViewer>

                                         <Border Margin="0,0,0,0" BorderThickness="0,1,0,0" BorderBrush="#EEE" Padding="0,5,0,0" HorizontalAlignment="Right" Width="200">
                                            <Grid>
                                                <TextBlock Text="Total Période" Foreground="#666" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding TempsTotalFormatte}" HorizontalAlignment="Right" Foreground="#00915A" FontWeight="Bold"/>
                                            </Grid>
                                         </Border>
                                     </StackPanel>
                                </Border>
                            </DataTemplate>
                        </DataGrid.RowDetailsTemplate>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="#666"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Padding" Value="15,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseLeftButtonUp" Handler="DataGridRow_MouseLeftButtonUp"/>
                                <Setter Property="Background" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F9F9F9"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="15,10"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Background="{TemplateBinding Background}" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.CellStyle>
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn x:Name="ColMembers" Header="MEMBRES" Binding="{Binding NomDeveloppeur}" 
                                                Width="*" FontSize="13" FontWeight="SemiBold"/>
                            
                            <DataGridTextColumn x:Name="ColTeam" Header="ÉQUIPE" Binding="{Binding NomEquipe}" 
                                                Width="120" FontSize="13"/>

                            <DataGridTextColumn x:Name="ColProgrammes" Header="PROGRAMMES" Binding="{Binding ProgrammesImpliques}" 
                                                Width="150" FontSize="12">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Padding" Value="5,0"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                                                
                            <DataGridTextColumn x:Name="ColProjects" Header="PROJETS" Binding="{Binding ProjetsImpliques}" 
                                                Width="150" FontSize="12">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Padding" Value="5,0"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn x:Name="ColNbMembres" Header="MBRS" Binding="{Binding NbMembresEquipeActifs}" 
                                                Width="60" FontSize="13">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="ToolTip" Value="Membres actifs de l'équipe sur le programme"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn x:Name="ColTotalTime" Header="TOTAL" Binding="{Binding TempsTotalFormatte}" 
                                                Width="80" FontSize="13" FontWeight="SemiBold"/>
                            
                            <DataGridTextColumn x:Name="ColCompletedTasks" Header="TÂCHES COMPLÉTÉES" Width="150" FontSize="13">
                                <DataGridTextColumn.Binding>
                                    <MultiBinding StringFormat="{}{0} / {1}">
                                        <Binding Path="TachesCompletes"/>
                                        <Binding Path="TachesTotal"/>
                                    </MultiBinding>
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>
                            
                            <DataGridTextColumn x:Name="ColCompletionRate" Header="TAUX COMPLÉTION" Binding="{Binding TauxCompletion}" 
                                                Width="140" FontSize="13" FontWeight="Bold">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <DataGridTextColumn x:Name="ColEstimatedDays" Header="JOURS ESTIMÉS" Binding="{Binding JoursEstimes}" 
                                                Width="150" FontSize="13" FontWeight="SemiBold"/>
                            
                            <DataGridTemplateColumn x:Name="ColContribution" Header="CONTRIBUTION" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="50"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <Border Grid.Column="0" Height="20" Background="#E0E0E0" 
                                                    CornerRadius="10" Margin="0,0,10,0">
                                                <Border Height="20" Background="#FF9800" CornerRadius="10" 
                                                        HorizontalAlignment="Left" Width="200">
                                                    <!--<Border.Width>
                                                        <MultiBinding Converter="{StaticResource PercentageToWidthConverter}">
                                                            <Binding Path="PourcentageContribution"/>
                                                            <Binding RelativeSource="{RelativeSource AncestorType=Border}" Path="ActualWidth"/>
                                                        </MultiBinding>
                                                    </Border.Width>-->
                                                </Border>
                                            </Border>
                                            
                                            <TextBlock Grid.Column="1" FontSize="13" FontWeight="Bold" 
                                                       Foreground="#FF9800" VerticalAlignment="Center">
                                                <Run Text="{Binding PourcentageContribution, StringFormat={}{0:0.0}}"/>
                                                <Run Text="%"/>
                                            </TextBlock>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Section Programme détaillée (caché par défaut, visible uniquement en mode Programme) -->
            <StackPanel x:Name="PanelProgrammeDetails" Grid.Row="7" Margin="0,0,0,20" Visibility="Collapsed">
                
                <!-- Nom du Programme avec bouton IA -->
                <Border Background="White" CornerRadius="12" Padding="30,25" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock x:Name="TxtNomProgramme" Grid.Column="0" 
                                   Text="E2E BG PROGRAM - AUTO DRAFTING" 
                                   FontSize="24" FontWeight="Bold" Foreground="#00915A" 
                                   VerticalAlignment="Center" TextAlignment="Center"/>
                        
                        <Button x:Name="BtnGenererPowerPoint" Grid.Column="1" 
                                Background="#2196F3" Foreground="White" 
                                Padding="15,10" BorderThickness="0" Cursor="Hand"
                                Click="BtnGenererPowerPoint_Click"
                                ToolTip="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Reporting_GeneratePowerPointTooltip]}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="📊" FontSize="16" Margin="0,0,8,0"/>
                                                        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Reporting_GeneratePowerPoint]}" FontSize="13" FontWeight="SemiBold"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#1976D2"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        
                        <Button x:Name="BtnGenererAvecIA" Grid.Column="3" 
                                Background="#00915A" Foreground="White" 
                                Padding="15,10" BorderThickness="0" Cursor="Hand"
                                Click="BtnGenererAvecIA_Click"
                                ToolTip="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Reporting_GenerateWithAITooltip]}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="✨" FontSize="16" Margin="0,0,8,0"/>
                                                        <TextBlock Text="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[Reporting_GenerateWithAI]}" FontSize="13" FontWeight="SemiBold"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#00A566"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>

                <!-- Phases/jalons dynamiques -->
                <Border Background="White" CornerRadius="12" Padding="30,25" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock x:Name="LblPhasesJalons" Text="📌 PHASES &amp; JALONS" 
                                   FontSize="16" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>
                        
                        <!-- Conteneur dynamique pour les phases -->
                        <StackPanel x:Name="ContainerPhases" Orientation="Vertical"/>
                    </StackPanel>
                </Border>

                <!-- Blocs Change Management et Evolving Scope -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Change Management -->
                    <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="25,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock x:Name="LblChangeManagement" Text="🔄 CHANGE MANAGEMENT" 
                                       FontSize="14" FontWeight="Bold" Foreground="#FF9800" Margin="0,0,0,15"/>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                                <StackPanel x:Name="TxtChangeManagement" Background="White"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                    
                    <!-- Evolving Scope -->
                    <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="25,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock x:Name="LblEvolvingScope" Text="📈 EVOLVING SCOPE" 
                                       FontSize="14" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,10"/>
                            <TextBlock x:Name="LblEvolvingScopeDesc" Text="items added over time" 
                                       FontSize="11" Foreground="#999" FontStyle="Italic" Margin="0,0,0,15"/>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                                <StackPanel x:Name="TxtEvolvingScope" Background="White"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Timeline d'avancement des projets du programme -->
                <Border Background="White" CornerRadius="12" Padding="30,25" Margin="0,0,0,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock x:Name="LblTimelineProjects" Text="📅 TIMELINE D'AVANCEMENT DES PROJETS" 
                                   FontSize="16" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>
                        
                        <!-- Conteneur pour la timeline avec mois -->
                        <Grid x:Name="TimelineContainer">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/> <!-- En-tête mois -->
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/> <!-- Projets -->
                            </Grid.RowDefinitions>
                            
                            <!-- En-tête des mois sera généré dynamiquement -->
                            <Grid x:Name="TimelineHeader" Grid.Row="0" Height="60"/>
                            
                            <!-- Liste des projets avec barres positionnées -->
                            <StackPanel x:Name="TimelineProjects" Grid.Row="2"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Tableau Progress Status (détails projets/bénéficiaires) -->
                <Border Background="White" CornerRadius="12" Padding="30,25" Margin="0,20,0,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel>
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="📋 PROGRESS STATUS" 
                                       FontSize="16" FontWeight="Bold" Foreground="#333"/>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Border Background="#FFEBEE" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                                    <TextBlock x:Name="TxtHighPriorityCount" Text="High Priority: 0" 
                                               FontSize="11" FontWeight="SemiBold" Foreground="#D32F2F"/>
                                </Border>
                                <Border x:Name="BadgeStatutProjet" Background="#E8F5E9" CornerRadius="4" Padding="8,4">
                                    <TextBlock x:Name="TxtStatutProjet" Text="WIP" 
                                               FontSize="11" FontWeight="Bold" Foreground="#2E7D32"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Conteneur pour les lignes du tableau (créé dynamiquement) -->
                        <StackPanel x:Name="ContainerProgressStatus" Background="White"/>
                    </StackPanel>
                </Border>
                
                <!-- Dashboard KPIs et Actions/Initiatives -->
                <Border Background="White" CornerRadius="12" Padding="30,25" Margin="0,20,0,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.1" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel>
                        <!-- Ligne des KPIs et filtres -->
                        <Grid Margin="0,0,0,30">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="250"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="280"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- % STATUS OVERVIEW -->
                            <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15">
                                <StackPanel>
                                    <TextBlock Text="📊 % STATUS OVERVIEW" FontSize="10" FontWeight="Bold" Foreground="#666" Margin="0,0,0,10"/>
                                    <Canvas x:Name="CanvasStatusOverview" Width="150" Height="150" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="TxtTotalInitiatives" Text="0" FontSize="28" FontWeight="Bold" 
                                               Foreground="#00915A" HorizontalAlignment="Center" Margin="0,-85,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- % of WP per Month -->
                            <Border Grid.Column="2" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15">
                                <StackPanel>
                                    <TextBlock Text="📈 % of WP per Month" FontSize="10" FontWeight="Bold" Foreground="#666" Margin="0,0,0,10"/>
                                    <Canvas x:Name="CanvasWPMonth" Width="150" Height="150" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- TEAM(S) -->
                            <Border Grid.Column="4" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15">
                                <StackPanel>
                                    <TextBlock Text="👥 TEAM(S)" FontSize="11" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="150">
                                        <StackPanel x:Name="ContainerTeamsFilter"/>
                                    </ScrollViewer>
                                </StackPanel>
                            </Border>
                            
                            <!-- SCOPE -->
                            <Border Grid.Column="6" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15">
                                <StackPanel>
                                    <TextBlock Text="🌍 SCOPE" FontSize="11" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                                    <CheckBox x:Name="ChkScopeGlobal" Content="Global" Margin="0,0,0,5" IsChecked="True"/>
                                    <CheckBox x:Name="ChkScopeLocal" Content="Local" Margin="0,0,0,5" IsChecked="True"/>
                                    <CheckBox x:Name="ChkScopeTransversal" Content="Transversal" IsChecked="True"/>
                                    
                                    <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,15,0,0">
                                        <StackPanel>
                                            <TextBlock Text="TOM Consolidation Rate" FontSize="9" Foreground="#666" Margin="0,0,0,8"/>
                                            
                                            <TextBlock Text="PRIORITY" FontSize="9" FontWeight="Bold" Foreground="#333" Margin="0,0,0,5"/>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Background="#4CAF50" Height="20" CornerRadius="3" Margin="0,0,2,0"/>
                                                <Border Grid.Column="1" Background="#FFC107" Height="20" CornerRadius="3" Margin="1,0,1,0"/>
                                                <Border Grid.Column="2" Background="#F44336" Height="20" CornerRadius="3" Margin="2,0,0,0"/>
                                            </Grid>
                                            
                                            <TextBlock Text="RISK" FontSize="9" FontWeight="Bold" Foreground="#333" Margin="0,0,0,5"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Background="#4CAF50" Height="20" CornerRadius="3" Margin="0,0,2,0"/>
                                                <Border Grid.Column="1" Background="#FFC107" Height="20" CornerRadius="3" Margin="1,0,1,0"/>
                                                <Border Grid.Column="2" Background="#F44336" Height="20" CornerRadius="3" Margin="2,0,0,0"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                            
                            <!-- DRIVERS -->
                            <Border Grid.Column="8" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15">
                                <StackPanel>
                                    <TextBlock Text="🎯 DRIVERS" FontSize="11" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                                    <CheckBox x:Name="ChkDriverComm" Content="Communication &amp; Continuous..." Margin="0,0,0,5" IsChecked="True"/>
                                    <CheckBox x:Name="ChkDriverIT" Content="IT Development" Margin="0,0,0,5" IsChecked="True"/>
                                    <CheckBox x:Name="ChkDriverProcess" Content="Process Efficiency" Margin="0,0,0,5" IsChecked="True"/>
                                    <CheckBox x:Name="ChkDriverTool" Content="Tool &amp; Automation Development" IsChecked="True"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                        
                        <!-- Tableau ACTIONS / INITIATIVES -->
                        <StackPanel>
                            <TextBlock Text="📋 ACTIONS / INITIATIVES" FontSize="14" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15"/>
                            
                            <!-- En-tête du tableau -->
                            <Grid x:Name="HeaderActionsInitiatives" Background="#00915A" Height="40">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0" Text="ACTIONS / INITIATIVES" FontSize="11" FontWeight="Bold" 
                                           Foreground="White" VerticalAlignment="Center" Margin="15,0,0,0"/>
                                <TextBlock Grid.Column="1" Text="PRIORITY" FontSize="11" FontWeight="Bold" 
                                           Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                <TextBlock Grid.Column="2" Text="NEXT STEPS" FontSize="11" FontWeight="Bold" 
                                           Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                <TextBlock Grid.Column="3" Text="ETA" FontSize="11" FontWeight="Bold" 
                                           Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
                            </Grid>
                            
                            <!-- Conteneur pour les lignes du tableau -->
                            <StackPanel x:Name="ContainerActionsInitiatives" Background="White"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
