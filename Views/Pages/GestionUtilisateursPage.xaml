<Page x:Class="BacklogManager.Views.Pages.GestionUtilisateursPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Actions -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="➕ Nouvel utilisateur" Click="BtnNouvelUtilisateur_Click"
                    Background="#00915A" Foreground="White" Padding="15,8" 
                    BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
            <Button Content="✏️ Modifier" Click="BtnModifierUtilisateur_Click" Margin="10,0,0,0"
                    Background="#6D6D6D" Foreground="White" Padding="15,8" 
                    BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
            <Button Content="🗑️ Supprimer" Click="BtnSupprimerUtilisateur_Click" Margin="10,0,0,0"
                    Background="#D32F2F" Foreground="White" Padding="15,8" 
                    BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
            <Button Content="🔄 Actualiser" Click="BtnActualiser_Click" Margin="10,0,0,0"
                    Background="#E0E0E0" Foreground="#1A1919" Padding="15,8" 
                    BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
        </StackPanel>

        <!-- Liste utilisateurs -->
        <DataGrid x:Name="LstUtilisateurs" Grid.Row="1" AutoGenerateColumns="False" 
                  IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  BorderThickness="1" BorderBrush="#E0E0E0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                <DataGridTextColumn Header="Username Windows" Binding="{Binding UsernameWindows}" Width="*"/>
                <DataGridTextColumn Header="Rôle" Binding="{Binding RoleId}" Width="150"/>
                <DataGridCheckBoxColumn Header="Actif" Binding="{Binding Actif}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
