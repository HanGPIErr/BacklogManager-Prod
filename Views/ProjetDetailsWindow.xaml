<Window x:Class="BacklogManager.Views.ProjetDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BacklogManager.Domain"
        Title="Détails du Projet" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header du projet -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.2" Color="#00915A"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Titre et statut -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="📊" FontSize="28" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock x:Name="TxtNomProjet" FontSize="24" FontWeight="Bold" 
                                   Foreground="#1A1919" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Border Grid.Column="1" x:Name="BorderStatut" CornerRadius="6" Padding="15,8">
                        <TextBlock x:Name="TxtStatut" FontSize="14" FontWeight="Bold"/>
                    </Border>
                </Grid>

                <!-- Description -->
                <TextBlock Grid.Row="1" x:Name="TxtDescription" FontSize="14" Foreground="#555" 
                           TextWrapping="Wrap" Margin="0,15,0,0"/>
                
                <!-- Phase du projet -->
                <Border Grid.Row="2" Background="#F3E5F5" CornerRadius="6" Padding="12" Margin="0,12,0,0" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔄 Phase : " FontSize="13" FontWeight="Bold" Foreground="#7B1FA2" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtPhase" FontSize="13" FontWeight="SemiBold" Foreground="#9C27B0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Métriques clés -->
                <Grid Grid.Row="3" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total tâches -->
                    <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="6" Padding="15" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="📋 Total Tâches" FontSize="12" Foreground="#1565C0" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TxtTotalTaches" FontSize="28" FontWeight="Bold" 
                                       Foreground="#2196F3" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Progression -->
                    <Border Grid.Column="1" Background="#E8F5E9" CornerRadius="6" Padding="15" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="✅ Progression" FontSize="12" Foreground="#2E7D32" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock x:Name="TxtProgression" FontSize="28" FontWeight="Bold" 
                                           Foreground="#00915A"/>
                                <TextBlock Text="%" FontSize="20" Foreground="#00915A" 
                                           VerticalAlignment="Bottom" Margin="2,0,0,4"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Charge totale -->
                    <Border Grid.Column="2" Background="#FFF3E0" CornerRadius="6" Padding="15" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="⏱️ Charge Estimée" FontSize="12" Foreground="#E65100" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock x:Name="TxtChargeEstimee" FontSize="28" FontWeight="Bold" 
                                           Foreground="#FF9800"/>
                                <TextBlock Text="j" FontSize="16" Foreground="#FF9800" 
                                           VerticalAlignment="Bottom" Margin="2,0,0,6"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Charge réelle -->
                    <Border Grid.Column="3" Background="#F3E5F5" CornerRadius="6" Padding="15">
                        <StackPanel>
                            <TextBlock Text="⚡ Temps Passé" FontSize="12" Foreground="#6A1B9A" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock x:Name="TxtTempsReel" FontSize="28" FontWeight="Bold" 
                                           Foreground="#9C27B0"/>
                                <TextBlock Text="j" FontSize="16" Foreground="#9C27B0" 
                                           VerticalAlignment="Bottom" Margin="2,0,0,6"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
                
                <!-- Équipes Assignées -->
                <Border Background="#F1F8E9" CornerRadius="6" Padding="15" Margin="0,15,0,0" x:Name="BorderEquipes">
                    <StackPanel>
                        <TextBlock Text="🏢 Équipes Assignées" FontSize="12" Foreground="#558B2F" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <ItemsControl x:Name="ListeEquipes">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#8BC34A" CornerRadius="4" Padding="10,5" Margin="0,0,8,0">
                                        <TextBlock Text="{Binding Nom}" FontSize="12" Foreground="White" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock x:Name="TxtAucuneEquipe" Text="Aucune équipe assignée" 
                                   FontSize="12" Foreground="#999" FontStyle="Italic" 
                                   Visibility="Collapsed"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Barre de progression globale -->
        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.1" Color="#00915A"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Avancement Global" FontSize="14" FontWeight="SemiBold" 
                           Foreground="#1A1919" Margin="0,0,0,8"/>
                <Grid>
                    <ProgressBar x:Name="ProgressBarGlobal" Height="30" Foreground="#00915A" 
                                 Background="#E0E0E0" BorderThickness="0"/>
                    <TextBlock x:Name="TxtProgressionLabel" FontSize="13" FontWeight="Bold" 
                               Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                
                <!-- Répartition par statut -->
                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="À faire" FontSize="11" Foreground="#1565C0"/>
                        <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                            <TextBlock x:Name="TxtCountAfaire" FontSize="18" FontWeight="Bold" Foreground="#2196F3"/>
                            <TextBlock Text=" tâches" FontSize="11" Foreground="#555" VerticalAlignment="Bottom" Margin="3,0,0,2"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="En cours" FontSize="11" Foreground="#E65100"/>
                        <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                            <TextBlock x:Name="TxtCountEnCours" FontSize="18" FontWeight="Bold" Foreground="#FF9800"/>
                            <TextBlock Text=" tâches" FontSize="11" Foreground="#555" VerticalAlignment="Bottom" Margin="3,0,0,2"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                        <TextBlock Text="En test" FontSize="11" Foreground="#6A1B9A"/>
                        <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                            <TextBlock x:Name="TxtCountEnTest" FontSize="18" FontWeight="Bold" Foreground="#9C27B0"/>
                            <TextBlock Text=" tâches" FontSize="11" Foreground="#555" VerticalAlignment="Bottom" Margin="3,0,0,2"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="3">
                        <TextBlock Text="Terminé" FontSize="11" Foreground="#2E7D32"/>
                        <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                            <TextBlock x:Name="TxtCountTermine" FontSize="18" FontWeight="Bold" Foreground="#00915A"/>
                            <TextBlock Text=" tâches" FontSize="11" Foreground="#555" VerticalAlignment="Bottom" Margin="3,0,0,2"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Liste des tâches -->
        <Border Grid.Row="2" Background="White" CornerRadius="6" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.1" Color="#00915A"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header de la liste -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="📋 Tâches du Projet" FontSize="16" 
                               FontWeight="Bold" Foreground="#1A1919"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBox x:Name="TxtRecherche" Width="200" Padding="8,5" 
                                 VerticalContentAlignment="Center" Margin="0,0,10,0">
                            <TextBox.Resources>
                                <Style TargetType="TextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Border Background="White" BorderBrush="#CCC" BorderThickness="1" 
                                                        CornerRadius="4" Padding="8,5">
                                                    <Grid>
                                                        <TextBlock Text="🔍 Rechercher..." Foreground="#999" 
                                                                   IsHitTestVisible="False" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        
                        <ComboBox x:Name="CmbFiltreStatut" Width="120" Padding="8,5" Margin="0,0,10,0">
                            <ComboBoxItem Content="Tous" IsSelected="True"/>
                            <ComboBoxItem Content="À faire"/>
                            <ComboBoxItem Content="En cours"/>
                            <ComboBoxItem Content="En test"/>
                            <ComboBoxItem Content="Terminé"/>
                        </ComboBox>
                        
                        <CheckBox x:Name="ChkAfficherArchivees" Content="📦 Afficher archivées" 
                                  IsChecked="True" VerticalAlignment="Center" 
                                  FontWeight="SemiBold" Foreground="#1A1919"/>
                    </StackPanel>
                </Grid>

                <!-- Liste scrollable -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ListeTaches">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1" 
                                        CornerRadius="6" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F0F0F0"/>
                                                    <Setter Property="BorderBrush" Value="#00915A"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <!-- Titre et priorité -->
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                <TextBlock Text="{Binding Titre}" FontSize="14" FontWeight="SemiBold" 
                                                           Foreground="#1A1919" VerticalAlignment="Center"/>
                                                <Border Background="{Binding PrioriteColor}" CornerRadius="3" 
                                                        Padding="6,2" Margin="10,0,0,0">
                                                    <TextBlock Text="{Binding Priorite}" FontSize="10" 
                                                               Foreground="White" FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>

                                            <!-- Info ligne -->
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="👤" FontSize="11" Margin="0,0,4,0"/>
                                                <TextBlock Text="{Binding DevNom}" FontSize="12" Foreground="#00915A" 
                                                           FontWeight="SemiBold" Margin="0,0,15,0"/>
                                                
                                                <TextBlock Text="📊" FontSize="11" Margin="0,0,4,0"/>
                                                <TextBlock Text="{Binding Statut}" FontSize="12" Foreground="#555" 
                                                           Margin="0,0,15,0"/>
                                                
                                                <TextBlock Text="⏱️" FontSize="11" Margin="0,0,4,0"/>
                                                <TextBlock FontSize="12" Foreground="#555">
                                                    <Run Text="{Binding ChiffrageJours, StringFormat={}{0:F1}}"/>
                                                    <Run Text="j estimé"/>
                                                </TextBlock>
                                                
                                                <TextBlock Text=" • " FontSize="12" Foreground="#CCC" Margin="5,0"/>
                                                
                                                <TextBlock FontSize="12" Foreground="#9C27B0" FontWeight="SemiBold">
                                                    <Run Text="{Binding TempsReelJours, StringFormat={}{0:F1}}"/>
                                                    <Run Text="j passé"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Progression -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Width="80">
                                            <TextBlock Text="{Binding ProgressionPct, StringFormat={}{0:F0}%}" 
                                                       FontSize="16" FontWeight="Bold" Foreground="#00915A" 
                                                       HorizontalAlignment="Right" Margin="0,0,0,3"/>
                                            <ProgressBar Value="{Binding ProgressionPct}" Height="6" 
                                                         Foreground="#00915A" Background="#E0E0E0" 
                                                         BorderThickness="0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>
