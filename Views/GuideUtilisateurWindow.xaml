<Window x:Class="BacklogManager.Views.GuideUtilisateurWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Guide d'utilisation - BacklogManager" 
        Height="650" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="CanResize">
    
    <Grid>
        <!-- Header -->
        <Border Background="#00915A" Height="80" VerticalAlignment="Top">
            <Grid Margin="30,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="📖 GUIDE D'UTILISATION" FontSize="24" FontWeight="Bold" 
                               Foreground="White"/>
                    <TextBlock x:Name="TxtRole" FontSize="14" Foreground="#E8F5E9" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Margin="0,80,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ContentPanel" Margin="30,25,30,30">
                <!-- Le contenu sera généré dynamiquement selon le rôle -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
