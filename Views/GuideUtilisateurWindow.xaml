<Window x:Class="BacklogManager.Views.GuideUtilisateurWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Caramel et Flopy vous guident 🐱🐰" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="CanResize">
    
    <Grid>
        <!-- Header -->
        <Border Background="#00915A" Height="80" VerticalAlignment="Top">
            <Grid Margin="30,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="💬 Posez vos questions à Caramel &amp; Flopy" FontSize="24" FontWeight="Bold" 
                               Foreground="White"/>
                    <TextBlock x:Name="TxtRole" FontSize="14" Foreground="#E8F5E9" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content avec Caramel et Flopy -->
        <Grid Margin="0,80,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Caramel et Flopy + Questions à gauche -->
            <Border Grid.Column="0" Background="White" Padding="15" Margin="20,20,10,20" 
                    CornerRadius="12" VerticalAlignment="Stretch">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2" Color="#00915A"/>
                </Border.Effect>
                <StackPanel>
                    <!-- Caramel & Flopy -->
                    <Image x:Name="ImgGuide"
                           Source="/Images/caramel-flopy-normal.png" 
                           Width="120" Height="120" 
                           Stretch="Uniform"
                           Margin="0,0,0,10"/>
                    <TextBlock Text="🐱 Caramel et Flopy 🐰" 
                               FontSize="16" 
                               FontWeight="Bold"
                               Foreground="#00915A"
                               TextAlignment="Center"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="Vos assistants personnels" 
                               FontSize="10" 
                               Foreground="#666"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Margin="0,0,0,20"/>

                    <!-- Questions prédéfinies -->
                    <TextBlock Text="❓ Questions rapides" 
                               FontSize="12" 
                               FontWeight="Bold"
                               Foreground="#333"
                               Margin="0,0,0,10"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="380">
                        <StackPanel x:Name="QuestionsPanel">
                            <!-- Les questions seront générées dynamiquement -->
                        </StackPanel>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <!-- Zone de conversation à droite -->
            <Border Grid.Column="1" Background="White" Padding="20" Margin="10,20,20,20" 
                    CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.15" Color="#00915A"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Message de bienvenue -->
                    <Border Grid.Row="0" Background="#E8F5E9" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock FontSize="14" FontWeight="SemiBold" Foreground="#00915A">
                                <Run Text="👋 Bonjour ! Nous sommes Caramel et Flopy, vos guides."/>
                            </TextBlock>
                            <TextBlock FontSize="12" Foreground="#666" Margin="0,8,0,0" TextWrapping="Wrap">
                                Cliquez sur une question à gauche pour que nous vous expliquions comment utiliser BacklogManager de manière simple et efficace. N'hésitez pas, il n'y a pas de question bête !
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- Zone de réponse scrollable -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ConversationPanel" Margin="0,0,10,0">
                            <!-- Les réponses de Caramel et Flopy apparaîtront ici -->
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
